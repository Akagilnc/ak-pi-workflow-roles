# Issue 26 reconciliation

- Target HEAD: `160b2327d5d80818bf3f80e46cdccc809bfb9f1c`
- Extracted occurrences: **7367** (deduplicated tracked UTF-8 lines).

## Extraction counts
- `catch-rejection`: 226
- `cause-persistence`: 3159
- `continuation-fallback`: 1624
- `failure-barrier-dispatch`: 3118
- `failure-construction-emission`: 3043
- `process-outcome`: 651
- `resource-cleanup`: 185

## Reconciliation verdicts
- `conforming`: 7219
- `documented_exception`: 29
- `violation`: 119

## Residue: 119 violation(s)

The repaired tree does **not** satisfy the zero-violation closure criterion. No repairs were made in this leg. Residues:
- `dist/exact-utf8.js:8` `catch-rejection` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/exact-utf8.js:8 is `    catch {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:302` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:302 is `    } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:308` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:308 is `    } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:314` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:314 is `    } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:320` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:320 is `    } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:339` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:339 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:356` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:356 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:369` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:369 is `      } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:421` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:421 is `      } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:488` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:488 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:495` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:495 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:510` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:510 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:517` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:517 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:541` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:541 is `      } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:573` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:573 is `    } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:579` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:579 is `    } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:588` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:588 is `    } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:595` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:595 is `    } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:668` `failure-construction-emission` — At dist/navigator-attendance.js:668, `const human = { ...error };` is part of the executable provider-error human projection/emission; it removes provider identity from the emitted error and provides no stable pointer to retained raw evidence.
- `dist/navigator-attendance.js:671` `failure-construction-emission` — At dist/navigator-attendance.js:671, `delete human.navigatorFailure;` is part of the executable provider-error human projection/emission; it removes provider identity from the emitted error and provides no stable pointer to retained raw evidence.
- `dist/navigator-attendance.js:672` `failure-construction-emission` — At dist/navigator-attendance.js:672, `return human;` is part of the executable provider-error human projection/emission; it removes provider identity from the emitted error and provides no stable pointer to retained raw evidence.
- `dist/navigator-attendance.js:690` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:690 is `          } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:716` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:716 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:717` `failure-construction-emission` — At dist/navigator-attendance.js:717, `wrapped.push({ ...event, message: result });` is part of the executable provider-error human projection/emission; it removes provider identity from the emitted error and provides no stable pointer to retained raw evidence.
- `dist/navigator-attendance.js:722` `failure-construction-emission` — At dist/navigator-attendance.js:722, `result = humanProviderError(event.error);` is part of the executable provider-error human projection/emission; it removes provider identity from the emitted error and provides no stable pointer to retained raw evidence.
- `dist/navigator-attendance.js:744` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:744 is `    } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:760` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:760 is `    } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:771` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:771 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:784` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:784 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:793` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:793 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/navigator-attendance.js:801` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/navigator-attendance.js:801 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `dist/reviewer-agent.js:49` `catch-rejection` — Applicable rule: every non-propagating catch continuation or cause-dropping wrap is a violation unless a valid cited continuation contract retains recoverable cause. At generated dist/reviewer-agent.js:49, Promise.allSettled settles child rejection into failed outcome with classification-only data; original cause is not retained, so this generated occurrence is a violation.
- `dist/reviewer-agent.js:53` `resource-cleanup` — Generated failure handling returns without disposing the acquired workspace; it mirrors the source skipped-disposal boundary.
- `dist/reviewer-bundle-materializer.js:51` `resource-cleanup` — Generated finally handle.close can replace a write/sync primary failure; it mirrors the source cleanup boundary and has no citation.
- `dist/reviewer-child-executor.js:196` `resource-cleanup` — Generated finally cleanup can throw while preserving no pending primary failure; it mirrors the source cleanup boundary and has no citation.
- `dist/reviewer-child-executor.js:218` `resource-cleanup` — Generated finally rm can throw after a fallible child execution and replace the primary failure; it mirrors the source boundary.
- `dist/reviewer-dispatch.js:30` `catch-rejection` — Proposal serialization failure is replaced by a sentinel hash, allowing dispatch identity processing to continue while dropping the original cause without citation.
- `dist/reviewer-dispatch.js:62` `catch-rejection` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at dist/reviewer-dispatch.js:62 is `            catch {` and continues/catches without such a citation (or discards the rejection).
- `dist/reviewer-execution-ledger.js:49` `failure-construction-emission` — Current generated line 49 is diagnostics: infrastructure-failure; source owner src/reviewer-execution-ledger.ts:111 has the same projection, which emits only a broad category and no retained raw-cause pointer. Source owner current line 111/216 is independently identified above; this key is its generated counterpart.
- `dist/reviewer-execution-ledger.js:166` `failure-construction-emission` — Current generated line 166 constructs a replacement Error from infrastructureFailure.diagnostics; source owner src/reviewer-execution-ledger.ts:216 does the same after the original failure was recorded, dropping cause/evidence. Source owner current line 111/216 is independently identified above; this key is its generated counterpart.
- `dist/reviewer-pinned-git.js:149` `process-outcome` — The commit rev-parse failure is immediately followed by stderr-based classification in this executable boundary.
- `dist/reviewer-workspace.js:31` `process-outcome` — Current line 9 is 'const child = spawn(command, args, { ...(options.cwd === undefined ? {} : { cwd: options.cwd }), stdio: ["ignore", "pipe", "pipe"], signal: options.signal });'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:48` `process-outcome` — Current line 18 is 'async function readRefs(cwd, signal) { return parseReviewerRefSnapshot((await git(cwd, reviewerRefSnapshotArgs(), signal)).stdout.trim()); }'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:50` `process-outcome` — Current line 20 is 'if ((await git(cwd, ["rev-parse", "--show-object-format"], signal)).stdout.trim() !== snapshot.objectFormat)'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:52` `process-outcome` — Current line 22 is 'const head = (await git(cwd, ["rev-parse", "HEAD^{commit}"], signal)).stdout.trim();'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:58` `process-outcome` — Current line 28 is 'await git(cwd, ["cat-file", "-e", `${entry.objectId}^{object}`], signal);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:60` `process-outcome` — Current line 30 is 'await git(cwd, ["cat-file", "-e", `${entry.peeledCommitId}^{commit}`], signal);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:71` `process-outcome` — Current line 41 is 'const objectFormat = (await git(accepted.repositoryRoot, ["rev-parse", "--show-object-format"], signal)).stdout.trim();'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:74` `process-outcome` — Current line 44 is 'const targetHead = (await git(accepted.repositoryRoot, ["rev-parse", "HEAD^{commit}"], signal)).stdout.trim();'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:84` `process-outcome` — Current line 54 is 'const present = await git(mirrorPath, ["cat-file", "-e", `${targetHead}^{commit}`], signal, [0, 1, 128]);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:86` `process-outcome` — Current line 56 is 'await git(mirrorPath, ["fetch", "--no-tags", accepted.repositoryRoot, targetHead], signal);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:87` `process-outcome` — Current line 57 is 'await git(mirrorPath, ["update-ref", "refs/ak-reviewer/target", targetHead], signal);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:94` `process-outcome` — Current line 64 is 'await git(mirrorPath, ["cat-file", "-e", `${object}^{object}`], signal);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:95` `process-outcome` — Current line 65 is 'await git(mirrorPath, ["config", "--remove-section", "remote.origin"], signal, [0, 5, 128]);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:99` `failure-construction-emission` — Current dist/reviewer-workspace.js:99 is `throw workspaceError(error, "snapshot", mirrorRoot === undefined ? "not-created" : { retained: mirrorRoot }, accepted);`. This catch emits through workspaceError, whose non-Error fallback drops the original cause while assigning the snapshot label
- `dist/reviewer-workspace.js:109` `process-outcome` — Current line 79 is 'await git(workspace, ["init", `--object-format=${snapshot.objectFormat}`, "--initial-branch=ak-reviewer-unborn"], signal);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:111` `process-outcome` — Current line 81 is 'await git(workspace, ["fetch", "--no-tags", "--force", "--update-shallow", snapshot.mirrorPath, snapshot.targetHead, "+refs/heads/*:refs/heads/*", "+refs/tags/*:refs/tags/*", "+refs/remotes/*:refs/remotes/*"], signal);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:112` `process-outcome` — Current line 82 is 'await git(workspace, ["config", "--remove-section", "remote.origin"], signal, [0, 5, 128]);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:113` `process-outcome` — Current line 83 is 'await git(workspace, ["checkout", "--detach", snapshot.targetHead], signal);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `dist/reviewer-workspace.js:119` `failure-construction-emission` — Current dist/reviewer-workspace.js:119 is `throw workspaceError(error, "workspace", workspace === undefined ? "not-created" : { retained: workspace }, target);`. This catch emits through workspaceError, whose non-Error fallback drops the original cause while assigning the workspace label
- `dist/reviewer-workspace.js:138` `failure-construction-emission` — Current dist/reviewer-workspace.js:138 is `throw workspaceError(error, "workspace", { retained: path }, frozenTarget);`. This catch emits through workspaceError, whose non-Error fallback drops the original cause while assigning the workspace label
- `extensions/role-runtime.ts:66` `process-outcome` — Executable pi.exec result uses stderr/stdout prose to classify a nonzero result as unavailable. At extensions/role-runtime.ts:66, current text is '  const result = await pi.exec("pi", ["--no-extensions", "--no-skills", "--no-prompt-templates", "--no-themes", "--no-context-files", "-e", extensionPath, "--ak-role", role, "--hel'. Exact current occurrence at extensions/role-runtime.ts:66; matched class: process-outcome.
- `extensions/role-runtime.ts:159` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at extensions/role-runtime.ts:159 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/canonical-skill-binding.ts:59` `catch-rejection,failure-construction-emission` — Catch path constructs a specific unavailable error from an arbitrary caught value, using message/String fallback while retaining cause but not a typed discriminant.
- `src/collector-github.ts:171` `catch-rejection,failure-construction-emission` — Rule requires exact propagation, infrastructure termination, or a cited typed negative/continuation result with recoverable cause; src/collector-github.ts:171 shows `} catch (error) { const detail = error instanceof Error ? error.message : String(error); throw new Error(`GitHub ${label} returned malformed JSON: ${detail}`);`, which drops or replaces the caught failure instead of retaining its identity/cause.
- `src/collector-ledger.ts:720` `catch-rejection,failure-construction-emission` — Owner seam: collector observe. Current src/collector-ledger.ts:720 is `} catch (error) {` with context [} | } catch (error) { | const message = error instanceof Error ? error.message : String(error);]; this exact boundary drops the caught rejection identity before public fatal emission.
- `src/navigator-attendance.ts:380` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at src/navigator-attendance.ts:380 is `  } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/navigator-attendance.ts:481` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at src/navigator-attendance.ts:481 is `      } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/navigator-attendance.ts:488` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at src/navigator-attendance.ts:488 is `      } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/navigator-attendance.ts:494` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at src/navigator-attendance.ts:494 is `      } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/navigator-attendance.ts:500` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at src/navigator-attendance.ts:500 is `      } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/navigator-attendance.ts:545` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at src/navigator-attendance.ts:545 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/navigator-attendance.ts:589` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at src/navigator-attendance.ts:589 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/navigator-attendance.ts:704` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at src/navigator-attendance.ts:704 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/navigator-attendance.ts:899` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at src/navigator-attendance.ts:899 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/navigator-attendance.ts:955` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at src/navigator-attendance.ts:955 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/navigator-attendance.ts:966` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at src/navigator-attendance.ts:966 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/navigator-attendance.ts:975` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at src/navigator-attendance.ts:975 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/navigator-attendance.ts:984` `catch-rejection,failure-construction-emission` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at src/navigator-attendance.ts:984 is `        } catch (error) {` and continues/catches without such a citation (or discards the rejection).
- `src/reviewer-agent.ts:41` `catch-rejection` — Applicable rule: every non-propagating catch continuation or cause-dropping wrap is a violation unless a valid cited continuation contract retains recoverable cause. At src/reviewer-agent.ts:41, code is `const settled = await Promise.allSettled(batch.workspaces.map(async workspace => {`. The rule forbids this continuation/classification because it does not expose a recoverable original cause at the failure boundary.
- `src/reviewer-agent.ts:43` `process-outcome,resource-cleanup` — The failure branch returns without disposing the acquired workspace, so disposal is skipped on child failure; no citation is present.
- `src/reviewer-child-executor.ts:213` `resource-cleanup` — finally begins fallible listener/unsubscribe/session cleanup after child execution and can replace the pending primary failure; no citation is present.
- `src/reviewer-child-executor.ts:227` `resource-cleanup` — finally begins fallible listener/unsubscribe/session cleanup after child execution and can replace the pending primary failure; no citation is present.
- `src/reviewer-pinned-git.ts:122` `catch-rejection,failure-construction-emission` — Frozen target 8c3eb9a and current src/reviewer-pinned-git.ts are byte-identical, so this occurrence is not moved or gone. Exact construct: `} catch (error) {`. Executable boundary: the surrounding catch normalizes a child-process/material-read failure. Failure identity is assigned from stderr prose or an untyped exit-code projection; the original rejection is not retained in a cause chain or stable evidence reference. This is specific fallback/prose relabeling and cause-dropping emission under fh26.dishonest-relabel-or-emission. Disposition: violation.
- `src/reviewer-pinned-git.ts:139` `catch-rejection,failure-construction-emission` — Frozen target 8c3eb9a and current src/reviewer-pinned-git.ts are byte-identical, so this occurrence is not moved or gone. Exact construct: `try { await gitText(repositoryRoot, ["merge-base", "--is-ancestor", commit, targetHead]); } catch (error) {`. Executable boundary: the surrounding catch normalizes a child-process failure. Failure identity is assigned from an untyped exit-code projection; the original rejection is not retained in a cause chain or stable evidence reference. This is specific fallback relabeling and cause-dropping emission under fh26.dishonest-relabel-or-emission. Disposition: violation.
- `src/reviewer-pinned-git.ts:149` `catch-rejection,failure-construction-emission` — Frozen target 8c3eb9a and current src/reviewer-pinned-git.ts are byte-identical, so this occurrence is not moved or gone. Exact construct: `} catch (error) {`. Executable boundary: the surrounding catch normalizes a child-process/material-read failure. Failure identity is assigned from stderr prose or an untyped exit-code projection; the original rejection is not retained in a cause chain or stable evidence reference. This is specific fallback/prose relabeling and cause-dropping emission under fh26.dishonest-relabel-or-emission. Disposition: violation.
- `src/reviewer-pinned-git.ts:175` `catch-rejection,failure-construction-emission` — Frozen target 8c3eb9a and current src/reviewer-pinned-git.ts are byte-identical, so this occurrence is not moved or gone. Exact construct: `} catch (error) {`. Executable boundary: the surrounding catch normalizes a child-process/material-read failure. Failure identity is assigned from stderr prose or an untyped exit-code projection; the original rejection is not retained in a cause chain or stable evidence reference. This is specific fallback/prose relabeling and cause-dropping emission under fh26.dishonest-relabel-or-emission. Disposition: violation.
- `src/reviewer-workspace.ts:34` `process-outcome` — Current line 28 is 'const child = spawn(command, args, { ...(options.cwd === undefined ? {} : { cwd: options.cwd }), stdio: ["ignore", "pipe", "pipe"], signal: options.signal });'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:45` `process-outcome` — Current line 37 is 'async function readRefs(cwd: string, signal?: AbortSignal): Promise<Record<string, ReviewerRefEntry>> { return parseReviewerRefSnapshot((await git(cwd, reviewerRefSnapshotArgs(), signal)).stdout.trim()); }'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:47` `failure-construction-emission,process-outcome,cause-persistence` — Current line 39 is 'if ((await git(cwd, ["rev-parse", "--show-object-format"], signal)).stdout.trim() !== snapshot.objectFormat) throw new Error("Review clone object format does not match the pinned session snapshot");'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:48` `process-outcome` — Current line 40 is 'const head = (await git(cwd, ["rev-parse", "HEAD^{commit}"], signal)).stdout.trim();'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:51` `process-outcome` — Current line 43 is 'for (const entry of Object.values(snapshot.refs)) { await git(cwd, ["cat-file", "-e", `${entry.objectId}^{object}`], signal); if (entry.peeledCommitId !== null) await git(cwd, ["cat-file", "-e", `${entry.peeledCommitId}^{commit}`], signal); }'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:61` `process-outcome` — Current line 53 is 'const objectFormat = (await git(accepted.repositoryRoot, ["rev-parse", "--show-object-format"], signal)).stdout.trim();'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:63` `process-outcome` — Current line 55 is 'const targetHead = (await git(accepted.repositoryRoot, ["rev-parse", "HEAD^{commit}"], signal)).stdout.trim();'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:68` `process-outcome` — Current line 60 is 'const present = await git(mirrorPath, ["cat-file", "-e", `${targetHead}^{commit}`], signal, [0, 1, 128]);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:69` `process-outcome` — Current line 61 is 'if (present.code !== 0) { await git(mirrorPath, ["fetch", "--no-tags", accepted.repositoryRoot, targetHead], signal); await git(mirrorPath, ["update-ref", "refs/ak-reviewer/target", targetHead], signal); }'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:72` `process-outcome` — Current line 64 is 'for (const object of new Set([targetHead, ...objects])) await git(mirrorPath, ["cat-file", "-e", `${object}^{object}`], signal);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:73` `process-outcome` — Current line 65 is 'await git(mirrorPath, ["config", "--remove-section", "remote.origin"], signal, [0, 5, 128]);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:75` `catch-rejection,failure-construction-emission` — At current src/reviewer-workspace.ts:75, the workspace catch passes the failure into workspaceError, whose construction at line 46 drops the original cause; this is the same executable cause-dropping boundary.
- `src/reviewer-workspace.ts:81` `process-outcome` — Current line 73 is 'dependencies.fault?.("workspace.init"); await git(workspace, ["init", `--object-format=${snapshot.objectFormat}`, "--initial-branch=ak-reviewer-unborn"], signal);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:82` `process-outcome` — Current line 74 is 'dependencies.fault?.("workspace.fetch"); await git(workspace, ["fetch", "--no-tags", "--force", "--update-shallow", snapshot.mirrorPath, snapshot.targetHead, "+refs/heads/*:refs/heads/*", "+refs/tags/*:refs/tags/*", "+refs/remotes/*:refs/remotes/*"], signal);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:83` `process-outcome` — Current line 75 is 'await git(workspace, ["config", "--remove-section", "remote.origin"], signal, [0, 5, 128]); await git(workspace, ["checkout", "--detach", snapshot.targetHead], signal);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `src/reviewer-workspace.ts:85` `catch-rejection,failure-construction-emission` — At current src/reviewer-workspace.ts:85, the workspace catch passes the failure into workspaceError, whose construction at line 46 drops the original cause; this is the same executable cause-dropping boundary.
- `src/reviewer-workspace.ts:103` `resource-cleanup` — src/reviewer-workspace.ts:103 executes cleanup 'async dispose(workspace) { await rm(workspace.path, { recursive: true, force: false }); return "deleted"; },'; this boundary has no cited typed race exception or primary-preserving combination.
- `test/collector-role.test.ts:2125` `catch-rejection` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at test/collector-role.test.ts:2125 is `        } catch {` and continues/catches without such a citation (or discards the rejection).
- `test/collector-role.test.ts:2216` `catch-rejection` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at test/collector-role.test.ts:2216 is `        } catch {` and continues/catches without such a citation (or discards the rejection).
- `test/collector-role.test.ts:2296` `catch-rejection` — Rule requires a valid adjacent Contract/ADR/Markdown citation for continuation or a cause-bearing termination; current code at test/collector-role.test.ts:2296 is `        } catch {` and continues/catches without such a citation (or discards the rejection).
- `test/helpers/pi-test-harness.ts:408` `resource-cleanup,cause-persistence` — test/helpers/pi-test-harness.ts:408 executes cleanup 'session.dispose();' in finally at line 430; no adjacent primary-preserving combination is present.
- `test/reviewer-pinned-reader.test.ts:2` `process-outcome` — Current line 2 is 'import { execFile, execFileSync } from "node:child_process";'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `test/reviewer-pinned-reader.test.ts:15` `process-outcome` — Current line 14 is 'const exec = promisify(execFile);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `test/reviewer-pinned-reader.test.ts:17` `process-outcome` — Current line 16 is 'return (await exec("git", ["-C", root, ...args])).stdout.trim();'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `test/reviewer-pinned-reader.test.ts:25` `process-outcome` — Current line 24 is 'const base = await git(root, "rev-parse", "HEAD"); await git(root, "branch", "review-base", base);'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `test/reviewer-pinned-reader.test.ts:28` `process-outcome` — Current line 27 is 'const blob = await git(root, "rev-parse", "HEAD:file");'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `test/reviewer-pinned-reader.test.ts:30` `process-outcome` — Current line 29 is 'const target = await git(root, "rev-parse", "HEAD");'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `test/reviewer-pinned-reader.test.ts:32` `process-outcome` — Current line 31 is 'const pinnedTagObject = await git(root, "rev-parse", "review-tag^{object}");'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `test/reviewer-pinned-reader.test.ts:67` `process-outcome` — Current line 62 is 'const base = await git(root, "rev-parse", "HEAD"); await git(root, "branch", "review-base");'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `test/reviewer-pinned-reader.test.ts:89` `process-outcome` — Current line 84 is 'const base = await git(root, "rev-parse", "HEAD");'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `test/reviewer-pinned-reader.test.ts:107` `process-outcome` — Current line 102 is 'const tree = await git(root, "rev-parse", "HEAD^{tree}");'; it is an executable process boundary whose settlement is not exposed with typed code, signal, timeout/abort, stderr, and retained cause.
- `test/reviewer-role.test.ts:143` `resource-cleanup` — finally recursively removes a temporary root after a fallible test body and rm can replace the primary failure; no citation is present.
