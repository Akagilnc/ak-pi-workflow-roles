# closure 产计数与 dockets 单账本（历史）

Status: accepted（historical）

原决定由调用方从 docket 人口确定性产出 StatsLine、医生只消费累积行、包不拥有 closure 调度。ADR 0017 起 Doctor 改读保留的 Pi session；Recorder、StatsLine 与工作树 docket 法已退役，Git history 是已删 `.ak/dockets/**` 的唯一档案，本页仅作历史记录。
