# 0082 — 三层运行时架构（角色本体 / 宿主适配 / 公开面）——誊自 #505 家族 owner 原话

Status: accepted（owner 2026-09-09「入」；直属通道，誊自 #505 家族 owner 原话）。本 ADR 不新增任何决定；每键绑定 #505/#508/#509/#510/#512/#513 票面所载 owner 原话（逐字），或票面记「owner 拍」之表述。真源票：#505（三层角色架构改造图，2026-09-05 关，家族 PR #648 并 main）。

## 为什么要立本 ADR
#505 家族的决定只在 issue 里；票庭/大理寺按 soul 只检索 docs/adr 与 CONTEXT.md，检索不到，于是 #644/#645/#646 宿主接入票过庭时无人以三层法封驳（2026-09-09 勘查：engine 轴只有 pi 消费；pi 不走共享信封；headless 抄 ACP 七处同形）。本 ADR 把既有决定搬进可检索的法源，供开票与判卷。

## Decision keys（键 | 值 | 绑定原话 | 出处）

| key | 值 | 绑定原话（逐字） | 出处 |
|---|---|---|---|
| `top-face-unchanged` | 顶层公开面不变：用户侧仍是 `ak-role <role>` 调角色做一件事，仅多 `--host` | 「用户侧感觉不到变化，发出的动作总是一样的 ak-roles 调用某个角色做一件事」 | #505 2026-08-28 |
| `middle-steps-abstract-hosts-only-implement` | 中层步骤抽象统一；接任何 CLI 只多出该宿主的实现，不多出第二套步骤 | 「中间层，根据host不同，走不同的实现路径，但是步骤应该是抽象出来基本上统一的，接多少cli也一样，只是多出不同的实现而已」；「步骤是抽象出来的步骤没错。但是宿主可以有不同的实现重写，而不是非要所有的宿主都共用一套严格的代码」 | #505 08-28；#752 |
| `bottom-role-host-agnostic` | 底层=角色本体（属性/行为/soul/法典/输入输出），宿主无关 | 「底层，只关心这个角色的属性，行为，soul，法典，输入输出 等。」 | #505 08-28 |
| `change-locator-one-layer` | 变更定位表：每类变更只落一层；跨层才能改成的设计即不合格 | 「要改用户体验，只需要动用户层；要改cli接入，行为，类似什么pi的fast补丁，什么超时重试机制，只需要看中间这层；要改角色，新增角色，只关心底层」 | #505 08-28 |
| `decoupling-bar` | 分层脱钩＝代码/逻辑零耦合、只经显式契约；不预设微服务 | 「逻辑上代码上要分层不要耦合。不要理解为现在就要考虑微服务」 | #505 08-28 勘正 |
| `institutions-stay-ak-side` | 承重墙：机构（闸/审刑院子会话/门下省派发/结算/家册）永留 AK 侧，宿主只供脑子轮次 | （票面记 owner 拍，无逐字） | #505 08-28 |
| `pi-no-privilege` | pi 是适配器之一，无特权，只是缺省宿主；终局移除 pi；切片不得加深 pi 耦合，pi 专有接缝只准在 pi adapter 内 | 「pi还不是变成一个插件，没有任何区别。但是调用方式理论上不变」；「我打算最终把pi去掉」；「理论上来说。三层架构的目标之一就是pi只是默认」 | #505 08-28；#505 08-29 方向令；卷 47ef0224 2026-09-06 |
| `unified-lifecycle-pipeline` | 所有角色走同一条可插拔管线：装载 soul/法典→输入检查(可插拔)→过程执行(可外包)→输出检查(门下省/审刑院/output 函数)；不适配者按类型开新管线，上限 2–3 根 | 「载入soul/法典… 输入检查(可插拔) 过程执行(可外包) 输出检查(门下省审刑院，output函数等)」；「管线不适用可以按照类型开新的管线。理论上最多2-3根管线。而且大部分角色我觉得肯定可以作为一个管线」 | #505 08-28；#512 08-28 |
| `pipeline-a-five-stages` | A 调用型管线五段（装载→受理→执行→交卷闸环→结算）；B 常驻型（游奕使）；插拔位=TS 接口+组合根 | （票面记 owner 逐项拍定，无逐字） | #512 08-28 |
| `sitian-normalize-cross-host` | 司天台接受不同 CLI 的输出，整理为一套格式存放；司天台=横切面（门面/Layout/Appender）；该记录的都记录 | 「司天台的工作方式也有变化。以前是存pi的输出就好。现在应该是接受不同cli的输出，整理为一套格式存放。多了一步」；「磁盘便宜。该记录的都要记录，除了那种纯噪音」 | #505/#513 08-28（后由 #617 DK-4 / ADR 0077 限定：格式各宿主自写，一致只经司天台入口） |
| `host-contract-four-verbs` | 宿主契约四组动词封顶：会话（开/续/关）/轮次/工具挂载/能力声明；交卷工具即闸入口；受控会话=硬要求 | 「门下省和审刑院的应用方式可能也会变化…如果不行的话这种衙门可以做成mcp的方式。」；其余票面记 owner 逐项拍定 | #508 08-28 |
| `host-flag-two-channels` | `--host` 旗标+席位配置双通道，旗标>配置>缺省 pi；未注册 host/模型×宿主不匹配一律响亮报错，禁静默顶替 | 「做个配置也不影响行为、改起来容易」；「报错，没得说，没有代劳」 | #510 08-28 |
| `transport-is-adapter-internal` | 运输面（ACP/headless 等）=适配器内政，不进公开面 | 「先选哪个其实不重要。都是可以接入的」 | #505 08-28 |
| `no-mechanism-for-pool-x-host` | 池令×host 不加机制：对不上响亮报错，改配置即可 | 「还要为了这个加机制？」「改个配置的事情很难吗」 | #505 08-28 |
| `two-axes-host-and-engine` | 中层含 `--host`（坐席宿主）与 `--engine`（劳务引擎）两轴；#505 那轮只施工宿主轴，engine 轴入图不动（ADR 0069 现役）——**未施工 ≠ 不属中层** | （票面记，无逐字） | #505 08-28 |
| `role-carrier-code-seams` | 角色本体载体=代码接缝，不造 manifest；souls/法典留 .md；两张角色注册表合一 | （票面记 owner 拍定） | #509 08-28 |
| `same-codebase-new-package` | 新 npm 包名=同一代码库就地分层，非重写、无双轨 | 「npm用一个新的包装。没说要重新写一套代码，别误会」 | #505 08-28 |
| `adversarial-review` | 设计票开庭必逐条对抗，不只说好话 | 「审的时候不要只说好话」 | #505 08-28 |

## 现状偏差（2026-09-09 勘查，供后续票引用，不在本 ADR 内裁决）
- `two-axes-host-and-engine`：engine 轴仅 pi 适配器消费（src/pi/role-turn-host.ts applyEngineChildEnv）；ACP/headless 零命中。
- `pi-no-privilege` / `middle-steps-abstract-hosts-only-implement`：pi 不走共享角色信封（自组 argv）；ACP 与 headless 适配器七处同形重复（retry 环、abort 合并、host-aborted、prior-native 拼接、串行包装、session 绑定、宿主表两建）。
- `sitian-normalize-cross-host`：非 pi 宿主运行中零 turn 记录（#811）。
- 内闸席宿主随父腿（public-role-summons.ts parentInvocationHost）压过席位表行：#645 施工自加，无原话。

## 关系
- 承继 #474（薄坐席+两轴）、ADR 0010（调用者拥有组合）、ADR 0069/0071（引擎轴）、ADR 0065/0077（司天台记录）、ADR 0066（typed 闸）。
- 与 CONTEXT.md「角色不含拓扑」一致。
