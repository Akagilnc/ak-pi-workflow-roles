# 引擎绕行失败＝整条 run 失败；必须调用是软规则

Status: accepted

引擎绕行失败（真被调用后 spawn 失败、非零退出、空输出等）返回 typed 失败并停止，整条 run 失败；禁止座席顶班，不得静默换用其他引擎 id，原 engineLaborFallback 申报废止。对腿的指令仍要求真调用；但「必须调用」对衙门是软规则——席位挂了 engine 而零次调用，不构成交卷不合法，任何衙门不得据本条撤销、作废、重跑该 run 或拒绝结算其产出。runner 义务只到派单要求外包；未配型号的席位按既有引擎材料办，runner 不得发明型号。
