# 0071 — 引擎绕行失败＝整条 run 失败；禁座席顶班、禁跳过绕行

Status: accepted（#483 拆除座席顶班实现并经三轮复核庭收敛，PR #491 已并入 main，2026-08-27 闭环转态）

**决策**：引擎绕行是**强制**的——席位必须实际调用 CLI。绕行失败＝**返回 typed 失败并停止，整条 run 失败**。失败后在座劳务**禁止**；**零次调用即在座劳务同样禁止**（没试过 ≠ 试了失败，不是顶班而是违规）。不得静默换用其他引擎 id。

**法源**：owner 2026-08-22 逐字令「我现在要的就是没有 backup。我现在的话大于我之前的话」——**明文推翻**本 ADR 原决策（座席顶班＋`engineLaborFallback` 申报）及其所据的 owner 2026-08-17 三道令中「顶班是合法 backup」一条。

**为什么改**：顶班让「外包」变成可选项——席位够强时会直接在座干完，`--engine` 形同虚设（实测：多条 judge run 的 `ak_engine_detour` 调用次数为 0，无 failure 可申报）。要外包真发生，唯一办法是让不外包等于失败。

**被推翻的内容**：原「座席顶班（原主路）照常劳务并交卷」＋`engineLaborFallback` 三键申报（`engine`/`failure`/`laborBy`）。申报机制随顶班一并废止——没有顶班就没有要申报的顶班。

**同步改动**：`resources/engines/{agy,opencode,grok-4.6,ox-alpha}.md` 的 Failure handling 段已改硬为同一措辞。

---

## 说明（owner 2026-09-12 逐字）

> 没有这种判断。我说了外包只是软规则。不得按照这个判不合法交卷

> 你派单尽量给指令让它外包就行了。实在不遵守那也没法。暂时就这样

> 0071 改一下。后面加一句说明。这是写在 md 里面的软规则，实际不用这个拦写好的工作

**本条关于「必须调用」的部分是软规则**——它写在 md 里，供派单与席位配置据以要求外包；**实际不用它拦已经写好的工作**。

**射程（只此一条，不扩大）**：席位挂了 `engine` 而某条 run **零次调用**该引擎，**不构成交卷不合法**。任何衙门（判官、票庭、闸官）**不得据本条**撤销、作废、重跑该 run，或拒绝结算其产出。runner 的义务只到派单——在派单文里要求外包；席位已配 `engineModel`（或派工令点名型号）时一并写明该型号，**未配型号的席位按既有引擎材料办，runner 不得发明型号**。腿不照办，不再追究。

**不在射程**：引擎**真被调用后失败**（spawn 失败、非零退出、空输出等）的处置**不受本节影响**——正文「绕行失败＝返回 typed 失败并停止，整条 run 失败」与 `src/engine-detour-tool.ts` 经基础设施接缝中止的现行行为**照旧**。本节软化的只是「调不调」的合规执行，不是「调了之后坏了」的失败诚实。

依据即本条法源栏所引的 owner 原话「我现在的话大于我之前的话」。

（背景：2026-09-12 实盘六条挂 cursor 的腿里五条零次调用，两庭据本条正文把已完成且经独立复核的修理 run 判为「不合法交卷、须重跑」，陛下当场否定该读法。御批冻结件：`~/WorkSpace/vault/ak-cc-wiki/raw/ming-llm-runner/2026-09-12/owner-ruling-0912-engine-soft-rule.md`。）
