# 劳动外包：引擎通用可插拔，治理一套逻辑

Status: accepted

角色劳动段可外包给本地 CLI 引擎，能力通用可插拔；治理面一概不动——角色 session 仍在 pi 里运行，外包内容交回同一 session 走既有 typed 交卷，不设第二条交卷/校验路径。形状是在干活一步造岔路、干完回主路。引擎权威与模型一样是 owner 池令；hang 处置等真实案例再议。弃用 MCP 桥交卷与确定性文件契约。
