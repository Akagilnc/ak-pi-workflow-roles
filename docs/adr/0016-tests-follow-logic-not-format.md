# 0016 — 测试跟逻辑走，不跟格式走

Status: proposed（2026-07-31，owner 拍定）

Pi 交卷层原生强制的 shape 校验不写测试；测试只覆盖自写逻辑（跨字段律、编译分支），每段先过护栏三问，过者至多一条真缝 tracer。理由：格式由运行时每单强制，测试重复它是纯成本；测试唯一该堵的是「坏了没人知道」的静默面。实证：2026-07-31 合同波全案仅留一条 scopeKeys 编译 tracer（圈界静默失效＝无声漏钱、无下游兜底）。
