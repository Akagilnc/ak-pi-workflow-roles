# targetHead 绑定闸 deferred

Status: deferred

大理寺回执的 targetHead 机械绑定闸本期不建：standalone 世界没有绑定方，建闸即预造。拉动时的施工图保留为——回执 schema 可含可选 targetHead，绑定方经单一 env 注入完整 SHA，交卷工具在 soul 审计前 fail-closed 全等校验；不造通用绑定框架；收货调用方保留单行全等 assert 兜底。「每次运行现造 schema-const + 整份 env 注入」不复活；大理寺应报告所判 head 属 soul 层要求，不待机械闸。
