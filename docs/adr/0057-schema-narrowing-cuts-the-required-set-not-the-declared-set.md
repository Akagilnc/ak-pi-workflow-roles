# 交卷 schema 仅三态判别必填

Status: accepted

交卷工具 schema 除三态判别字段（枚举并必填，作宿主生成端约束）外 required 为空；其余字段保留声明与 description、不必填，顶层允许额外属性。声明不是包侧校验授权：包不因格式拒收或判死；判别字段没填或读不出时只说读不出并附收到的值，不在代码里写状态名。key 名字不必逐字相同，合不合格由审刑院按是否读得懂来判。（#1055 裁定，源卷 d634a8d9）
