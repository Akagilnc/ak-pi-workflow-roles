# ADR 体例归 skill，本仓不另立；0078 四条承继作废

Status: accepted

本仓 ADR 的写法以 domain-modeling skill 的 ADR-FORMAT 为真源（标题加简述 context、决定与理由），不另立体例。ADR 0078 曾要求决策键表增设承接列、单元格只写 — 或 #x、表内不记实施状态、读表方用 gh 现场核验——这四条决定由本 ADR 承继作废；0078 文件删除，跨文引用改为 ADR 编号加决定句，键名不再充当引用锚。
