import{execFile}from"node:child_process";import{promisify}from"node:util";import{readFile,mkdir,writeFile,open}from"node:fs/promises";import{constants}from"node:fs";import{join}from"node:path";import type{AssistedCallConfigV1}from"./assisted-contracts.ts";import{canonicalSnapshotDigestV1,type CurrentPositionSnapshotV1,type SettledAttemptV1}from"./navigator-contracts.ts";import{sha256Hex}from"./sha256.ts";import{assistedRunDirectory}from"./assisted-ledger.ts";const exec=promisify(execFile);
export type GitWorkspaceObservationV1={head:string;target:string;relation:"repository"|"worktree"};export interface AssistedGitTransportV1{observeWorkspace(root:string):Promise<GitWorkspaceObservationV1>};export type GitHubIssueObservationV1={id:string;state:"open"|"closed";labels:Array<{id:string;name:string}>;body:string|null;observedAt:string};export interface AssistedGitHubTransportV1{repository(owner:string,name:string):Promise<{id:string}>;issue(owner:string,name:string,number:number):Promise<GitHubIssueObservationV1>};
export function createGitCliTransportV1():AssistedGitTransportV1{return{async observeWorkspace(root){const[{stdout:head},{stdout:common}]=await Promise.all([exec("git",["rev-parse","HEAD"],{cwd:root}),exec("git",["rev-parse","--git-common-dir"],{cwd:root})]);return{head:head.trim(),target:head.trim(),relation:common.trim()===".git"?"repository":"worktree"}}}}
export function createGhJsonTransportV1(env:NodeJS.ProcessEnv=process.env):AssistedGitHubTransportV1{async function api(path:string){const{stdout}=await exec("gh",["api",path],{env,maxBuffer:8*1024*1024});return JSON.parse(stdout)as any}return{async repository(owner,name){const x=await api(`repos/${owner}/${name}`);if(typeof x.node_id!=="string")throw new Error("invalid GitHub repository response");return{id:x.node_id}},async issue(owner,name,number){const x=await api(`repos/${owner}/${name}/issues/${number}`);if(typeof x.node_id!=="string"||(x.body!==null&&typeof x.body!=="string")||!(x.state==="open"||x.state==="closed")||!Array.isArray(x.labels))throw new Error("invalid GitHub issue response");return{id:x.node_id,state:x.state,body:x.body,labels:x.labels.map((l:any)=>{if(typeof l.node_id!=="string"||typeof l.name!=="string")throw new Error("invalid GitHub label response");return{id:l.node_id,name:l.name}}),observedAt:new Date().toISOString()}}}}
export type AcquiredPositionV1={snapshot:CurrentPositionSnapshotV1;handles:Map<string,Uint8Array>};
export const MAX_ASSISTED_EVIDENCE_BYTES=8*1024*1024;
async function readBoundedRegular(path:string){const descriptor=await open(path,constants.O_RDONLY|constants.O_NONBLOCK|("O_NOFOLLOW" in constants?constants.O_NOFOLLOW:0));try{const stat=await descriptor.stat();if(!stat.isFile())throw new Error("evidence must be a regular file");if(stat.size>MAX_ASSISTED_EVIDENCE_BYTES)throw new Error("evidence exceeds size bound");const bytes=new Uint8Array(stat.size);let offset=0;while(offset<bytes.length){const {bytesRead}=await descriptor.read(bytes,offset,bytes.length-offset,offset);if(!bytesRead)throw new Error("evidence changed during admission");offset+=bytesRead}const after=await descriptor.stat();if(after.size!==stat.size||after.mtimeMs!==stat.mtimeMs||after.ino!==stat.ino)throw new Error("evidence changed during admission");return bytes}finally{await descriptor.close()}}
export async function acquireCurrentPositionV1(config:AssistedCallConfigV1,positionCursor:number,latestAttempt:SettledAttemptV1|null,deps:{git:AssistedGitTransportV1;github:AssistedGitHubTransportV1}):Promise<AcquiredPositionV1>{const repo=await deps.github.repository(config.subject.github.owner,config.subject.github.name),parent=await deps.github.issue(config.subject.github.owner,config.subject.github.name,config.subject.parentIssue),childObs=await Promise.all(config.subject.children.map(c=>deps.github.issue(config.subject.github.owner,config.subject.github.name,c.number))),workObs=await Promise.all(config.acquisition.workspaces.map(w=>deps.git.observeWorkspace(w.root)));const handles=new Map<string,Uint8Array>(),evidence:CurrentPositionSnapshotV1["evidence"]=[];function admit(id:string,kind:CurrentPositionSnapshotV1["evidence"][number]["kind"],source:Uint8Array,reference:string,handle:string){if(evidence.some(x=>x.id===id))throw new Error("duplicate evidence id");if(source.byteLength>MAX_ASSISTED_EVIDENCE_BYTES)throw new Error("evidence exceeds size bound");const bytes=new Uint8Array(source);handles.set(handle,bytes);evidence.push({id,kind,sha256:sha256Hex(bytes),provenance:{kind:"acquired",reference},handle})}admit("parent-issue-body","issue_body",new TextEncoder().encode(parent.body??""),`github-issue:${config.subject.parentIssue}`,"memory:parent-body");for(let i=0;i<childObs.length;i++)admit(`child-${config.subject.children[i]!.number}-issue-body`,"issue_body",new TextEncoder().encode(childObs[i]!.body??""),`github-issue:${config.subject.children[i]!.number}`,`memory:child-${i}-body`);for(const d of config.acquisition.evidence)admit(d.id,d.kind,await readBoundedRegular(d.path),`${d.provenance.kind}:${d.provenance.reference}`,d.path);if(latestAttempt&&(latestAttempt.reference.id.startsWith("receipt:")||latestAttempt.reference.id.startsWith("failure:"))){const [artifactKind,artifactInvocation]=latestAttempt.reference.id.split(":");if(artifactInvocation!==latestAttempt.invocationId)throw new Error("settlement artifact identity mismatch");const runDir=assistedRunDirectory(config.subject.repositoryRoot,config.subject.parentIssue,config.runId),path=artifactKind==="receipt"?join(runDir,"invocations",artifactInvocation,"receipt.json"):join(runDir,"invocation-inputs",artifactInvocation,"failure.json"),bytes=await readBoundedRegular(path);if(sha256Hex(bytes)!==latestAttempt.reference.sha256)throw new Error("settlement artifact digest mismatch");admit(`settlement:${artifactInvocation}`,artifactKind==="receipt"?"acceptance":"failure",bytes,latestAttempt.reference.id,path)}evidence.sort((a,b)=>a.id.localeCompare(b.id));const evidenceRoot=join(assistedRunDirectory(config.subject.repositoryRoot,config.subject.parentIssue,config.runId),"evidence");await mkdir(evidenceRoot,{recursive:true});for(const item of evidence){const bytes=handles.get(item.handle)!;const path=join(evidenceRoot,item.sha256);try{await writeFile(path,bytes,{flag:"wx",mode:0o600})}catch(error){if((error as NodeJS.ErrnoException).code!=="EEXIST")throw error;if(sha256Hex(await readFile(path))!==item.sha256)throw new Error("evidence content-address collision")};handles.delete(item.handle);item.handle=path;handles.set(path,bytes)}const common={observedAt:parent.observedAt,query:{transport:"github_rest" as const,operation:"issue"}};const base:Omit<CurrentPositionSnapshotV1,"digest">={version:1,capturedAt:new Date().toISOString(),runId:config.runId,subject:{repositoryRoot:config.subject.repositoryRoot,github:{...config.subject.github,id:repo.id},parent:{number:config.subject.parentIssue,id:parent.id}},children:config.subject.children.map((c,i)=>({...c,id:childObs[i]!.id,state:childObs[i]!.state,labels:childObs[i]!.labels,observedAt:childObs[i]!.observedAt,query:{transport:"github_rest",operation:"issue"}})),parentObservation:{state:parent.state,labels:parent.labels,...common},labelPolicy:config.acquisition.labelPolicy.map(x=>({...x})),workspaces:config.acquisition.workspaces.map((w,i)=>({id:w.id,root:w.root,relation:workObs[i]!.relation,head:workObs[i]!.head,target:workObs[i]!.target})),evidence,positionCursor,latestAttempt};return{snapshot:{...base,digest:canonicalSnapshotDigestV1(base)},handles}}
