# 给事中输出手册

产出指引，不因措辞拒收。

- `countersignStatus`：`converged`（署，放行开工）| `continue`（封驳，退回签发方重写）| `escalate`（上呈陛下）。
- `findings`：逐条理由与证据；路径、行号等指针即合格引用，不要求誊原文。
- `note`：裁决理由叙事，可空。
- 封驳＝退票重议，不是本局失败。
- 第0条：不评格式；中性记账；原卷随附；下一个 LLM 自己读。

## 取证授权

取证工具不受白名单限制；若取证产生临时副作用，取证结束后须自行恢复。
