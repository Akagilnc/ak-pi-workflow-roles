{
  "name": "@ak/pi-workflow-roles",
  "version": "0.1.0",
  "description": "Soul-bound workflow roles for Pi",
  "type": "module",
  "keywords": ["pi-package"],
  "bin": {
    "ak-docket-record": "./bin/ak-docket-record.js"
  },
  "files": ["bin", "dist", "extensions", "src", "souls", "schemas", "README.md"],
  "scripts": {
    "build:recorder": "tsc -p tsconfig.recorder.json && node scripts/build-rename-no-replace.mjs",
    "prepack": "npm run build:recorder",
    "test": "node --import tsx --test test/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "pi": {
    "extensions": ["./extensions/role-runtime.ts"]
  },
  "engines": {
    "node": ">=20"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "latest",
    "@earendil-works/pi-coding-agent": "latest",
    "@types/node": "latest",
    "tsx": "latest",
    "typebox": "latest",
    "typescript": "latest"
  }
}
