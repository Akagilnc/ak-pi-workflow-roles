{
  "status": "planned",
  "report": "Plan at HEAD `1e6189e` (clean tracked worktree; no session inspection, no changes made).\n\n**Behavior**\n- `Authorization: Token plainsecrettokenvalue999` is one authorization credential boundary: scanning replaces the header, generic scheme, and complete credential together, leaving neither the full credential nor any suffix.\n- Existing composed/quoted Basic and Bearer behavior, standalone Basic/Bearer rules, every other scanner rule, admission semantics, and surrounding non-secret boundaries remain unchanged.\n\n**Owner**\n- Change only the existing `authorization-header` rule in `src/recorder/scanner.ts`; `scanString`/`scanBytes` remain the sole scanner used by the existing `storeOnce` external-input admission path. Add no scanner or admission mechanism.\n\n**Red**\n1. Extend the focused Authorization scanner regression in `test/recorder-extract-scan.test.ts` with the generic `Token` example and assertions that `authorization-header` fires and neither `plainsecrettokenvalue999` nor `[REDACTED] plainsecrettokenvalue999` survives. Confirm this fails at current HEAD.\n2. Extend the existing non-structural external-input admission regression (not a scratch/probe duplicate) so an admitted file contains the generic Authorization header; assert successful promotion and absence of the complete credential and meaningful suffix from the promoted input and all docket files. Confirm current HEAD exposes the suffix.\n\n**Green**\n- Adjust the one existing authorization-header regex to consume a generic scheme plus its complete credential atomically while retaining its quoted and Basic/Bearer alternatives and CR/LF bounds.\n- Run the focused scanner/admission tests first, then `npm run build:recorder` to regenerate tracked `dist/recorder/scanner.js` (native output remains ignored), `npm run typecheck`, `npm test`, and `npm pack --dry-run --json`; remove generated pack scratch if any and prove `git status` contains only authorized source/test/dist changes before commit and is clean afterward.\n\n**Scope**\n- Expected files: `src/recorder/scanner.ts`, `test/recorder-extract-scan.test.ts`, and regenerated `dist/recorder/scanner.js` only. No Souls/docs/schema/runtime admission changes, no AC relaxation, and no session access.\n- Create exactly one forward commit from `1e6189e`, titled `fix(recorder): close generic authorization boundary`, with root cause, tests, and verification in the body; verify new HEAD is its strict descendant. Return the completed Fixer Receipt with commit SHA and verification evidence."
}
