{
  "status": "planned",
  "report": "| R# | Plan |\n|---|---|\n| R1 | In `src/recorder/scanner.ts`, change only the existing `authorization-header` regex so every separator currently expressed with vertical-capable `\\s` uses horizontal-only space/tab matching, while line-start recognition does not consume CR/LF. Permit a zero-length post-separator value so empty and horizontal-whitespace-only Authorization fields redact only that field and preserve CR, LF, CRLF, and the following line. Keep the sole rule, scanner loop, and admission path intact; retain complete nonempty field consumption, escaped whole-quoted parameters, Basic/Bearer/Token, and assignment behavior. Extend `test/recorder-extract-scan.test.ts` with focused empty and whitespace-only matrices for CR, LF, and CRLF, asserting the Authorization field is redacted and exact line boundaries/following content survive; add the same boundary cases to the existing admitted external-input promotion regression and assert promoted input/artifacts preserve following lines without leakage. Regenerate tracked `dist` via `npm run build:recorder`. In Apply, run focused recorder tests, `npm run typecheck`, full `npm test`, isolated/real package verification with `npm pack --dry-run --json`, and `git diff --check`; verify the tree contains only R1 changes, create exactly one new forward `fix(recorder): ...` commit (no amend), then confirm clean tree and forward-descendant HEAD. |"
}
