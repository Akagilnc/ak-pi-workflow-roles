# Repair packet: Git-reference identity fail-closed

At current HEAD, in the existing Git-reference admission seam fail closed whenever scanning changes identity-bearing repositoryRoot or path, before returning/admitting the artifact. Preserve verified exact original identity and do not invent replacement coordinates. Add end-to-end pre-spawn/no-promotion regression using a valid credential-shaped repository coordinate, plus ordinary control; focused/typecheck/full, one forward commit and Receipt. Do not inspect sessions.
