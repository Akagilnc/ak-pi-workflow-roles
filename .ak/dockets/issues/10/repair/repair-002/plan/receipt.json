{
  "status": "planned",
  "report": "Plan at exact clean HEAD `5b63636506eb8673601b4457882fc0f8e8b51eb1` (all five supplied SHA-256 values independently reproduced).\n\n**Behavior** — Add one new append-only trail artifact, proposed path `.ak/dockets/issues/10/judgment/review-findings-002/trail-correction-001.md`. It will (1) list the five repository-relative paths and exact supplied SHA-256 identities in a seal table; (2) state that all prior accepted bytes remain unchanged; and (3) explicitly dispose the omitted checklist beat: the caller omitted a separate Apply Judge invocation for this documentary repair. It will distinguish the Fixer `completed` Receipt as construction evidence, state that review-findings-002 adjudicates existing Apply evidence and the Reviewer finding only, and expressly deny that this Review Judge Receipt is a retroactive Apply Receipt or that a new Apply invocation is required.\n\n**Owner** — The new issue-10 review-findings-002 trail correction owns only the missing artifact identities and omission disposition. Existing Receipts/manifests/tasks/amendment/bootstrap correction remain their respective owners and are citation targets only. Acceptance will preserve the correction in the new forward commit and cite its independently computed exact-byte SHA-256 in that commit’s body, avoiding a self-hash/circular second artifact.\n\n**Red** — Before editing, demonstrate that no correction/disposition artifact exists under `review-findings-002`, and that the current trail has only path-level references for the five named artifacts plus no explicit disposition of the separate Apply Judge omission. Preserve the reproduced five-hash output as baseline evidence.\n\n**Green** — After adding only the new file: run `sha256sum` over all five cited artifacts and require exact equality with the Judge values; compute `sha256sum` for the new correction for the acceptance citation; inspect its text to require all five path/hash pairs and the three role-topology distinctions (omitted separate Apply Judge, Fixer Receipt is construction evidence not verdict, current Review Judge is not an Apply Receipt); use `git diff --check`; verify `git diff --name-only` contains exactly the proposed new path; and verify the five cited files plus every pre-existing artifact remain byte-identical. In apply phase, create one new forward `docs(docket): ...` commit whose body cites the correction path and exact-byte digest, then confirm HEAD strictly descends from `5b63636506eb8673601b4457882fc0f8e8b51eb1`.\n\n**Scope** — One newly added Markdown trail correction and one forward commit only. No edits to manifests, Receipts, review task, amendment, prior correction, docs, tests, runtime, Recorder implementation, accepted range, or historical commits; no new role invocation or broad resealing."
}
