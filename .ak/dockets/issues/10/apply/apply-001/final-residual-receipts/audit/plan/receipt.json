{
  "status": "planned",
  "report": "Plan at exact clean HEAD `c82c9a4717b802e28018330d63297581cb67f9c7` (no files changed; no session payload inspected).\n\n**Behavior**\n- Add a production-shaped, fully bound Judge or Reviewer lifecycle whose `toolCallId` is itself credential-shaped. Exercise the existing issuance → start → accepted terminal extraction path, not a forged/bare result.\n- On success, prove the raw marker is absent from the extracted/stored Receipt, `audit-observation.json`, the in-memory and persisted final manifest (the staged manifest bytes subsequently atomically promoted), and `redaction-report.json`; also prove Receipt/audit identity remains coherently joined after sanitization and the expected artifacts/report hits exist.\n- Add a failure-path run using the same credential-bearing lifecycle and an existing production failure trigger (destination collision). Prove public failure JSON contains no raw marker and no complete final docket is published.\n- Remove exactly the second trailing newline at EOF from `test/recorder-transaction.test.ts`. Preserve `.ak/dockets/issues/10/authority/materials.md` and all sealed packet/authority bytes.\n\n**Owner / existing seams**\n- Regression owner: `test/recorder-extract-scan.test.ts`, using its lifecycle builders, Recorder harness, secret/tree assertions, and production `runRecorder` path.\n- Reuse only `extractAcceptedReceipt` → existing `scanJsonValue`/`scanString` → `storeGeneratedJson` → `buildManifest`/final scan closure → atomic promotion, plus existing `serializePublicFailure` scanning. No scanner rule, production hook, schema, runtime path, or parallel sanitization mechanism is planned.\n- Hygiene owner: final byte only in `test/recorder-transaction.test.ts`.\n\n**Red**\n- First add the focused end-to-end counterexample with a credential-shaped bound call identity and explicit raw-marker assertions over extraction and all promoted/public outputs. The current coverage is red by absence: existing end-to-end matrix uses only Coder and never requires `audit-observation.json`; the existing Judge/Reviewer test checks binding/usage but not credential-bearing promotion or storage.\n- The failure half explicitly observes the existing public failure JSON seam after the credential-bearing child output and verifies non-publication.\n\n**Green**\n- The production lifecycle is accepted as Judge/Reviewer, creates both Receipt and audit observation, and their sanitized `toolCallId` values remain equal in Receipt, audit observation, and manifest.\n- Raw credential value is absent from every file in the final docket and specifically from `receipt.json`, `audit-observation.json`, `manifest.json`, and `redaction-report.json`; report entries remain structural (`ruleId`, `location`, `count`) only.\n- Failure JSON has recorder failure semantics unchanged, contains no raw value, and no final `manifest.json` is published.\n- No production change is expected unless the new test exposes a real gap at an existing seam; any such repair will be minimal at that owning seam and will not add a scanner/hook.\n\n**Scope / verification / commit**\n1. Modify only `test/recorder-extract-scan.test.ts` for the proof and remove only the active EOF blank in `test/recorder-transaction.test.ts` (plus generated `dist` only if an evidenced production repair becomes necessary).\n2. Run focused Recorder extraction/storage tests (`node --import tsx --test test/recorder-extract-scan.test.ts`), `npm run typecheck`, exact full `npm test`, and `git diff --check 6e76efa HEAD`/working-tree diff checks. Accept only the pre-existing sealed `.ak/dockets/issues/10/authority/materials.md:46` finding; the transaction-test EOF finding must disappear.\n3. Verify sealed authority/material packet hashes are unchanged and the worktree contains only authorized changes.\n4. Create exactly one forward commit, expected title `test(recorder): prove audit credential sanitization` (use `fix(recorder): ...` only if a production defect is actually repaired), with body recording root cause/coverage and preserved behavior. Do not amend or rewrite.\n5. Confirm new HEAD is a strict descendant of `c82c9a4`; provide the exact final Receipt with commit SHA, changed paths, focused/typecheck/full/diff results, sealed-byte verification, and explicit dispositions for the audit proof and EOF hygiene."
}
