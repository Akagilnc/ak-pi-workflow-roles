{
  "status": "planned",
  "report": "## Behavior\n- Remove the generated trailing blank in `dist/recorder/errors.js` through its owner: minimally reformat the multiline `RecorderError` constructor declaration in `src/recorder/errors.ts`, run `npm run build:recorder`, and verify the generated file has no trailing whitespace. No runtime behavior or public contract changes.\n- Add one new append-only closure record under `.ak/dockets/issues/10/migration/v3-reconstruction/`; do not edit the existing reconstruction report, migration inventories, historical Receipts/manifests, or `.ak/dockets/issues/10/apply/apply-001/coder-incomplete.json`.\n- The closure record will capture commands and observed results for final-tree sizing, ancestry, seals, omissions, prohibited-payload absence, mergeability, package inventory/install, diff hygiene, residue, and status. No archived payload is opened.\n- Git cannot embed a commit’s own SHA in bytes belonging to that same commit. With the mandated single forward commit/no amend, the append-only record will identify Final HEAD mechanically as the commit introducing that record and provide the exact resolving command; the returned Receipt will state the resulting exact SHA. This preserves the authority’s no-self-seal rule rather than fabricating a literal self-reference.\n\n## Owner\n- Source/generation owner: `src/recorder/errors.ts` plus regenerated `dist/recorder/errors.js` via `tsconfig.recorder.json` / `npm run build:recorder`.\n- Evidence owner: one new append-only Markdown record beside `.ak/dockets/issues/10/migration/v3-reconstruction/final-recorder-reconstruction.md`.\n- Git and the sealed inventories remain the measurement authority. The existing missing-Coder disposition remains authoritative and byte-identical.\n\n## Red\n- Reproduce `grep -nE '[[:blank:]]+$' dist/recorder/errors.js`, currently reporting line 20 (`constructor(code, `).\n- Before committing, fail closure on any of: generated trailing whitespace; generated diff beyond the intended formatting; failed typecheck/test/package install; package inventory drift; production-range whitespace errors; changed retained seal; missing/changed omission or Coder disposition; failed default ancestry; newly added generic session/tool-event path; merge conflict; inconsistent logical-byte totals; tarball/consumer/scratch/symlink residue; or unrelated dirty files.\n\n## Green\n- Regeneration emits a whitespace-clean `dist/recorder/errors.js` with no semantic output change.\n- All exact gates below exit 0, inventories/counts are recorded as observed (not predicted), all 71 retained items match OID/SHA-256/length, all 28 omission dispositions remain, `lawfulReceipt` remains `null`, and no Coder Receipt is manufactured.\n- The final Receipt gives the exact new HEAD, confirms it is a strict forward descendant of starting HEAD `a5374fcdcc412997305cc53414a6ecc0f6845b7b`, and reports every gate and measurement.\n\n## Exact gates\n1. `npm install`\n2. `npm run build:recorder`\n3. `grep -nE '[[:blank:]]+$' dist/recorder/errors.js` (expected no output/non-match)\n4. `npm run typecheck`\n5. `node --import tsx --test test/recorder-admission.test.ts test/recorder-cli.test.ts test/recorder-extract-scan.test.ts test/recorder-package-lifecycle.test.ts test/recorder-transaction.test.ts`\n6. `npm test`\n7. `npm pack --dry-run --json`; compare exact file list/count with the package lifecycle accepted inventory.\n8. Pack to a temporary directory, install that tarball into a clean temporary consumer, inventory installed package files, and invoke its `node_modules/.bin/ak-docket-record` through the existing package lifecycle acceptance path; remove temporary tarball/consumer afterward.\n9. Production-range hygiene: `git diff --check 6e76efa93588844996611b86320de7747cca3d24..HEAD` before the evidence commit, then repeat against committed final HEAD. Also run `git diff --check` for the worktree/index while preparing the commit.\n10. Confirm only the intended source, generated artifact, and new evidence file differ; compare regenerated `dist` to source build output.\n\n## Append-only measurements\n- Exact starting SHA and branch; Final HEAD identity by `git log -1 --format=%H -- <new-evidence-path>`, with the literal resulting SHA returned in the Receipt.\n- Whole-tree logical bytes: `git ls-tree -r -l HEAD | awk '{sum+=$4} END {print sum}'`.\n- Issue-10 logical bytes: `git ls-tree -r -l HEAD .ak/dockets/issues/10 | awk '{sum+=$4} END {print sum}'`.\n- Default ancestry: `git merge-base --is-ancestor 6e76efa93588844996611b86320de7747cca3d24 HEAD`; also prove starting HEAD ancestry after commit.\n- Retained seals: replay all 71 entries in `migration/v2-source/retained.json` against `HEAD:path`, checking blob OID, SHA-256, and byte length; record item/mismatch counts.\n- Omission dispositions: verify 28 entries in `omitted.json`, their excluded-execution-exhaust disposition, preservation of related disposition files, and byte preservation of `coder-incomplete.json` with `lawfulReceipt: null`.\n- No new generic payload: path/name inspection of the issue-10 tree and added paths in `6e76efa…HEAD` for `session.jsonl`, tool-event/tool_event, raw-session, or cold roots; no payload/content inspection and no posture-fixture audit.\n- Mergeability: `git merge-tree --write-tree 6e76efa93588844996611b86320de7747cca3d24 HEAD`, recording the resulting tree and absence of conflict failure.\n- Package dry-run and clean-install inventories, focused/full test counts, and all exit statuses.\n- Final residue/status: `find . -maxdepth 3 \\( -name '*.tgz' -o -name '*scratch*' -o -name '*consumer*' \\) -print`, `[ ! -L node_modules ]`, and `git status --short` (empty after commit).\n\n## Scope\n- Only constructor source formatting, its generated JS, and one new append-only closure record.\n- No behavior expansion, session inspection, numeric retention engine, cold/session subsystem, test weakening, authority change, historical artifact edit/deletion, missing-Coder replacement, amend/rebase/rewrite, or unrelated cleanup.\n\n## One forward commit\n- Stage only the three authorized paths and create exactly one new commit, proposed title: `docs(docket): close recorder hygiene evidence` (body: source-owned generated whitespace correction plus final closure gates/dispositions).\n- Verify final HEAD is a strict descendant of `a5374fc…`; return one Receipt containing exact HEAD, changed paths, gate outputs, measurements, preservation statements, and clean status."
}
