# Repair packet: generated native artifact ownership

At current committed HEAD with only `dist/recorder/rename_no_replace.node` dirty, repair the demonstrated contradiction: repeated clean native build/install changes the tracked Mach-O bytes, so the accepted Receipt's clean-tree claim is false. Make generated host-specific binding ownership truthful: source/build scripts remain tracked and packed, clean checkout install builds the host binding, prepack includes a usable binding, packed install rebuilds/replaces a foreign producer binding, and repeated install/build/pack leaves Git clean. Do not require cross-host binary byte reproducibility or add a second publisher. Preserve atomic publication, OS/CPU admission, CI proof, one caller-child spawn and all unrelated behavior. Add exact lifecycle regression, one forward commit, Receipt. Do not inspect sessions.
