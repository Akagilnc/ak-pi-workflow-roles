# Repair packet: native build/install stability and truthful platform boundary

At current committed HEAD, diagnose and close demonstrated default-concurrency `npm test` nondeterminism at existing package/process seams. The tracked native output must never be transiently truncated while concurrent tests execute. A clean git/local or packed install must build or select a binding matching every claimed OS/CPU; Ubuntu CI must not consume a Darwin bundle, and lifecycle proof must not pass merely by same-host prepack. Include required build source in the package, state truthful compiler/platform requirements or fail package admission explicitly, preserve one atomic publication implementation and one caller-child spawn. Exact repeated full tests, clean install/build/pack/consumer and CI-shaped foreign/stale-binding counterexamples must pass without production fault hooks. Preserve all other behavior; coherent forward commit and Receipt. Do not inspect sessions.
