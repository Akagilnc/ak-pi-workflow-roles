# Issue #36 direct Fixer Apply

Sole authority: `.ak/dockets/issues/36/judgment/authority-002/receipt.json`. No Plan.

Repair the complete class:

- `Recorder native thinking-sibling issuance admission`

Follow the exact owner/boundary/disposition and detailed Fix summary. Repair production extraction first. Admit exactly one direct package terminating ToolCall plus only closed, correctly typed inert ThinkingContent siblings under the complete current Pi AssistantMessage envelope. Preserve every rejection and lifecycle law listed by Judge. Thinking must never enter promoted state, scanner input, audit observation, Receipt, or manifest.

Add bounded parser mutation coverage and one sanitized representative real persisted-shape Recorder vertical. Rebuild generated Recorder output. Run focused tests, typecheck, isolated-HOME full tests, package/cold-install checks, `npm pack --dry-run`, and `git diff --check`; commit forward. If completed, submit the exact class in `classesRepaired` through `ak_fixer_output`.
