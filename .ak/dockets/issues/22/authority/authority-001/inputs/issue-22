{"body":"## 案由:apply 侧无并行能力,墙钟损失已三次实测\n\n工厂目前单工作树、apply 全串行。三组实测(全部 2026-07-29/30,dockets 可复核):\n\n1. **独立修理包串行**:#10 的五个修理包,plan 腿已自发并行(一 commit 三份 plan receipt),apply 仍串行 ~80 分钟(含一次死亡)。包间无依赖,纯排队损失。\n2. **依赖链中段有宽度**:#17 的六片 apply 里,dispatch owner / runner / projection 是三个不同模块,双车道可并,仅终段集成必须等全员——估算可把后半场 ~40 分钟压至 ~25 分钟。\n3. **跨票排队**:#17 施工期间,#13/#15B/#12/#11 全部干等单工作树。互不依赖的票之间是纯粹的产能虚位。\n\n## 护栏三问(新机制准入,先答)\n\n- 出事概率:高频——每张多片/多包票、每次票池 ≥2 张独立票时发生;\n- 后果:每单几十分钟墙钟,跨票场景成倍;\n- 下游兜底:无。单工作树是物理串行,现有任何闸都救不了。\n\n## 机制输入(外审侧,作 authority 设计输入,非处方)\n\n**删除优先的形状:大多数合并不需要智能。**\n\n1. **worktree 隔离**:并行 apply 各在独立 worktree(#10 重建、本设计分支均有先例,`git worktree` 原生);\n2. **机械合并队列**:干净合并(无冲突)= 纯 git 操作,零模型调用;合并后跑验证闸(typecheck+test)——机械件,caller/driver 持有,不是角色;\n3. **冲突才召角色**:仅当合并真冲突时,派一条 merger 腿——单冲突单腿,解完即还权,保留双方意图,永不 abort(V2 判例原样带走:merger worker 只解一个冲突;家族 slice 合家族 base;resolving-merge-conflicts 纪律);\n4. **拓扑仍归 caller**(ADR 0010):merger 永不决定「合什么、何时合」,只执行「解开这一个冲突」。\n\n## 待 authority 裁的边界(不预答)\n\n- merger 是第六个角色(有 soul/交卷工具/审计)还是 fixer 的一种修理包形态(merge-packet)?\n- worktree 生命周期归谁(创建/清理/失败残留处置);\n- 队列与合并后验证闸的持有方(driver 机械件 vs 独立机械模块);\n- 并行度上限与逃生口(全部冲突时退化为串行的路径)。\n\n## V2 判例携带清单(只带法,不带码)\n\n家族 slice 合并进家族 base、末端一个 PR 走线上;merger 解一个冲突即返回,不碰队列;合并保双方意图、never abort;每次合并后必过验证闸再进下一单。\n","comments":[{"id":"IC_kwDOTklDoM8AAAABMl5tDg","author":{"login":"Akagilnc"},"authorAssociation":"OWNER","body":"## 优先级提升(owner 2026-07-31 15:3x 明令:「赶快把并发能力提上来」)\n\n当日新证(串行成本实测,#23 全程):\n\n- 一张宪法级修理票走了 **13 条串行腿 ≈ 2 小时墙钟**,其中真施工 <40 分钟——站台钱全在排队;\n- 三轮复审各 10-15 分钟,腿与腿手拉手,无任何两条并行;\n- 独立 R 键修理(#17 模式)与复审腿本质可并,现状单工作树物理串行。\n\n与既有案由(票间排队/依赖链中段宽度)合并,本票现为**墙钟瓶颈的唯一解**:并行不省 token,省的是墙钟——在配额窗口经济下,单位时间吞吐就是产能。\n\n队列位置归 owner/主刀排;本评论只记录提级令与当日实测。\n","createdAt":"2026-07-31T06:28:50Z","includesCreatedEdit":false,"isMinimized":false,"minimizedReason":"","reactionGroups":[],"url":"https://github.com/Akagilnc/ak-pi-workflow-roles/issues/22#issuecomment-5140016398","viewerDidAuthor":true}],"createdAt":"2026-07-30T15:42:03Z","labels":[],"number":22,"state":"OPEN","title":"Parallel apply lanes: worktree isolation, mechanical merge queue, conflict-only merger leg","updatedAt":"2026-07-31T06:28:50Z","url":"https://github.com/Akagilnc/ak-pi-workflow-roles/issues/22"}
