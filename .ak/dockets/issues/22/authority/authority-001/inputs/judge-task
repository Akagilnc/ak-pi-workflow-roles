# Issue #22 first authority review — parallel apply lanes

Adjudicate GitHub issue #22 against the fixed repository snapshot and its durable law. This is an Authority-posture review only: do not implement, edit, or require apply-level proof.

The admitted issue snapshot contains the owner’s explicit priority escalation and the observed serial wall-clock costs. Treat those as authority/evidence, while independently checking current repository contracts and ADRs.

Decide whether construction authority is presently converged. In particular, resolve or explicitly escalate:

1. **Ownership and repository boundary:** which parts, if any, belong in this independently installable roles package, which are caller/driver mechanics, and whether construction requires a separate orchestrator repository. Preserve ADR 0010 caller-owned composition and avoid placing workflow topology inside roles.
2. **Merger shape:** whether conflict handling is an existing Fixer apply packet, a new role, or neither; identify what real demand is sufficient under ADR 0001.
3. **Worktree lifecycle:** owner of creation, naming, cleanup, failed residue, branch identity, and immutable inputs.
4. **Mechanical merge queue:** exact boundary between model-free Git operations and role judgment; preconditions, post-merge verification, failure behavior, and preservation of both branches.
5. **Concurrency and degradation:** who selects lane width, what limits are package law versus caller policy, and how the mechanism safely degrades when conflicts or failures occur.
6. **Credential/evidence/failure honesty:** minimum durable evidence and diagnostics without promoting generic sessions or hiding causes.
7. **Construction slicing:** identify independently authorized slices and prerequisites. Do not prescribe shallow helper names. State whether a Plan leg has a legitimate missing deliverable or whether the authority verdict itself is construction-ready.

Reject attractive but unlawful scope expansion. Existing roles must continue to own one invocation; no role may decide ordering, repetition, budgets, stopping, commits, or cross-ticket composition. Do not infer that this package must own a mechanism merely because the issue is filed here.

Submit exactly one verdict through `ak_judge_output` after inspecting the admitted issue snapshot, CLAUDE.md, ADR 0001, ADR 0010, README, and relevant current code.