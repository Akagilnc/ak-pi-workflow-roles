# PR #47 external review findings

Target HEAD `22336f70338548d58a2df58e683280d603622402`.

CodeRabbit inline findings:

1. Comment 3695541932, `extensions/role-runtime.ts:62`: production `createProductionMergerGitState()` captures `process.cwd()` when the package extension binds. Pi sessions can supply `ExtensionContext.cwd` for an isolated worktree. Finding requests a context-bound factory so activation uses the assigned session cwd rather than ambient process cwd.
2. Comment 3695541933, `src/merger-git-state.ts:47`: `AUTO_MERGE^{tree}` depends on Git/merge strategy. Finding requests documentation that AUTO_MERGE requires merge-ort/Git support (suggested Git 2.32+; ort explicit on 2.32–2.33 and default on 2.34+), and other strategies/older Git fail preflight.
3. Comment 3695541936, `src/merger-role.ts:60`: active-tool verification uses `pi.getActiveTools?.() ?? [...MERGER_ACTIVE_TOOLS]`, so an absent accessor passes without evidence. Finding requests fail-closed absence before exact comparison.

CodeRabbit also emitted a generic docstring-coverage warning; no repository standard establishing that threshold was cited.