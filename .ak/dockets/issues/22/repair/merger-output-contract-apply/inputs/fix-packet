{
  "version": 1,
  "instructions": "Repair all executable open classes in .ak/dockets/issues/22/judgment/merger-real-use-output/receipt.json: MergerModelVisibleOutputContract and OutputInstructionRegression. Teach both exact legal output leaves at the ak_merger_output model-facing registration seam without changing Soul, broad transport admission, sole exact validator, fatal malformed behavior, Git verification, Recorder, or orchestration boundaries. Add structural model-visible contract tests without prose regex and retain real Pi valid/malformed behavior. Work test-first, run all gates, commit forward with per-class accountability, and settle exactly both classes.",
  "prerequisites": []
}
