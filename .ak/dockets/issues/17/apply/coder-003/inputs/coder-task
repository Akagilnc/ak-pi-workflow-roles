# Issue #17 bounded Apply slice 003 — complete pure dispatch owner

Implement only approved Plan steps 1–2 on top of `021e827`: make `src/reviewer-dispatch.ts` a production-quality, deeply tested pure construction/transition owner before role/runner integration.

Scope of this slice:

- complete closed V1 capability parsing and exact task-byte digest binding;
- exact seven-tool vocabulary, byte-exact bash strings, closed prerequisite enum, subset/no-widening checks;
- immutable pinned-reader base/range/commit/material resolution;
- deterministic complete Standards baseline extraction from the canonical Skill snapshot, axis isolation, exact prompts/UTF-8 lengths/SHA;
- established-Spec two-leg and no-spec Standards-only cardinality;
- atomic preflight: any rejection invokes no runner and leaves correction open; one accepted proposal irreversibly closes dispatch; no numeric retry cap;
- immutable minimal rejection/acceptance evidence exposed for later append-only projection;
- fake-runner tests for repeated rejection→correction, both cardinalities, prompt determinism, material/range/capability failures, and no runner call before acceptance;
- refactor the initial compressed implementation for repository readability without changing authorized semantics.

Do not yet wire Reviewer CLI/role, replace reviewer-agent, or migrate the old execution ledger in this slice. Do not leave a second alternate implementation inside the new module. Use canonical TDD, commit the coherent pure seam, and report exact residual integration work. Submit through `ak_coder_output`.
