# Issue #17 bounded Apply slice 005 — append-only Reviewer evidence projection

Implement approved Plan step 5 on top of the pure dispatch owner and opaque runner.

Scope `src/reviewer-execution-ledger.ts` and its owning tests/types only:

- replace old arbitrary Agent-call/persisted-assistant-message lifecycle authority with an append-only projection of typed events emitted by reviewer-dispatch and reviewer-agent;
- projection records immutable rejected proposal identity/violations/no-start facts, accepted recipe/input/target/range/material/grant identities, exact compiled prompts/UTF-8 lengths/SHA, irreversible dispatch-start identity/cardinality, and actual axis-keyed runner prompt/target/result/usage/workspace evidence;
- established Spec completion requires exactly one accepted two-child sibling dispatch with both axes settled successfully;
- established no-spec completion requires exactly one accepted Standards-only dispatch and no Spec prompt/start/result;
- exact compiled-versus-actual prompt bytes/length/SHA and shared pinned target are mechanically provable;
- rejected proposals cannot contain mirror/workspace/provider/child/usage/start evidence;
- evidence is defensively cloned/frozen and monotonic;
- the ledger exposes no accept/reject/correct/start/reopen decisions and cannot be a second lifecycle owner;
- preserve fatal infrastructure/refusal evidence needed by the existing auditor, but remove obsolete arbitrary-call, same-message batch, bash-transcript lifecycle, and fake-Spec fixture shapes rather than retaining parallel APIs.

Use fake dispatch/runner events in `test/reviewer-execution-ledger.test.ts`; do not wire reviewer-role in this slice. Use canonical TDD, run focused tests/typecheck, commit the coherent projection, and report exact residual role/auditor integration. Submit through `ak_coder_output`.
