# Issue #17 bounded Apply slice 004 — opaque Reviewer runner

Implement approved Plan step 4 on top of the completed pure dispatch owner at `1ef56de`.

Scope only `src/reviewer-agent.ts` and its owning tests/integration types:

- add the production opaque accepted-dispatch runner consuming `AcceptedReviewerDispatch` from `reviewer-dispatch`;
- it may materialize/verify mirror and one or two isolated workspaces only after receiving an accepted dispatch;
- execute exactly one Standards leg for no-spec or two overlapping Standards/Spec legs for established Spec;
- send each accepted prompt byte-for-byte unchanged and return axis-keyed result, usage, target, actual prompt bytes/length/SHA, and workspace disposition evidence;
- expose only each leg’s exact accepted subset of the existing seven tools;
- wrap child bash so execution occurs only when the `command` string byte-equals one accepted `bashCommands` member; no trim/prefix/glob/tokenization/inference/widening;
- perform only accepted runner prerequisite operations and verify target/ref/object identity against accepted evidence;
- keep active model/provider/auth, writable clone isolation, cancellation/failure retention, and operational-not-security boundary;
- runner must have no base/material resolver, prompt compiler, capability intersection, proposal acceptance, correction, or lifecycle authority.

Migrate `test/reviewer-agent.test.ts` to prove one/two-leg cardinality, overlap, exact prompt identity, exact tool visibility/bash denial, zero widening, and post-acceptance setup. If temporary compile compatibility with the old role is unavoidable, keep only a clearly internal deprecated adapter scheduled for removal in the next role slice; do not create a second lifecycle owner.

Use canonical TDD, run focused tests/typecheck, commit this coherent runner slice, and report exact residual role/ledger/auditor migration. Submit through `ak_coder_output`.
