# Issue #17 continuation Apply task

Continue the approved issue #17 construction from partial commit `021e827f58b63242fbeb54f912158471cf0973ad` and refusal `.ak/dockets/issues/17/apply/coder-001/receipt.json`.

The atomic dispatch/compiler seam and its focused 4/4 tests already exist. Complete—not parallel-layer—the remaining approved work:

1. Make `--ak-review-capabilities` mandatory and task-digest-bound in Reviewer activation/help/package lifecycle.
2. Replace parent-visible arbitrary `{description,prompt}` Agent dispatch with the atomic structured proposal schema and correction-before-acceptance lifecycle.
3. Integrate immutable runtime pin and dispatch-owned base/range/material preflight before acceptance with zero mirror/workspace/provider/child/batch effects on rejection.
4. Convert reviewer-agent into opaque post-acceptance one-or-two-leg executor with exact visible tools and byte-exact bash command enforcement; no target/compiler/grant/lifecycle decisions.
5. Convert execution ledger into append-only projection with compiled/actual prompt identity evidence and correct Standards-only no-spec cardinality.
6. Update the existing semantic auditor, role/package integration, README/help, and fixtures. Remove obsolete arbitrary-prompt, same-message provenance, and fake-Spec paths/tests rather than retaining a second implementation.
7. Preserve active model/provider/auth, thin Reviewer Receipt, Souls, caller orchestration, other roles, Recorder, and package distribution.

Use canonical TDD. Run focused tests and typecheck; run the full suite with a timeout sufficient for this repository or report exact residual evidence. Commit forward only when the remaining construction is coherent. Include same-pattern, introduced-regression, and behavior-fact checks. Submit through `ak_coder_output`.
