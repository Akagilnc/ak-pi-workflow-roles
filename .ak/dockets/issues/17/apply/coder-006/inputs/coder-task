# Issue #17 bounded Apply slice 006 — integrate Reviewer role, dispatch, runner, ledger, auditor

The pure dispatch owner, opaque runner, and append-only projection are complete. The branch intentionally no longer typechecks because old role/runtime/auditor integration still calls removed ledger APIs. Complete the core integration now.

Required scope:

1. Reviewer activation/runtime:
   - add mandatory `--ak-review-capabilities <json-path>` beside existing task flag;
   - load task exact bytes once, parse the closed capability file, bind digest, fail closed when absent/invalid/mismatched, and expose no widening fallback;
   - pin target/ref identity before proposal handling and construct the narrow pin-bound Git reader.
2. Parent tool seam:
   - replace arbitrary `{description,prompt}` Agent parameters with the structured atomic `ReviewerProposalV1` schema;
   - route corrections through one `createReviewerDispatcher`; rejected preflight starts no runner; accepted dispatch is irreversible;
   - established Spec runs two legs; established no-spec runs Standards only.
3. Execution/evidence:
   - run only `runReviewerDispatch(AcceptedReviewerDispatch)`;
   - append typed rejection/acceptance/start/result/fatal events to the new projection;
   - completion/refusal gates consume projected facts, with exact compiled/actual prompt/target/cardinality checks.
4. Auditor/runtime exports:
   - update `reviewer-auditor.ts`, `role-runtime.ts`, and extension wiring to the new record/types while preserving the auditor as sole semantic compliance check and preserving the thin Reviewer Receipt;
   - delete deprecated `runReviewerAgent`, old same-message Agent provenance hooks, and obsolete ledger exports—no parallel path.
5. Tests:
   - migrate `reviewer-role.test.ts`, auditor tests, and fatal-stage tests needed for typecheck/core behavior;
   - prove preflight reject→correct causes zero starts, one accepted dispatch only, one/two cardinality, exact prompt equality, fatal infrastructure blocking, and audit revise/resubmit behavior.

Do not yet spend scope on README or independent installed-package lifecycle polish unless required by core tests; report those exact residuals for the final packaging slice. Use canonical TDD, restore full typecheck, run core focused tests, commit coherently, and submit through `ak_coder_output`.
