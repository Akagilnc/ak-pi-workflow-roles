# Issue #17 implementation planning task

Plan the first implementation authorized by `.ak/dockets/issues/17/authority/judge-001/receipt.json`.

The Plan must preserve the Authority decisions:

- one atomic structured Standards/Spec proposal/dispatch seam replaces parent-authored arbitrary child prompt prose for the two-leg method;
- deterministic runtime compilation binds canonical Skill/task, resolved range/commits, material identities/bytes, complete Standards baseline, axis briefs, and structured per-leg capabilities;
- both prompts preflight before workspace/provider/child-start state;
- rejected proposals may be corrected before accepted dispatch; lifecycle transition, not numeric retry count, is the bound;
- structured least-privilege executable policy is exclusive; task prose cannot create an executable allowlist;
- accepted evidence retains exact compiled prompts, lengths, SHA-256, recipe version, and actual-dispatch identity;
- existing post-run audit remains semantic and proves actual prompt equality, target traceability, axis isolation, child evidence, and aggregation;
- explicit no-spec is a prerequisite/skip path, not a malformed one-legged batch;
- no Soul, role topology, caller ordering/repetition, generic session retention, or cross-model-panel changes.

Inspect the existing Reviewer module and produce a construction-ready Plan. For every planned change state exactly:

- Behavior
- Owner
- Red
- Green
- Scope

Identify the smallest deep module/seam, migration of current tests, source/dist/package implications, and a safe incremental implementation order. Do not edit or commit. Submit only through `ak_coder_output`.
