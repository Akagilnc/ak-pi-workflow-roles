# Issue #17 revised implementation Plan

Revise `.ak/dockets/issues/17/plan/coder-001/receipt.json` under the exact corrections in `.ak/dockets/issues/17/plan/judge-001/receipt.json`.

Required corrections:

1. Keep existing opaque `--ak-review-task`, canonical Skill snapshot, and runtime-pinned target. Do not add a broad review-input document. If transport is necessary, allow only a narrow task-digest-bound capability ceiling. Base/material selections and established spec state belong to the atomic proposal and are resolved to exact bytes by the deep owner.
2. Capability vocabulary is Reviewer-local only: existing child tool names, exact allowed bash command strings, and package-owned prerequisite operations. No wildcard filesystem/network/process policy or sandbox framework. Runner tool selection and bash guard enforce the accepted operational grant without widening.
3. Correct no-spec: established no-spec dispatches one complete Standards child through the same owner, creates no Spec prompt, and records no two-leg sibling batch. It is not a zero-child successful review. Replace the obsolete fake-Spec fixture.
4. `reviewer-dispatch` is sole transition/compiler/capability owner. Role/extension only adapt/load; runner receives opaque accepted immutable prompts/grants; ledger is append-only evidence projection, not lifecycle authority.
5. Extract the complete Standards baseline from the exact canonical Skill snapshot. Do not copy Fowler text into source constants, schema, auditor, or Soul.

Return a complete replacement construction Plan with Behavior / Owner / Red / Green / Scope for every change, a bounded interface shape, test migration, and incremental implementation order. Do not edit or commit. Submit through `ak_coder_output`.
