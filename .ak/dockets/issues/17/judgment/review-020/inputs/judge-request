# Issue #17 review-finding adjudication

Construction target: `381ca14a702a4a854258da079d0acc0e57e3ca74` on `feature/issue-17-reviewer-preflight`, base `cc5cdf4c021b04ac67ef160906b5c73dc1c3a1c5`.

Authority and converged Plan are the supplied durable receipts. Review-020 reports two Spec findings:

1. Each leg can omit bash/the exact canonical diff command, so its prompt names a diff command but it may lack means to inspect the diff. Earlier review-011 called mandatory bash an unrequested restriction, and review-012 contained contradictory Standards/Spec conclusions. The fixed capability schema intentionally permits no bash and bash with zero commands; decide whether the concrete Reviewer dispatch recipe must nonetheless require each review leg’s exact canonical diff command, or whether optional grants satisfy authority. Do not treat wording/headings as evidence; reconcile substantive least-privilege and review-capability law.

2. Abbreviated SHA resolution uses live object-database disambiguation rather than only immutable pin roots, so unrelated colliding objects introduced after activation may change resolution without HEAD/ref drift.

Determine whether the current construction is complete/correct. If continue, emit bounded exact repair dispositions; if converged, explain why each current finding is not a residual mandate. Submit only through `ak_judge_output`.
