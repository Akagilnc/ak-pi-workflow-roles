# Issue #17 Review-034 findings and reproducible axis-method adjudication

Adjudicate current HEAD after Review-033 contract repairs. Review-034 executed one lawful atomic batch; both legs settled successfully; audit truthfully revised completed to refused; old generated Recorder initially blocked publication, now separately repaired under the existing class. No Review-034 docket was published, so the reports below remain summarized raw evidence, not authority.

## Child findings

1. `src/package-contracts/reviewer-output.ts` rejects `ReviewerRefEntry.peeledCommitId: null` even though target pinning law and `parseReviewerRefSnapshot` permit refs that do not peel to a commit. Decide exact admissible nullable/object-ID validation.
2. Runtime V2 receipt validation proves only a nonempty all-success subset. An accepted two-leg batch containing only Standards can validate as completed; accepted refused can carry zero/partial outcomes. The receipt lacks authenticated expected axes/cardinality. Decide how accepted batch identity must carry and enforce exact terminal outcome/report coverage without parsing reports.

## Reproducible method issue

Review-033 and Review-034 independently produced the same cross-axis presentation. The Spec child was asked `Apply the canonical Spec brief from the bundle` but emitted both `## Standards` and `## Spec`; the Standards child was asked `Apply the complete canonical Standards brief and baseline from the bundle` but also emitted both sections. This follows the full canonical Skill’s dual-axis output template even though package questions are distinct. Audit then rejects both reports for crossing their assigned question. The prior judgment dismissed this as invocation-only, with an explicit reopen condition on reproducible evidence; that condition is now met.

Determine a package-owned mechanical method repair consistent with all owner rulings:

- keep the exact canonical Skill snapshot and common material population available to both children;
- no source/material allowlists;
- Standards and Spec remain distinct questions;
- do not edit or depend on changing the global Skill;
- do not parse/sanitize child report prose, add replacement legs, or weaken verbatim preservation;
- canonical semantic content remains input, while package recipe owns transport/output mechanics.

Consider axis-specific compiled output envelopes/instructions that supersede the dual-axis presentation template for each fixed question while preserving semantic brief content, and align semantic audit with that lawful envelope. Escalate only if a genuine major owner choice remains; otherwise authorize direct Fixer classes. No code edits or Plan. Submit via `ak_judge_output`.
