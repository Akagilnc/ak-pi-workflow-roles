# Issue #17 aggregation and non-commit tag repair

Authority: accepted Reviewer receipt `.ak/dockets/issues/17/review/review-013/receipt.json` and canonical code-review aggregation method.

R1 — On successful `ak_reviewer_agent` dispatch, expose the complete Standards and (when present) Spec child reports to the parent Reviewer in deterministic tool-result content, clearly delimited as child report data with axis and prompt identity. Do not summarize, truncate, or reinterpret. Preserve the opaque accepted result in `details`; no report is exposed before successful complete settlement.

R2 — Model captured refs with original `objectId` and nullable `peeledCommitId`. Preserve blob/tree tag identities in immutable snapshots and equality/audit evidence, but only apply commit reachability/`^{commit}` checks to entries with a real peeled commit. Resolving a non-commit tag as review base must reject cleanly; unrelated non-commit refs must not fail runner snapshot verification.

R3 — Extract one shared handler for the duplicate `tool_execution_end` / persisted `tool_result` transport terminal handling, preserving deduplication and terminal provenance. Extract one dispatch rejection-evidence constructor for duplicate preflight rejection paths. Rename only the flagged opaque test helpers when safe.

R4 — Add red/green tests proving exact child reports reach the parent tool content; no report leakage on partial/failed settlement; annotated commit tags verify; unrelated blob/tree tags remain pinned without failing; non-commit base rejects; dual transport terminal deduplicates.

R5 — Run package lifecycle, focused tests, full suite, typecheck, pack, and diff check; commit forward.

Return exactly one nonblank keyed line for R1–R5.
