# Issue #17 direct Fixer Apply — settlement/output vertical

This is the second deliberate independent vertical. The committed construction/bundle vertical is now the sole accepted input seam. Complete the remaining current authority classes:

- `I17 atomic settlement and runtime reports`
- `I17 cohesion and regression ownership`
- `MERGE judge class contract test compatibility`

Extend the sole atomic owner through all-leg settlement. Before returning control to the driver, permanently revoke `Agent`/spawn capability and expose only `ak_reviewer_output`. Do not expose report text in Agent results. Preserve each successful child report exactly with UTF-8 length/SHA and build the typed axis-keyed Reviewer Receipt at runtime. The driver may submit only completed intent or refused intent with a separate nonblank diagnostic; it cannot transport, aggregate, rerank, omit, rewrite, or replace child reports. Completed requires every expected report. Refused preserves actual outcomes/reports and cannot launder provider, bundle, audit transport, or unknown infrastructure failure. Audit receives the runtime-assembled candidate and dual Skill/recipe/bundle/target/prompt evidence; it checks distinct questions and exact report preservation, never source allowlists or finding merit. Audit revision cannot reopen dispatch.

Deepen remaining module boundaries: separate settlement/receipt assembly from role transport, and workspace/bundle materialization from child provider/session execution. Census the prior bundle vertical too; close the full cohesion class without adding wrappers or a parallel lifecycle. Preserve one/two-leg cardinality, no-spec behavior, pre-acceptance corrections, exact grants, #31 scope, #23 session retention/verification policy, Recorder extraction, other roles, and unchanged Soul.

Repair the two stale Judge typed-class test expectations without weakening the production contract. Migrate Reviewer output/package/Recorder fixtures exactly once; no v1 aggregate compatibility core.

Run focused tests, typecheck, isolated-HOME full tests, package/cold-install checks, pack dry-run, and diff check; commit forward. Submit exactly these three keys in `classesRepaired` only if complete; otherwise refuse cleanly.
