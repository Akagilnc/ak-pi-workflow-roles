# Issue #17 Fixer Apply — final receipt evidence repair

Sole authority: `.ak/dockets/issues/17/judgment/review-037/receipt.json`.

Repair both exact classes:

- `ADR 0016 cross-field tracer`: remove primitive Git-ID format matrices; retain at most the one objectFormat/ID-width cross-field red-green tracer described by Judge.
- `Per-leg runtime materialization evidence in V2 receipts`: project exact typed materialization evidence from ledger into every outcome for which materialization completed, including later failures; validate leg, manifest and exact canonical read-back entries against accepted bundle, with allowed omission only before materialization. Keep runtime paths explicitly runtime-owned, never target identity.

Preserve thin intent/result, accepted leg/outcome/order/prompt laws, report bytes, semantic audit, no prose parsing, ownership seams, and generated/package parity. Add only logic-focused settlement/validator/production seams specified by Judge. Run focused, typecheck, isolated-HOME full, package/cold-install, pack dry-run, diff check; regenerate tracked artifacts, commit forward, and submit exactly both keys.
