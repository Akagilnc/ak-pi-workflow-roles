# Issue #17 review-findings repair

Authority: accepted Reviewer receipt `.ak/dockets/issues/17/review/review-003/receipt.json`, the converged Plan, and exact V1 capability contract.

R1 — Fixed-point identity: resolve the submitted base, derive merge-base for canonical three-dot comparison, and fail preflight when merge-base differs from the resolved submitted base under this authorized ancestor-only Plan. Do not silently replace the submitted base identity. Preserve three-dot diff and non-empty check.

R2 — For `spec.state: "not-established"`, resolve/read/hash every selected evidence material exactly as for established Spec and retain it in accepted construction/evidence so the semantic auditor receives real evidence, not null placeholders. It still creates no Spec prompt/child.

R3 — Before `createPinnedGitReader()` or any pin operation, parse the task-bound capability ceiling and mechanically require `preflight.git.pin-target` in that ceiling. Missing authority fails with zero Git pin/read effect. Preserve later proposal-subset checks for all other operations.

R4 — Restore the approved least-privilege contract: `bash` with an empty `bashCommands` array is valid and exposes bash while permitting no command. Remove the invented nonempty-command rejection and its tests. Exact command enforcement/no widening remains.

R5 — Remove parallel vocabulary drift: build Reviewer proposal TypeBox enums from exported `REVIEWER_CHILD_TOOLS` and `REVIEWER_PREREQUISITES`; replace reviewer-agent string/`as any` prerequisite handling with the exported domain type and closed checks.

R6 — Add red/green tests for divergent base, actual no-spec evidence bytes/hash/audit input, no pin call before pin capability authorization, valid empty bash grant, and shared typed vocabulary. Run focused tests, installed package lifecycle, full suite, typecheck, pack, and diff check. Do not rewrite historical evidence.

The Reviewer Data-Clump observation is a judgement-only refactoring suggestion; do not create a new abstraction unless required by these concrete fixes.

Return exactly one nonblank keyed line for R1–R6.
