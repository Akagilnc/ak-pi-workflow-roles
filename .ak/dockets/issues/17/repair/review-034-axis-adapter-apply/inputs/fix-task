# Issue #17 Fixer Apply — axis-specific output adapter vertical

Sole authority: `.ak/dockets/issues/17/judgment/review-034-findings/receipt.json`. No Plan.

Repair exactly `Axis-specific package output adapter and semantic audit alignment` under its full Judge boundary.

Compile deterministic axis-specific method/output adapters into exact child prompt identities. The unchanged canonical Skill snapshot remains available common semantic input, but each isolated leg must be clearly governed by a single-axis output contract that supersedes canonical dual-agent orchestration/aggregation/presentation mechanics. Standards emits only Standards assessment; Spec emits only Spec assessment. Keep all material freely readable, report bytes verbatim, no prose splitting/sanitizing, no replacement legs, no global Skill edit/fork. Align semantic audit with package-owned output mechanics and canonical substantive semantics; cross-material citation remains lawful.

Add logic-focused tests proving axis-distinct deterministic identities, unchanged Skill/common bundle bytes, single-axis acceptance and cross-question revision, verbatim preservation, and absence of parser/allowlist behavior. Preserve the just-repaired runtime receipt/accepted-leg laws. Regenerate tracked package artifacts if runtime paths require them. Run focused tests, typecheck, isolated-HOME full tests, package/cold-install, pack dry-run, diff check; commit forward. Submit exactly this one repaired key.
