# Issue #17 opaque runner interface repair

Authority: accepted Reviewer receipt `.ak/dockets/issues/17/review/review-030/receipt.json` and opaque-runner law.

R1 — Define a closed immutable `AcceptedReviewerExecution` value containing only dispatch identity/recipe, pinned target, accepted operational prerequisites, and ordered frozen legs with axis/prompt identity/effective grant. It must not contain task text/identity beyond dispatch ID, capability document, range/commits/diff, selected materials, proposal, canonical Skill, or construction recipe inputs.

R2 — reviewer-dispatch remains construction owner and returns complete `AcceptedReviewerDispatch` evidence to role/ledger. Before runner invocation, project it through one dispatch-owned `toReviewerExecution` function with exact closed keys/deep freeze. `ReviewerAgentRunner` and `runDispatch` dependency accept only this execution type. Runtime must make construction-semantic inspection unavailable through the runner interface, not merely unused by current code.

R3 — Preserve ledger projection of complete accepted construction before runner start, and preserve result identity/target/axis checks against the accepted evidence. Add compile-time/runtime closed-key tests and a fake runner asserting task/capability/range/material/Skill fields are absent while exact prompts/grants/pin/prerequisites remain.

R4 — Do not perform optional broad module extraction in this bounded repair. Run focused tests, package lifecycle, full suite, typecheck, pack, and diff check; commit forward.

Return exactly one nonblank keyed line for R1–R4.
