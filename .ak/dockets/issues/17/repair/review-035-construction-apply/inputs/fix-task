# Issue #17 Fixer Apply — construction ownership vertical

Sole authority: `.ak/dockets/issues/17/judgment/review-035/receipt.json`. No Plan.

Repair exactly `Reviewer construction ownership` under its complete owner/boundary/disposition.

Extract typed mechanical admission (no repository I/O/construction), pinned evidence acquisition/normalization (after admission, truthful correctable/infrastructure errors), and deterministic construction from admitted immutable inputs plus frozen evidence. Leave irreversible proposal chronology, corrections/closure, final drift check, acceptance evidence, and exactly-once runner in the lifecycle coordinator. Preserve current dual identity, common bundle, hint/no-allowlist law, capability ceilings, race closure, typed scope, and failure semantics. Test interfaces and behavior, not file size/text.

Do not implement workspace ownership or Thin Agent result in this invocation. Regenerate tracked package artifacts if needed. Run focused tests, typecheck, isolated-HOME full tests, package/cold-install, pack dry-run, diff check; commit forward and submit exactly this class.
