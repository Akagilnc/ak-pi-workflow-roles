# Issue #17 recipe prerequisite and documentation layering repair

Authority: accepted Reviewer receipt `.ak/dockets/issues/17/review/review-028/receipt.json`, Plan recipe-union law, and repository deepest-owner law.

R1 — Compute accepted runner prerequisites as the union of package recipe-required `runner.git.*` operations plus any operations declared by accepted legs. First prove every resulting operation is authorized by the task-bound ceiling. Leg prerequisite arrays may be empty and remain exactly their declarations; do not inject recipe operations into child-visible per-leg grants. Missing recipe authority in the ceiling rejects before acceptance; empty leg arrays with a complete ceiling are valid.

R2 — Reduce README Reviewer text to caller-facing CLI/input usage only: required flags, closed capability/proposal shapes at a concise level, and where/how to invoke. Remove duplicated lifecycle, ordering, evidence, cardinality, prompt-construction, and internal enforcement prose whose deepest owner is TypeScript/schema. Do not weaken runtime or tests; README should not become an alternate normative implementation spec.

R3 — Add empty-leg-prerequisite/complete-ceiling green, missing-recipe-ceiling red, leg-extra union green, and exact per-leg no-injection assertions through installed package lifecycle.

R4 — Extract one failed-leg evidence constructor for snapshot/workspace/child failure paths and rename `singleton` to `requireSoleReviewerOutputCall` (or equivalent), preserving behavior.

R5 — Run focused tests, package lifecycle, full suite, typecheck, pack, and diff check; commit forward.

Return exactly one nonblank keyed line for R1–R5.
