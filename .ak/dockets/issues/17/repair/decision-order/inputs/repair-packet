# Issue #17 atomic decision-evidence ordering repair

Authority: accepted Reviewer receipt `.ak/dockets/issues/17/review/review-027/receipt.json` and monotonic proposal-disposition evidence law.

R1 — Make proposal lifecycle decision and append-only evidence projection one atomic synchronous boundary owned by reviewer-dispatch. Before a rejected/closed/accepted decision becomes observable or another proposal can transition, invoke a narrow decision-evidence sink that records the immutable identity/disposition. Acceptance evidence must precede dispatch-start; a pre-acceptance rejection must remain ordered before a later acceptance even when async compilation resumes out of order.

R2 — Remove the role’s post-`await dispatcher.propose()` rejection append. The role supplies the ledger-backed decision sink at dispatcher construction and must not duplicate decisions. Sink failure is fatal infrastructure and cannot leave a visible accepted transition or start children.

R3 — Add deterministic barriers reproducing: slow invalid compile, fast valid accept, delayed invalid completion; fast rejection then acceptance; simultaneous valid proposals; closed-after-acceptance. Assert exact ordered identities/dispositions once each, one accepted batch, no ordinary rejection turning into tool error, and no post-acceptance correctable rejection.

R4 — If local and safe, move exact runner outcome validation/settlement projection into one exported ledger projection operation so role runtime invokes rather than traverses it; preserve fatality and exact axis/target/identity checks. Do not split dispatcher’s public ownership or create generic event machinery.

R5 — Run concurrency/fatal subprocess tests, package lifecycle, focused tests, full suite, typecheck, pack, and diff check; commit forward.

Return exactly one nonblank keyed line for R1–R5.
