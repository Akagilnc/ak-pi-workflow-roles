# Issue #17 axis isolation repair

Authority: issue Authority requires independent Standards/Spec contexts; accepted Reviewer refusal `.ak/dockets/issues/17/review/review-010/receipt.json`; README now correctly distinguishes task evidence from child prompts.

R1 — Remove opaque parent task prose/bytes from both child prompts. Each child receives only deterministic axis-specific instructions, canonical Skill section, selected axis materials, pinned range/snapshot facts, and the task SHA-256/byte-length identity. Exact task bytes remain frozen in accepted construction/ledger/auditor evidence and capability binding, never indiscriminately copied across axes.

R2 — Preserve exact valid UTF-8 bytes for each selected axis material through its own prompt, including BOM/multibyte bytes. Do not weaken the byte-roundtrip, independent recompilation, actual-delivery identity, or semantic audit checks.

R3 — Add a production-shaped two-axis regression whose opaque task contains unique Standards-only and Spec-only sentinel prose/material references. Assert neither sentinel appears in the opposite compiled or delivered prompt, while task digest/length and exact task evidence remain bound. Cover Standards-only no-spec similarly.

R4 — Run focused Reviewer tests, package lifecycle, full suite, typecheck, pack, and diff check; commit forward. Do not reinterpret the refusal as success.

Return exactly one nonblank keyed line for R1–R4.
