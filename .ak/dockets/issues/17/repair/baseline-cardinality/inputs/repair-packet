# Issue #17 baseline-only and result-cardinality repair

Authority: accepted Reviewer receipt `.ak/dockets/issues/17/review/review-024/receipt.json` and canonical Skill baseline/no-spec cardinality law.

R1 — Permit an empty `standardsMaterials` array in the closed proposal schema and preflight. The deterministic Standards prompt must still include the complete canonical Skill Standards/smell baseline and fixed range/task identity, with no fabricated repository standards material. Nonempty selections retain exact read/identity/path rules. Add baseline-only two-leg and Standards-only no-spec cases.

R2 — Before settlement projection, require actual runner result axes to equal accepted dispatch axes exactly: Standards is required; Spec is required only for accepted two-leg dispatch and forbidden for Standards-only dispatch; no unknown/extra keys. Any missing/extra/wrong axis is fatal infrastructure before misleading ledger/audit evidence or receipt. Tighten result union types if practical so cardinality is represented structurally.

R3 — Add no-spec counterexample returning a hidden Spec artifact and two-leg result omitting/adding axes; prove no completed/refused receipt and no discarded artifact. Add exact successful one-leg/two-leg results.

R4 — Rename the role execution closure to `executeAndProjectDispatch` (or equally explicit) and extract one successful-leg fixture builder in `test/reviewer-role.test.ts` where it reduces repeated evidence shape without obscuring tests.

R5 — Run package lifecycle, focused baseline/cardinality tests, full suite, typecheck, pack, and diff check; commit forward.

Return exactly one nonblank keyed line for R1–R5.
