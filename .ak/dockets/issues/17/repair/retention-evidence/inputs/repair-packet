# Issue #17 failure-retention evidence repair

Authority: accepted Reviewer receipt `.ak/dockets/issues/17/review/review-014/receipt.json` and Plan cancellation/failure-retention law.

R1 — Preserve attached infrastructure evidence from `prepareSnapshot` and `prepareWorkspace` failures. The failed settlement must use the actual bounded `workspaceDisposition` (`{retained:absolutePath}` when created, otherwise `not-created`) and attached target snapshot, rather than inferring from an assignment that never completed. Apply consistently to snapshot setup, workspace setup, verification, child failure, and cancellation; never delete retained failure artifacts automatically.

R2 — Add deterministic fault-injection tests at mirror creation/verification and workspace init/fetch/verification proving the retained path exists, the ledger reports that exact path, successful cleanup still reports deleted, and pre-creation failures report not-created. Cover cancellation after creation.

R3 — Narrow structural cleanup without changing ownership: move the concrete pinned Git reader/ref-range/material I/O implementation into a dedicated `reviewer-pinned-git.ts`, while `reviewer-dispatch` remains sole owner of capability parsing, prompt compilation, preflight policy, and acceptance lifecycle. Derive ledger projection evidence types from exported accepted-dispatch leg/range/material types where practical. Rename flagged opaque Reviewer test harness locals.

R4 — Run package lifecycle, focused failure-path tests, full suite, typecheck, pack, and diff check; commit forward.

Return exactly one nonblank keyed line for R1–R4.
