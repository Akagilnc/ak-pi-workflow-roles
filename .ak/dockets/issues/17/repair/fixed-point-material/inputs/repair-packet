# Issue #17 fixed-point and material repair

Authority: accepted Reviewer receipt `.ak/dockets/issues/17/review/review-007/receipt.json`, canonical code-review Skill, and converged Plan.

R1 — Resolve every canonical fixed-point form against immutable pin evidence without consulting mutable live ref names: full and unambiguous abbreviated commit SHA; `HEAD` and ancestry expressions such as `HEAD~5`/`HEAD^2` rewritten against pinned target; exact captured branch/tag/ref names and unambiguous conventional short aliases. Capture/peel annotated tags to immutable commit identities at activation. Reject ambiguity, unknown names, unsafe revision grammar, non-commit objects, or commits not reachable from the pin. Preserve base-as-merge-base ancestor and non-empty three-dot law.

R2 — Mechanically reject cross-axis material placement by both unique `id` and unique normalized `repositoryPath` across Standards and Spec/no-spec selections. Same path with different IDs must reject before reads/runner.

R3 — Admit valid repository-owned UTF-8 paths containing spaces or non-ASCII while rejecting controls/newlines/NUL, absolute paths, backslashes, empty/dot/dot-dot segments, and traversal. Do not interpolate raw path text into child prompt; render a deterministic escaped identity or omit path prose while retaining exact path/bytes/hash in construction evidence.

R4 — Remove concrete duplication findings where local and safe: one shared capability request validator used by file parser/proposal validation; one shared NUL-ref parser/snapshot helper used by dispatch and runner; one explicit dispatch-to-ledger projection function/type mapping rather than repeated field assembly. Preserve module ownership and do not create generic frameworks.

R5 — Add red/green tests for HEAD ancestry expressions, abbreviated SHA, annotated tag, ref movement after pin, ambiguous aliases, duplicate cross-axis path under different IDs, spaces/Unicode paths, injection/control negatives, and shared validators/parsers/projection. Run real package lifecycle, focused tests, full suite, typecheck, pack, and diff check.

Return exactly one nonblank keyed line for R1–R5; commit forward and preserve historical findings.
