# Issue #17 Fixer Apply — workspace/executor ownership vertical

Sole authority: `.ak/dockets/issues/17/judgment/review-035/receipt.json`. No Plan.

Repair exactly `Reviewer workspace ownership` under its complete owner/boundary/disposition.

Create a Git workspace owner for pinned mirror creation/verification, per-leg clone/bundle installation, leases, disposal, and retained-path truth. Create a child executor for provider/runtime/session, accepted tool/bash authorization, prompt delivery, usage, provider/session cleanup, and child failure classification. Keep `reviewer-agent` as accepted-batch orchestration/settlement only. Preserve prepare-and-install-all-before-any-provider, concurrent accepted legs, settle-all-siblings, cancellation, irreversible shutdown, exact sessions, complete outcome union, and failure honesty. Prove workspace prep without provider, execution against prepared workspace without Git materialization, and coordinator lifecycle behavior.

Do not alter construction ownership already repaired or Thin Agent result. Regenerate tracked package artifacts. Run focused tests, typecheck, isolated-HOME full tests, package/cold-install, pack dry-run, diff check; commit forward and submit exactly this class.
