# Issue #17 direct Fixer Apply — construction/bundle vertical

This is a deliberately defined independent vertical delivery, not a test slice. It leaves the currently functioning settlement/output path unchanged while replacing the complete pre-acceptance construction and per-clone material transport seam that the later settlement vertical consumes.

Sole authority remains the two current Judge receipts. Repair these four classes completely:

- `I17 canonical semantic source and recipe`
- `I17 pinned mechanical bundle`
- `I17 common readable materials`
- `I17 mechanical preflight and capability law`

Build the typed package recipe/dual identity, common material proposal with nonbinding hints, deterministic pinned bundle, path+digest child prompts, and confined atomic per-clone materialize/readback verification before provider start. Preserve exact task/target/capability laws and #31 scope keys. Split compiler/bundle/materializer responsibilities deeply enough that no new mega-function is introduced, but do not claim or repair `I17 cohesion and regression ownership` yet—the later settlement vertical will census both modules and own that class.

Do not alter driver aggregation/output schema in this delivery and do not add a second/compatibility dispatcher. Migrate superseded axis-compartment/transcription tests once. Prove common availability, cross-hint legality, confinement/symlink/collision/digest failures, identical one/two-clone bundle, zero provider start before verification, dual identities, exact prompt references, package lifecycle, and current settlement regression safety.

Run focused tests, typecheck, isolated-HOME full tests, package/cold-install checks, pack dry-run, and diff check; commit forward. Submit exactly these four keys in `classesRepaired` only if all four complete; otherwise refuse cleanly.
