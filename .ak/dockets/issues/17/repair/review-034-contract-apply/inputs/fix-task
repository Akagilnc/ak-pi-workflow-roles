# Issue #17 Fixer Apply — runtime receipt contract vertical

Sole authority: `.ak/dockets/issues/17/judgment/review-034-findings/receipt.json`. No Plan.

Repair exactly these classes completely:

- `Nullable pinned-ref and Git object-ID contract`
- `Authenticated accepted-leg coverage in Runtime Reviewer receipts`

Implement the exact Judge boundaries: object-format-aware closed Git IDs with lawful nullable peeled IDs; accepted-batch projection derived from accepted ledger legs, canonical ordered axes and exact prompt identities; exact terminal outcome/report coverage for one/two-leg completed and accepted-refused receipts; empty pre-acceptance refusal; malformed/mismatch native-session rejection. Keep thin intent projection, content identity, settlement irreversibility, and all other terminating tools intact. Regenerate and commit the tracked package/Recorder build closure so the real CLI and cold install use the repair.

Do not touch the separately authorized axis adapter class in this invocation. Run focused tests, typecheck, isolated-HOME full tests, package/cold-install, pack dry-run, diff check; commit forward. Submit exactly the two repaired class keys.
