# Issue #17 Judge-directed final repair

Authority: `.ak/dockets/issues/17/judgment/review-020/receipt.json` (`continue`).

R1 — Keep generic V1 capability shapes representable, including no bash and empty command arrays, but require the concrete code-review recipe after range derivation to grant every constructed leg `bash` plus exactly its canonical `range.diffCommand`. Omission from task-bound ceiling or any leg rejects the whole proposal as correctable capability preflight before acceptance/effects. Do not require unrelated commands. Update README and replace accepted underpowered tests with no-bash, empty-bash, one-leg-omission reds and exact minimum two-leg green.

R2 — Resolve abbreviated hexadecimal base prefixes uniquely only among commit identities reachable from immutable activation roots (at minimum pinned target ancestors), never via live object-database-wide disambiguation. Preserve full SHA, captured symbolic ref, HEAD-relative, commit, and ancestry checks. Add production regression: activate reader, resolve abbreviation, add unreachable colliding object without HEAD/ref changes, same prefix resolves identically; multiple matching reachable commits reject. No second pin mechanism or test-only production hook.

R3 — Run focused dispatch/pinned-reader/package lifecycle tests, full suite, typecheck, pack, and diff check; commit forward and preserve historical contradictory reviews/Judge disposition.

Return exactly one nonblank keyed line for R1–R3.
