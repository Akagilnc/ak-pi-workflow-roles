# Issue #17 direct Fixer Apply — amended authority and full finding admission

Sole current authority: `.ak/dockets/issues/17/judgment/full-finding-admission-current/receipt.json`, interpreted with `.ak/dockets/issues/17/judgment/owner-amendment-design-recheck-003/receipt.json`. No Plan.

Repair the complete bounded classes:

- `I17 canonical semantic source and recipe`
- `I17 pinned mechanical bundle`
- `I17 common readable materials`
- `I17 atomic settlement and runtime reports`
- `I17 mechanical preflight and capability law`
- `I17 cohesion and regression ownership`
- `MERGE judge class contract test compatibility`

Follow every exact owner/boundary/disposition. Production construction first:

- external canonical Skill remains semantic input only; do not edit/fork/depend on its global installation;
- package typed recipe owns path+digest mechanical transport;
- construct one common pinned bundle before acceptance and materialize/verify it in every clone before provider start;
- relevance hints are nonbinding and never an allowlist;
- preserve distinct Standards/Spec questions and exact reports while allowing free clone reads/citations;
- extend atomic lifecycle through settlement, revoke spawn after settlement, and build typed axis reports verbatim at runtime rather than accepting driver aggregate prose;
- narrow compliance audit to actual method/identity facts; finding/source survival belongs Judge;
- retain exact capabilities/commands and mechanical preflight without task taste or predictive command success;
- deepen the compiler/materializer/executor seams rather than preserving divergent mega-functions;
- preserve #31 scope-key contract, #23 verification/session retention, #33/#36 Recorder, other roles, caller-owned topology, and Reviewer Soul.

Migrate old schemas/tests once; do not keep a compatibility dispatcher or second mechanism. Add focused red/green tests for bundle confinement/digest/readback, zero child before verification, common population and cross-hint legality, one/two-leg cardinality, post-settlement tool lockdown, exact report bytes, completed/refused/fatal matrix, dual identities, and package/cold-install behavior. Remove superseded allowlist/transcription/driver-aggregation assertions.

Run focused tests, typecheck, isolated-HOME full tests, package/cold-install checks, `npm pack --dry-run`, and `git diff --check`; commit forward. Submit truthful typed `classesRepaired` for exactly all seven keys if completed, or refuse with precise residuals rather than claiming partial completion.
