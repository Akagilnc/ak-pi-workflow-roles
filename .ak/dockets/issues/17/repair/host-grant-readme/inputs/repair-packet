# Issue #17 effective host-grant and README repair

Authority: Plan addendum requires each leg’s effective tools equal its declared requirement after proving that requirement is a subset of task-bound ceiling and current host availability. Review: `.ak/dockets/issues/17/review/review-025/receipt.json`.

R1 — Remove full-ceiling host-availability rejection. A ceiling may authorize a tool absent from the current host when no accepted leg requests it. During atomic proposal preflight, validate every tool in each declared leg grant against both ceiling and current host; any requested unavailable tool rejects both legs before effects. Preserve exact grant equality/no widening. Add green ceiling-with-unrequested-edit/read-only-leg and red requested-unavailable-edit cases.

R2 — Correct README: unavailable *requested/effective* host tools fail closed; unused ceiling authority does not. Also document `standardsMaterials` as an array that may be empty, with canonical Standards baseline still mandatory; Spec materials/no-spec evidence remain nonempty.

R3 — Remove unused axis/index parameters from `validateMaterialSelection`. Extract one private exact-event-shape helper for Reviewer ledger branches while preserving distinct closed event schemas and no generic payload retention. Do not split dispatcher policy/lifecycle ownership.

R4 — Add package-lifecycle/readme regression for baseline-only proposal and host-ceiling subset behavior. Run focused tests, full suite, typecheck, pack, and diff check; commit forward.

Return exactly one nonblank keyed line for R1–R4.
