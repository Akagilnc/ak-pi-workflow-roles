# Issue #44 Coder apply — prospective prerequisite audit semantics

Implement test-first the complete follow-up authority in `.ak/dockets/issues/44/authority/prerequisite-semantics-followup/receipt.json`.

At the existing Fixer compliance-auditor seam, make `prerequisite_unmet` prospective and assignment-domain bound without changing the public enum, settlement schemas, Soul, or audit inputs. Add the required provider-backed current-head red and production-lifecycle greens/revise behavior:

- completed work falsely refused because tests were committed later must revise;
- corrected completed settlement disclosing production-before-regression, current verification, and no TDD claim passes unchanged;
- a genuinely absent packet-required predecessor artifact/owner decision that presently makes work inadmissible passes as prerequisite refusal.

Do not regex-test prompt prose, add fields/status/categories, claim retrospective TDD, or add orchestration. Follow probe lifecycle. Update README only if necessary, generated dist if source changes require it, full package tests/gates, and commit forward.