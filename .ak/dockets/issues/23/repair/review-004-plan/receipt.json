{
  "toolName": "ak_fixer_output",
  "toolCallId": "call_w6KBxA1T3LCNmrJddGQtKPOF|fc_0e6fe9a6dd87eeb7016a6c426fa6d08194838d0d49339c3b6e",
  "details": {
    "status": "planned",
    "report": "R1: In `src/recorder/manifest.ts`, add one private manifest invariant constructor that accepts either the original caught cause or a short private authored reason, and replace every direct `internalRecorderError(\"manifest\", new Error(\"manifest invariant\"))` call with that owner. The schema read/JSON catch will pass its caught exception unchanged. In `src/recorder/admit.ts`, attach the caught external `readFileSync` exception to the existing `admission-failed`; in `src/recorder/paths.ts`, attach the caught exceptions from the cited input `realpathSync`/`statSync`, Git top-level discovery, and top-level realpath operations to their existing `invalid-path`/`invalid-archive` errors. Preserve fixed public messages, codes, scanner placement, and expose only the existing allow-listed stage/category diagnostic.\nR2: Extend the owning Recorder production-boundary suites (using the existing real bin harness and temporary filesystem/package seams, without production hooks) for unreadable/malformed manifest schema, unreadable external input, missing/inaccessible path, and invalid/unreadable Git worktree. Each regression will assert exact code and stage, a non-null allow-listed category attributable to the real caught cause, fixed public message, required null or typed location, `not-spawned` child truth, and absence of injected secrets, raw OS/JSON/Git messages, paths, stacks, argv, environment, and IDs. Temporary package copies and permission fixtures will use unconditional cleanup, with platform-safe inaccessible-path handling.\nR3: In `test/recorder-failure-contract.test.ts`, wrap every `recorder-tee-test-*` allocation in `try/finally` recursive removal so roots disappear on pass and assertion failure. Keep one table-driven owning exit/signal tee-settlement regression and do not add or retain a duplicate probe shape; add a temp-directory residue assertion around the focused suite seam where it can verify ownership without depending on presentation text.\nR4: Expand `test/recorder-failure-contract.test.ts` (and shared Recorder harness only as needed) with a shipped-schema validator for complete parsed JSON emitted by the actual `bin/ak-docket-record.js`. Cover real pre-spawn `not-spawned`, post-spawn `exited`, post-spawn `signaled`, and an isolated temporary-package missing-entry launcher fallback, asserting exact exit/signal facts and closed-shape/location/diagnostic laws. Update the launcher's fixed fallback in `bin/ak-docket-record.js` to include `recorder.location: null` and `recorder.diagnostic: null`, with no caught or attacker-controlled interpolation. Retain the existing enum synchronization test separately and clean every temporary root in `finally`.\nR5: Rename the remaining opaque one-letter values, records, paths, kinds, indexes/items, regex argument, and `buildChildEnv` environment/key/value names in `src/recorder/config.ts` to domain terms only. Preserve parser behavior, typed locations, pure-structure → metadata-scan → stateful ordering, and public output; regenerate the corresponding dist file rather than hand-maintaining divergent output.\nR6: Regenerate `dist/recorder/*` with `npm run build:recorder`; run focused manifest/config/admission/failure-contract/CLI/transaction/package-lifecycle suites, full `npm test`, `npm run typecheck`, another clean Recorder build verification, package/schema content checks, `npm pack --dry-run`, and `git diff --check`. Search for direct repeated manifest constructors, cited catches that still discard causes, incomplete real-bin schema coverage, opaque cited config names, and residual/new `recorder-tee-test-*` trees; inspect source/dist synchronization and the complete diff. Confirm only authorized files changed, create one new forward `fix(recorder): ...` commit without amend/rewrite, rerun current-HEAD evidence as necessary, verify HEAD is a strict descendant of opening `0d6d713`, and report a clean worktree. Completion will preserve the historical-root non-claim and issue #13 exclusion."
  },
  "artifactKind": "acceptedReceipt"
}
