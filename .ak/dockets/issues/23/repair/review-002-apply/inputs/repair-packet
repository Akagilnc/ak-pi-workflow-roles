# Issue #23 Judge-authorized Review-002 repair

Sole[REDACTED]

R1 — Make config phases/locations exact: `config-read → config-structure → config-metadata-scan → config-state`; complete all pure checks including provenance and reserved generated paths before scanner/state; explicit safe location for every `invalid-config`; root only for malformed JSON/root; exact fields/indices otherwise; narrow path conversion to known lexical `invalid-path`.

R2 — Centralize structural ownership in config boundary: declaration ID legality/uniqueness, reserved IDs/generated paths, authority/task presence, structural duplicate/conflict checks. Remove duplicate/impossible guards from admission/path policy while retaining stateful content/digest admission facts.

R3 — Immediately install exact child outcome after `spawnOnce` settles and before tee reads/scans. Assign truthful post-spawn, extraction, generated-artifacts, manifest, cleanup, and promotion stages. Manifest/redaction closure divergence is safely observable `internal-error`, never `admission-failed`. Keep one finite allow-listed unknown classifier.

R4 — Add the Judge-required production location matrix, ordering canaries, unknown stage sentinels, exact post-spawn exit/signal tests, actual bin-output schema validation, and secret/nonleak assertions without test-only production hooks. Regenerate source/dist/schema consistency.

R5 — Forward evidence must explicitly retract prior receipt `.ak/dockets/issues/23/repair/review-001-apply/receipt.json` claims that R1/R2/R4 and R6 regression/schema proof were complete. Report only current-head evidence. Run focused/full tests, typecheck, Recorder build, pack, diff check, package checks, and all Judge-listed searches. Preserve historical-root non-claim and issue #13 exclusion.

Return exactly one nonblank keyed disposition for R1–R5; commit forward.
