# Issue #23 Judge-authorized Review-001 repair

Sole[REDACTED]

R1 — Remove broad-boundary laundering. Preserve only owner-established `RecorderError` classifications; route every arbitrary non-Recorder exception through one closed `internal-error` builder with truthful current stage/category. Narrow path conversion to recognized lexical path errors. Manifest implementation/invariant/schema faults must not borrow `admission-failed`.

R2 — Make every `invalid-config` producer provide its exact typed schema location. Root `[]` is only JSON/root failure; array entry failures include indices; duplicate/reserved declarations point to the later indexed field; missing authority/task uses `["declarations"]`; closed-object unknown-key errors identify the containing object without echoing attacker keys. Remove the silent root fallback.

R3 — Add and package one closed Recorder public-failure schema synchronized with TypeScript/runtime: finite stage/category vocabularies, closed objects, typed location segments, bounded diagnostic, and coherent child status/exit/signal combinations. Centralize allow-listed category derivation; no dynamic constructor names, raw messages, stacks, values, argv, or environment.

R4 — Complete the scanner-free structural phase. All shape/type/enum/lexical/uniqueness/reserved-ID-and-path/required-authority-task/cross-field checks finish before credential scanning, canonicalization, repository/file/Git/content/hash/admission/stage/spawn work. Then scan metadata, then perform stateful validation/admission. Own each invariant once.

R5 — Config read failures: recognized missing/inaccessible/non-file paths use truthful `invalid-path` with null location and safe category; malformed JSON remains root `invalid-config`; unknown read failures use stage-aware `internal-error`. Preserve exact child truth.

R6 — Close the README JSON fence and link the shipped failure schema. Add production-seam/table-driven tests for all Judge-listed locations/orderings, broad-boundary sentinel failures, secret canaries, schema validation, and not-spawned/exited/signaled states. Regenerate dist; run focused tests, full suite, typecheck, Recorder build, pack, diff check, and broad-catch search. Preserve the historical root-cause non-claim and issue #13 exclusion.

Return exactly one nonblank keyed disposition for R1–R6; commit forward.
