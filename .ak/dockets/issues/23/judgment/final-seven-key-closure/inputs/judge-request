# Issue #23 final seven-key closure judgment

Adjudicate all seven keys in one invocation and return exactly one explicit nonblank disposition for each key K1–K7, plus one construction posture. This is a batch judgment, not seven serial calls.

- K1 `no-mislabel`: unknown/non-Recorder exceptions cannot impersonate `invalid-config` or another specific label.
- K2 `cause-trace`: caught internal causes leave scanner-safe stage/category evidence rather than being discarded.
- K3 `structural-pointer`: config failures expose safe typed schema path/index locations.
- K4 `mine-removed`: the evidenced pre-spawn catch-all/cost-order failure mechanisms are removed without claiming the unknowable historical 920-second root.
- K5 `static-before-expensive`: pure structural defects are rejected before metadata scanning, filesystem/Git admission, scratch/state work, or child spawn.
- K6 `shared-entry`: all Recorder-wrapped roles use the same entry and no role-specific bypass exists.
- K7 `corrected-case`: durable evidence preserves child `not-spawned`, valid historical config, catch-all impostor label, and indeterminate historical timing/root; no superseded allegation is revived.

Admitted closure evidence includes issue #23 Authority/Plan, the class repair and its receipt, bounded Review-010 (Standards 0, no K1–K7 finding), and bounded Judge-010 (`converged`, sole Scope-B finding dismissed, K1–K7 finding-free). The owner-added Reviewer verification policy is already converged and needs no new disposition here.

Jurisdiction is strict: Review-009 Git-128/location-domain/`__proto__` residuals and all unknown constitutional debt are transferred to issue #26. Do not reopen, repair, or use them to block issue #23. Do not require a Reviewer or Fixer. If all seven keys are delivered, return `converged`; otherwise identify only the failed K-key and bounded evidence gap. Submit only through `ak_judge_output`.
