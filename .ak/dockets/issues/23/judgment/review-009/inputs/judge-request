# Issue #23 Review-009 adjudication

Reviewer fixed target was `932147d7c5b658d6bc96362c84e9a5d66e282908`; current forward head additionally contains owner-requested leg-session persistence `76e7347` and the immutable review docket. Adjudicate every Review-009 Must before repair.

Separate duplicate instances into defect classes. In particular decide:

- whether treating every Git `rev-parse` 128 as no-repository proves the expected-negative predicate;
- whether the shipped location contract is truly typed/closed to actual path shapes rather than token arrays;
- whether lawful `__proto__` environment overrides preserve exact own-property child semantics;
- whether a newly added invocation-recording rule can retroactively invalidate immutable historical manifests created before that rule, where history rewrite is forbidden;
- whether the owner-ratified default-line Fixer Soul merge is within issue #23 repair authority or an actionable issue-23 finding.

Preserve the new owner hotfix: persisted parent invocation sessions must place Reviewer leg sessions under that invocation session directory; manual scratch cleanup remains accepted. Preserve historical-root non-claim and issue #13 exclusion. If repair is sustained, authorize one class-oriented direct Fixer Apply without a Fixer Plan. Submit only through `ak_judge_output`.
