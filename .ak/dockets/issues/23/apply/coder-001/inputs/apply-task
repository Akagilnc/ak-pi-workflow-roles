# Issue #23 authorized Apply

Authority and approved Plan:

- `.ak/dockets/issues/23/authority/judge-001/receipt.json`
- `.ak/dockets/issues/23/plan/coder-001/receipt.json`
- `.ak/dockets/issues/23/plan/judge-001/receipt.json`

Apply the converged Plan test-first. The exact run-1 config is preserved at `.ak/dockets/issues/23/plan/judge-001/inputs/run-1-config`; no original raw child/session log or invoking-script artifact is available, so do not invent it.

Mandatory outcomes:

R1. Diagnose through the real production seam. Reproduce and identify any current swallowed pre-spawn exception if possible; preserve stage/order evidence. If the historical root cannot be reproduced, explicitly leave only that historical claim indeterminate rather than guessing.

R2. Split pure structural validation from stateful validation/admission so all structure-only defects are rejected before repository/file/hash/scan/stage/spawn work, with typed schema locations.

R3. Reserve `invalid-config` for established config defects. Add truthful stable internal/unknown classification and caller-traceable non-secret cause evidence using typed fields plus existing scanning or a bounded 0600 local diagnostic. Never expose offending values/raw messages/secrets and never require regex over prose.

R4. Preserve exact child status and apply one shared Recorder path to every wrapped command/role. No issue #13 recurrence/retry/routing behavior.

R5. Preserve Recorder atomic promotion, evidence admission, credential boundaries, manifest/Receipt contracts, and forward history. Generate packaged artifacts by repository convention; run focused tests, full suite, typecheck, pack, and diff checks; commit forward.

Return exactly one nonblank keyed disposition for R1–R5 through `ak_coder_output`.
