# Reviewer protocol research repair packet

Repair three documentation seams. (1) Make ADR 0010’s caller-ownership decision true across the package’s textual surfaces: delete mandatory Judge provenance/routing from `CONTEXT.md:10`, `README.md:46,75,84,86`, `souls/fixer.md:3,18,31,52-56`, and Fixer/Coder tool metadata; describe caller-supplied inputs and caller-owned disposition instead. Audit all role text for the same contradiction. Do not add a provenance guard, topology mechanism, or new generalized test guard: deleting the erroneous topology is sufficient because runtime already validates only packet/task presence and per-invocation phase invariants. Update only existing exact-text expectations affected by the deletion. (2) Separate accepted policy, current v1, observations, and future proposals in `docs/research/external-reviewer-protocols.md`: put or cite the owner-approved protocol-adaptation rationale in the ADR layer; explicitly mark profile/manifest/receipt designs as unaccepted and unimplemented; state current v1 accurately (`collector-legs-v1` has no `protocol`, transport has no reactions/checks/statuses, submitted statuses are only `valid|unavailable|missing`, and `pending` is internal). Label the JSON and four-state model as future sketches, not usable contracts. (3) Remove unsupported terminal-authority claims: Codex’s `+1` plus commit-naming issue comment is repository observation, not the cited OpenAI contract, so do not call it documented or authorize it as terminal without first-party support and a captured fixture. Cursor’s cited docs define overloaded `neutral` but no stable check-output discriminator; label quota output as observation and do not claim reliable findings/cancellation/internal-error/quota discrimination until supported. Give PR #4/#5 observations durable links/IDs or captured fixtures while keeping them non-normative. Verify the new head with affected existing tests, full tests, typecheck, and documentation consistency checks.

## Judge note

Disposition: both Standards findings are sustained, but Standards #1 and the future-design Spec finding are the same layering/status-blur root, not independent defects. Standards #2 is broader than the receipt states because the bundled Fixer Soul and exact tool metadata also encode Judge provenance/next-role semantics; Coder caller guidance has the same broad-ADR conflict. The Codex and Cursor Spec findings are sustained. GitHub object semantics, CodeRabbit trigger/progress/rate-limit claims, Cursor’s documented success/neutral/failure meanings and analytics fields, and the PR #4/#5 review/comment/check observations otherwise checked out. The absent current c73 PR reaction does not falsify the historical observation because reactions are deletable and a later Codex run replaced visible evidence, but it reinforces the fixture requirement. Test audit on current head: after restoring lockfile dependencies with `npm ci`, `npm test` passed 233/233, `npm run typecheck` passed, and `git diff --check` passed with a clean worktree. Runtime/schema tests cover manifest parity and unknown fields, exact output states, pending-review rejection, comment-only non-completion, HEAD movement, mutation, and failure paths. They do not validate ADR/README/Soul consistency, while an existing exact-metadata test snapshots the contradictory Judge-directed wording; its green result therefore does not cure the defect. No new guard is justified: the reproducible failure is contradictory text, the owning seams are the ADR/README/Soul/tool descriptions, and deleting that text addresses the class without parallel machinery. No baseline smell is carried forward.
