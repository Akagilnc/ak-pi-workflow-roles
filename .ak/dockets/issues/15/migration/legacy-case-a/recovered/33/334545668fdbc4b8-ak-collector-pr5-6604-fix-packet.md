# Collector current-head repair packet at 6604a73

Repair six reproducible seams at their existing owners; do not change Collector Soul or add parallel classifiers.
1. `src/collector-receipt.ts`: remove snapshot-membership/first-observation as proof of target-scoped unavailability. Require evidence-carried exact-HEAD provenance (head-bearing review/review-comment records must match `targetHead`; headless issue comments must not qualify as `target` without an explicit binding). Preserve `global` behavior. Add the missing case where Collector starts on H2 and first sees an H1 review/comment.
2. `src/collector-role.ts` → `src/collector-ledger.ts` → `src/collector-github.ts`: thread the request tool's `AbortSignal` through the existing request/POST transport seam, preserving cancellation as cancellation and killing the owned `gh` child. Cover a hung real POST path and ensure cancellation cannot become an ordinary rejected attempt or late successful tool result.
3. `src/collector-evidence.ts`: a review version first seen only after the deadline must not inherit/backdate its current body/state from `submitted_at`; retain an uncertain authoritative time so it cannot prove valid/unavailable. Keep the repaired known-version null/uncertain reuse. Add an after-cutoff first-observation receipt-path test.
4. `src/collector-github.ts`: after a 2xx POST, parse/normalization failure is ambiguous response loss, not rejection, because the comment may exist. Route it through the existing marker recovery path; keep non-2xx as rejected. Test malformed, truncated, and required-field-missing 2xx responses plus later observe recovery without reposting.
5. `src/collector-receipt.ts`: do not attach another account's inline reply to a configured reviewer's report solely by `pull_request_review_id`; minimally require matching author ownership (while retaining the raw record in the self-contained ledger). Test an intruder reply sharing the review id.
6. `src/collector-config.ts` and `schemas/collector-legs-v1.schema.json`: align the published request-body contract with runtime trim-non-empty and 60,000 UTF-8-byte validation, using a nonblank constraint and explicit byte-limit annotation/metadata where JSON Schema cannot express bytes. Strengthen parity tests for both packaged and exported schema copies, including whitespace and multibyte boundaries.

## Judge note

Audited exact clean HEAD `6604a733886dfb5d074f558963e20e01e587aa6d`. Current verification passes (`npm run typecheck`; full `npm test`: 216/216; `npm pack --dry-run`; `git diff --check`), but the green suite lacks the six paths above. Direct current-code reproductions showed: an H1 review first observed on H2 is accepted as target-unavailable and stamped H2; a version first observed at 00:16 is assigned its pre-cutoff `submitted_at` and accepted unavailable; an `intruder` reply is rendered/cited in the Codex report; malformed HTTP 201 is classified `rejected`; and the schema accepts whitespace and >60,000-byte bodies.

Prior-inline disposition: the repeated valid-precedence findings (R1: missing/unavailable), valid cross-leg-reference findings (R2), tombstoned-author findings (R6), and pagination-budget findings (R10) are duplicates already disposed on current HEAD. The singleton prior fixes for known edited-version timestamp reuse/model-view consistency (R5), full transitive receipt embedding (R4), outdated `originalLine` fallback (R7), authenticated-user raw redaction (R8), PR `updatedAt` observation bracket (R9), and observe/GET cancellation (R11) remain present with focused tests. The current first-seen-after-cutoff and request-POST cancellation findings are distinct from R5/R11. The two prior target-scope comments (R3) are not fully disposed: `establishedHeadFor` still relabels stale evidence first observed after a HEAD move, so that residual is item 1.
