Plan not approved. Revise the linked full plan at these exact seams:

1. Schema: `/tmp/ak-collector-fixer-plan-2.md:86` uses `^.*\\S.*$`, which rejects nonblank multiline rationales even though the required law is trim/non-whitespace nonblank. Use the singular TypeBox owner with an exact non-whitespace predicate (for example unanchored `\\S`) and add a schema-valid multiline control so registration, batch `Value.Check`, and receipt parsing cannot over-reject.

2. Ownership: define “latest relevant” mechanically (the canonical ledger attempt ordering and the exact proof/status eligibility), then add a two-attempt same-leg counterexample proving older same-leg comment/snapshot/recovery refs are not auto-linked. Resolve the allowed-class ambiguity: the table currently broadly allows expected-author material while §3c says after/uncertain material is non-qualifying, and the transport row leaves an optional “or explicit global” policy. State one status-specific predicate at the owning receipt/attempt seam. Split M2 into independent snapshot and recovery rows and M3 into independent dangling evidence and dangling snapshot rows; slash-separated fixtures do not satisfy one decoy per counterexample.

3. Concrete F3 construction: replace “forced … fixture/API abuse,” “register extension such that,” and “inject failing/invalid” with executable fixture mechanics. Specify how the real in-process Pi registry is made to omit one required tool without a production hook; inject exact nonempty `systemPromptOptions.skills`, `contextFiles`, and `appendSystemPrompt` values through the real system-prompt event, and register an exact ambient command through the real command registry. Every row must assert provider count and every GitHub counter are zero. Likewise state exactly how two embedded records/snapshots are made to collide at `buildCollectorReceipt` without failing at an earlier lookup, and construct the requested terminal v3 edit rather than merely naming v3 while describing only v1 plus one edit.

4. Byte seams: do not assume one added source byte produces one normalized/receipt byte; calibrate and assert each measured target independently. Specify a reproducible legal fixture that reaches 8 MiB and 8 MiB+1 through real `observe`. For 32 MiB, remove the `and/or` test-home ambiguity and spell out how the calibrated ledger is replayed into the in-process Collector role: each snapshot remains within 8 MiB, ledger materialization remains below its earlier 32 MiB gate, exact MAX is accepted, and exact MAX+1 executes `ak_collector_output`, hits the receipt size check and `ledger.latchFatal`, invokes a `failInfrastructure` spy, exits nonzero, and emits no receipt. A direct builder used for calibration cannot substitute for that role-path assertion.
