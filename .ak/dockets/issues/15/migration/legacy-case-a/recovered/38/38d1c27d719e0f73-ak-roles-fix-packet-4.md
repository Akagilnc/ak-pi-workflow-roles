Authority set: `/tmp/ak-roles-authority-v2.md` clauses 2–3 (no ADR or ticket authority was supplied). FIX_NOW — P1, coalescing the duplicate Standards/Spec reports: at clean HEAD `93c8911`, `src/soul-auditor.ts:111-126,175-181` resolves auth with provider-only `getProviderAuth(model.provider)` and directly invokes `provider.stream()`. Pi’s model-aware path calls `getAuth(model)` and merges configured per-model headers (`node_modules/@earendil-works/pi-coding-agent/dist/core/model-runtime.js:262-275`), while `ModelRegistry.getProviderAuth` delegates with only the provider string (`model-registry.js:68-69`). Thus a judge request may succeed but its mandatory Soul audit can omit routing/tenant/auth headers and fail or target the wrong tenant, violating clauses 2–3. Repair the audit dispatch so it preserves the newly supported auth-resolved base URL/API key/env while also using Pi-equivalent model-aware header preparation; avoid another provider-only/manual partial preparation seam. Add a behavioral integration test where provider-auth headers/base URL and configured per-model headers are distinct and assert the audit receives their correct merged result. The current integration test proves only provider-auth resolution, so it cannot close this defect. Verification observed on this HEAD: `npm test` green (35 tests) and `npm run typecheck` green, but neither exercises the missing model-header path. Repair-surface ledger: this is a fix-fix in the current auth-preparation seam; prefer consolidation/simplification over another additive mechanism.
