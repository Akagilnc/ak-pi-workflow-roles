# Collector two residual receipt

```json
{
  "status": "completed",
  "report": "# Fixer apply — two residuals @ 25d0da2 → f2c0614\n\n## Status\n`completed`. Single forward commit `f2c06146fb4fc89ac88675c41a8d976298bef623` (strict descendant of plan HEAD `25d0da2`). No amend/rewrite. Worktree clean; only authorized residual files touched.\n\n## Residual 1 — signal-state cancellation\n**Owner:** `src/collector-github.ts` `createIssueComment` catch.\n\n**Knife applied:** Primary authority is `input.signal?.aborted`; on true, `throw error` preserves runner `signal.reason` (Error or non-Error). Removed `/abort|cancel/i` message matching. Kept `AbortError` name as non-signal belt only. No new cancel enum/binder/ledger state.\n\n**Green coverage (`test/collector-github.test.ts`):**\n- Shared hung-POST child-kill fixture for:\n  - existing `request canceled`\n  - `Error(\"deadline exceeded\")` (no abort|cancel text)\n  - non-Error `\"stop now\"`\n- Asserts original reason text, child killed, attempt status not in `rejected|ambiguous_loss|succeeded|recovered`, process-local one-shot blocks second same-HEAD request.\n- Existing R11 observe/GET cancel tests remain green.\n\n## Residual 2 — conservative first-sighting time\n**Owner:** `src/collector-ledger.ts` `observe()` evidence stamping.\n\n**Knife applied:** After successful `fetchObserveSurfaces` (+ identity retry), stamp normalize/`firstObservedAt` from `clock.wallNow()` at surface hand-off. Snapshot `observedAt` (start) / `completedAt` (end) distinction preserved. Budget retain during fetch may keep temporary start stamp. `applyEvidenceVersionHistory` predicate untouched; exact-deadline equality still retains `submitted_at`.\n\n**Green coverage (`test/collector-receipt.test.ts`):**\n- New integration: activation 00:10, observe starts 00:24:59 empty; mid-listReviews advances to 00:25:01 and injects expected-author exact-head review with backdated `submitted_at`. Asserts `firstObservedAt > deadline`, `authoritativeTime=null`, not `before`/`within`, cannot prove `valid` or `unavailable`.\n- Existing unit oracles remain green: exact-deadline keep `submitted_at`; after-deadline null + R5 known-null reuse; after-deadline first observation receipt gate.\n\n## Verification\n- `npm run typecheck` — pass\n- `node --import tsx --test test/collector-github.test.ts test/collector-ledger.test.ts test/collector-receipt.test.ts` — 86/86 pass\n- Worktree = only the four authorized files\n- HEAD forward of `25d0da2`; no Soul/docs/schema edits\n\n## Refused / out of scope\nNone. Both residuals adopted at named seams; no regex extension, no one-shot/child-kill/exact-deadline relaxation, no firstObservedAt backdate+heuristic compensation.\n\n## Preservation\nR1–R11 and prior six-root 25d0da2 seams retained (target unavailable provenance, 2xx parse→ambiguous_loss, inline author ownership, body schema parity, request signal threading, after-deadline guard existence).",
  "commitSha": "f2c06146fb4fc89ac88675c41a8d976298bef623"
}
```
