Wave-1 needs one focused ledger correction, not a redesign. In `src/reviewer-execution-ledger.ts`: (1) require the current runtime `description`/`prompt` observation to equal the persisted current call before creating, updating, or dispatching an attempt; treat disagreement as an atomically recorded provenance failure; (2) permit repeated `beginAgentCall` only while that same attempt is running and the observation is identical—re-beginning a successful or failed attempt must fail before child execution, preserve the terminal attempt, and record fatal state; (3) remove the `@earendil-works/pi-ai` import by owning a structurally compatible ledger usage/evidence type; and (4) make `recordInfrastructureFailure` return the exact original thrown value, including strings and sentinel objects, while separately deriving copied diagnostics/evidence. Do not change `failAgentCall`'s expressly allowed wrapping of non-Error child failures. Add direct ledger tests and focused production-handler tests for first/repeated argument conflict, terminal replay with zero additional child starts, and exact non-Error identity through audit/cleanup fatal adapters. Correct the existing defensive-copy fixture that currently legitimizes different persisted/runtime arguments. Keep the current operation-based factory; do not add handles, getters, target-binding machinery, or broader runtime decomposition. Re-run typecheck, isolated-HOME tests, dry-run pack/no-SKILL check, diff-check, and confirm a clean new HEAD.

## Dispositions
Adjudication against the ordered wave-1 authority and clean `6bb1eb6c10da4dcb5dd3132575298993346a085b`:

- **Persisted/runtime Agent arguments (Standards 1; Spec 2): sustained, live ledger/runtime contract defect.** Independent probe persisted `persisted-A prompt`, began with `runtime-B prompt`, completed successfully, and produced an audit attempt containing `runtime-B prompt`. This contradicts the approved requirement that repeated Pi observations be identical and lets dispatched/audited provenance diverge. These two receipt entries are one root issue.
- **Terminal re-begin (Standards 2): sustained, live lifecycle defect.** Independent `running → successful → beginAgentCall` probe returned normally. The registered execute path consequently has no pre-dispatch stop. Exact lifecycle ownership requires rejection before a second child can run.
- **Conflicting snapshots (Standards 3): counterexample reproduced, defect conclusion rejected as an out-of-contract/spec interpretation.** A directly injected runner result can produce top-level `HEAD-A` with attempts `HEAD-A,HEAD-B`. However the production owner of the session-pinned-target invariant is `createReviewerAgentRunner`: one `snapshotPromise` supplies every leg, changed repository roots are rejected, clones are verified against that snapshot, and the real two-leg pinned-snapshot test passed. The approved plan expressly requires preserving first-invocation top-level snapshot selection. A second ledger consistency guard lacks a real production failure and would widen this behavior-preserving wave toward target binding.
- **Attempt-handle/state-machine redesign (Standards 4): rejected as judgement-call architecture advice.** The approved authority explicitly permits the present operation set. The module exposes no getters or mutable state bag and hides reconciliation, transitions, proof, cloning, and freezing. Findings above are local missing checks, not evidence that a parallel handle abstraction is needed.
- **Pi SDK type import (Spec 1): sustained as a spec-only boundary miss, not a runtime defect.** Line 1 is a type-only import, but the approved revision unambiguously says the factory/module has “no Pi imports.” A local structural usage shape is sufficient and smaller than retaining SDK coupling.
- **Original infrastructure throw identity (Spec 3): sustained as a behavior-preservation defect.** Independent sentinel-object probe showed `recordInfrastructureFailure(sentinel) !== sentinel` and returned `Error('[object Object]')`. Baseline audit/cleanup catches passed the original unknown value to `failInfrastructure`; the approved revision specifically preserves that value. The immediate host paths currently throw the replacement.

Current mechanical gates are otherwise green: typecheck passed, isolated-HOME tests passed 84/84, dry-run pack had 14 files with the ledger and no `SKILL.md`, diff-check passed, commit count is one, and the worktree remained clean. Those tests omit—or, for argument mismatch, currently normalize—the sustained counterexamples.
