# Collector Fixer receipt 2

```json
{
  "status": "completed",
  "report": "# Fixer apply receipt — Collector second repair (F1–F3)\n\n**Status:** `completed`  \n**Base HEAD:** `c7ee3b56597470b273603a214abcc65ced6c1182`  \n**New HEAD:** `b43bf20c7d0f082d5c9e69e929624819ea97df8d`  \n**Forward descendant:** yes (`c7ee3b5` ⊂ `b43bf20`)  \n**Soul / orchestrator:** unchanged\n\n## What was fixed\n\n### F1 — Singular TypeBox schema owner\n- Added `src/collector-tool-schemas.ts` as the sole arg contract:\n  - observe/request/wait/output schemas\n  - status-discriminated output leg union\n  - `unavailableScope` required only on `unavailable`\n  - nonblank rationale via unanchored `pattern: \"\\\\S\"`\n  - `additionalProperties: false`\n- Wired three consumers to that owner:\n  - `collector-role` tool registration parameters\n  - `collectorToolArgumentsValid` via `Value.Check` (rejects `undefined|null` before check)\n  - `parseCollectorOutputCandidate` via `Value.Check`\n- Deleted dual hand-rolled key-list / optional-scope-on-all-status validators\n\n### F2 — Leg-owned receipt refs\n- Rewrote `collectMissingProofRefs` to auto-link **only** `latestRelevant(L)` attempt’s present `{commentEvidenceId, snapshotId, recoverySnapshotId}` (+ final snapshot)\n- `latestRelevant` = last chronological same-leg attempt with `status ∈ {succeeded,recovered}` and at least one of those proof ids\n- Missing model cites fail-closed to allow-list (final snapshot, final PR/user facts, expected-author material, same-leg attempt-owned ids, marker-joined same-leg requester comments)\n- No independent `ak-collector:v1` / `leg=` parser for join\n- Unavailable: every model cite must `qualifiesUnavailableEvidence`; bind leg + terminal-report refs to qualifying proof only (deleted `boundRefs = proof ∪ remaining`)\n- Transport failures never enter leg/report refs\n\n### F3 — Exact constructions\n- Timestamp-less review state/text via real `applyEvidenceVersionHistory` seam (reused `submitted_at`, not cleared)\n- v3 before→after comment edit; review edit/dismiss/disappearance retention\n- Three collision facades (duplicate evidenceId / snapshotId / cross-namespace) with specified embed/lookup paths\n- Harness pass-through: `noSkills`, `noContextFiles`, `skillsOverride`, `appendSystemPrompt`\n- Required-tool absence: same-API `registerTool` wrapper + `createRoleRuntimeExtension` (Plan Judge wiring correction)\n- Ambient skills / contextFiles / appendSystemPrompt / commands\n- Explicit `session_start` → `createCollectorRoleRuntime.activate` for overflow role path\n- Independently measured exact 8 MiB snapshot MAX accept / MAX+1 fatal (`review.body` + `raw:{}`)\n- Independently measured exact 32 MiB receipt MAX accept / MAX+1 fatal via one-leg valid rationale; role path hits output execute → size check → `latchFatal` → spy `failInfrastructure`\n\n## Tests added/updated\n- F1 schema matrix + registered-schema inspection + real-Pi invalid output rows/siblings/controls\n- F2 recovered-then-succeeded latestRelevant + contamination + M*/U* decoys\n- F3 §3.1–3.4 exact fixtures above\n- Package lifecycle file lists include `src/collector-tool-schemas.ts`\n\n## Verification\n- `npm run typecheck` — pass\n- `HOME=$(mktemp -d) npm test` — **191/191 pass**\n- `git diff --check` — clean\n- `npm pack --dry-run` — includes `src/collector-tool-schemas.ts` (26 files)\n- One forward commit only; no amend; working tree clean\n\n## Refused / not adopted\n- None. Full F1–F3 packet adopted.\n\n## Commit\n```\nb43bf20 fix(collector): close F1–F3 schema, leg-owned refs, and exact boundary residual\n```\n\nFresh independent Reviewer over `c5f75b6...b43bf20` is for Flow/Judge; not performed by Fixer.",
  "commitSha": "b43bf20c7d0f082d5c9e69e929624819ea97df8d"
}
```
