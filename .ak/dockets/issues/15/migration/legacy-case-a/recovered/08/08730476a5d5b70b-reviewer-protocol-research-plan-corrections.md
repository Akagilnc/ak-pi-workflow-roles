Do not authorize apply yet. Revise `/tmp/reviewer-protocol-research-fixer-plan.md` as follows:

1. **Close Root 1’s audit surface.** Add `README.md:181` (“routing belong[s] to a separate orchestrator”) to Owner/Red/Green/Scope and replace it with caller ownership/optional-orchestrator wording. Provide a repository-wide disposition of every topology hit, not only Fixer/Coder sections. In particular, `docs/adr/0003-per-role-submission-tools.md` is currently `proposed`, not a safely classified “historical ADR”; either mark its Judge-consumer clause as superseded by ADR 0010 or give a verifiable reason it remains nonnormative. Classify the other Flow/orchestrator ADR text explicitly. Keep legitimate Judge-role descriptions distinct from worker provenance/routing claims.

2. **Perform the required Fixer Soul layering audit.** Root 1 currently proposes editing `souls/fixer.md` while retaining schema/runtime/transport rules such as singleton invocation, exact status/`commitSha` combinations, and `escalate` impossibility. Record their disposition under Behavior/Red/Green/Scope: remove mechanically owned material from the Soul and retain only irreducible repair judgment principles. Do not add replacement runtime guards or tests.

3. **Give Root 3 object-level durable anchors.** PR container URLs plus a commit SHA do not identify the mutable observations. Cite stable object URLs/API IDs for every retained unsupported observation—for example Codex comment `#issuecomment-5102352848`, Cursor quota comment `#issuecomment-5102308579` and check run `90233500616`, and the applicable CodeRabbit comment/review/discussion IDs. The claimed `c73bf31…` reaction is absent from the current PR #5 reaction listing and has no captured fixture/ID in the plan; remove that factual claim or supply a durable captured identifier. Do not substitute the later reaction `428138437`, which corresponds to a different run. All such material must remain explicitly dated, nonnormative observation; no profile terminal rule may derive from it.

Preserve Root 2’s proposed separation: the thin owner-approved ADR owns only adaptation rationale, while a separately labeled Current v1 section cites the existing schema/transport/receipt rules and states that no protocol field/profile or expanded transport/status exists. Keep all profile shapes under unaccepted future sketches. Scope remains documentation/tool-metadata text and exact-string expectation updates only—no runtime, schema, transport, receipt, profile, or protocol mechanism.
