# Judge-authorized Collector repair packet 2

The six-defect repair is not complete on c7ee3b5.

1. Make finalized-batch argument classification match the registered schemas exactly. `src/collector-ledger.ts:169-236` does not reject unknown fields inside output legs and does not validate `unavailableScope`; it also treats missing observe arguments as valid despite the approved raw-call rule. An independent probe showed sole output calls with an extra leg field, an invalid/misplaced scope, and related malformed output shapes all return `allow:true`. Reject these at `message_end`, latch fatal, and add the ordered real-Pi sole-schema-invalid and valid+schema-invalid output cases with zero side effects.

2. Repair leg ownership in receipt refs. `collectMissingProofRefs` (`src/collector-receipt.ts:327-375`) adds every authenticated `ak-collector:v1` request marker to every missing leg, regardless of attempt leg. A two-leg probe requested leg `a`, then emitted both legs missing; leg `b` incorrectly contained leg `a`'s `commentEvidenceId` (`bContaminated:true`). Unavailable validation likewise preserves requester/wrong-leg and mechanically non-qualifying extra refs instead of rejecting all decoys required by the approved plan. Associate request comments, attempt snapshots, recovery snapshots, and transport facts with their owning leg; auto-link only the latest relevant same-leg proof and reject wrong-leg/non-qualifying candidate refs. Add two-leg tests for both missing and unavailable, asserting leg and terminal-report refs.

3. Complete the binding test plan rather than relying on labels. The v3 before-deadline comment edited terminal after deadline case, timestamp-less state/text variants, review edit/dismiss/delete report retention, required-tool absence, ambient instruction-resource failures, ID-collision cases, and exact byte boundaries are absent. In particular `test/collector-ledger.test.ts:761-763` says “max accept, max+1 fail” but explicitly does not construct either exact boundary, and the receipt-overflow test only grows the ledger; it never exercises `src/collector-receipt.ts:748-753`. Add exact `==8 MiB`/`+1` and `==32 MiB`/`+1` tests without truncation, including the actual output infrastructure-failure path.

After a forward repair commit, rerun typecheck, the hermetic full suite, focused real-Pi probes, and diff checks. Then obtain the already-ordered fresh independent Reviewer over the full Collector range `c5f75b6...<new HEAD>` and return its receipt; convergence remains barred until that review passes.

Independent current-head gates passed (`npm run typecheck`; hermetic `npm test`: 169/169; `git diff --check`; clean tree; ce5d2e4 is an ancestor). Those green gates do not cover the focused failures above. No fresh full-range Reviewer receipt was supplied.
