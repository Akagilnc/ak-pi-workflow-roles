# Judge-authorized Collector repair packet

Repair only these evidenced Collector construction defects on a forward commit:

1. **Enforce finalized-message batch provenance, not a filtered duplicate.** `src/collector-ledger.ts:290-299` ignores non-Collector siblings and stores no permitted call provenance (`lastAssistantBatchKey` is unused). A batch containing valid `ak_collector_observe` plus `unknown_tool` is currently allowed and the observe can begin. Make legality depend on the entire finalized tool-call batch; persist the exact sole permitted call ID/name and cross-check it at `tool_call` and execute. Any extra, mixed, schema-invalid, unknown, early-output, second-output, or later-turn batch must latch fatal before a valid operation executes. Prove valid→invalid, invalid→valid, invalid→invalid, operational↔output, dual-output, two-valid, and unknown-sibling cases through real Pi, asserting zero transport/request execution—not only by calling `evaluateBatch` directly.

2. **Make cutoff/final-snapshot law mechanical.** An independent current-head probe accepted `missing` immediately at activation (`mono=0`), and accepted output on the stale pre-request snapshot after a successful POST. `request`/`wait` only clear `finalObservationCompleted` (`collector-ledger.ts:593,671`), while receipt validation checks it only when `finalObservationRequired` is true (`collector-receipt.ts:311`). Require a new complete observation after every request/wait; observe and output boundaries must evaluate monotonic cutoff, including operations/provider turns crossing it. Permit `missing` only after cutoff and the required final complete observation; valid/unavailable may still terminate early when proven. Record final observation completion time rather than the pre-fetch time at `collector-ledger.ts:371`, and ensure the snapshot’s OPEN state/HEAD are the final authoritative read. The request precheck must use the same eligible qualifying-review law as receipt validation, including authoritative time.

3. **Correct immutable version time provenance.** `normalizeReviewEvidence` assigns `submitted_at` to every body/state version (`collector-evidence.ts:243`). A probe observed an old review, edited its body after deadline to a decline without an authoritative update timestamp, and the new version was labeled `before` and accepted as `unavailable`. Separate the submitted review event from mutable body/state versions: use an authoritative version timestamp only when GitHub supplies one; otherwise mark the changed version `uncertain`, never backdate it with `submitted_at` or `firstObservedAt`. Digest all normalized version-significant fields. Add the v3 cases for pre-activation review edited after deadline, before-deadline comment edited terminal after deadline, and timestamp-less state/text changes.

4. **Preserve prior inline findings in reports with their own versions.** `collectSubstantiveReviewReports` builds every historical review against only `finalSnapshot` (`collector-receipt.ts:287-294`). A probe showed a prior inline finding remained in `evidenceRecords` but disappeared from every report once absent from the final snapshot. Build each historical review/inline report from the immutable versions and observation membership that actually contained it; do not attach final-snapshot inline text to older review versions. Prove A→B→C, inline removal/edit, and review edit/dismiss/delete while retaining every distinct prior substantive version and correct head/window tags.

5. **Finish self-contained receipt invariants and fatal failure behavior.** Bind unavailable report relation to the actual qualifying evidence rather than the first arbitrary ref; reject wrong-leg/author refs. Missing must cite the final snapshot plus automatically retained latest relevant pending/negative evidence. Preserve request-loss/recovery transport facts and the snapshot that established authenticated-marker recovery. Ensure embedded snapshot/evidence IDs resolve uniquely and that omitted unrelated records cannot leave missing-proof completeness unverifiable. Make receipt/ledger 32 MiB overflow a latched non-zero infrastructure failure, not a retriable ordinary output error (`collector-receipt.ts:611-615`), and test exact 8/32 MiB boundaries without truncation.

6. **Cross the actual operational/startup/package seams.** Current transport tests inject a `GhApiRunner`, Collector lifecycle tests inject an in-memory transport, and the package test inventories a tarball without installing/running it. Add a hermetic executable fake `gh` test through `createGhApiRunner` for fixed argv, pagination, POST success/rejection/ambiguous loss and recovery; real Pi tests for detectable required-tool collision/absence and exposed ambient instruction resources with zero provider/GitHub side effects; and an installed-tarball independent-consumer print/JSON lifecycle. Keep the Soul thin and do not add parallel mechanisms.

## Judge evidence note

Independent verification on exact head `ce5d2e48477c238b3c4fe3f7b5f11dda723384d4`: `npm run typecheck`, `HOME=$(mktemp -d) npm test` (147/147), `git diff --check`, and `npm pack --dry-run --json` pass, but the probes above demonstrate those tests do not enforce the controlling behavior. No fresh Collector Reviewer receipt was found. After repair and fresh gates, obtain a fresh independent Reviewer against the new head before convergence can be considered.
