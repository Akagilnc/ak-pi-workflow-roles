# Collector repair packet 5

Smallest remaining packet on clean HEAD 91a1a92:

F1 — close the still-visible exact-oracle gaps, test-only.
1. `test/collector-receipt.test.ts`: remove the `raw === null` skip so every malformed output row reaches `classifyCollectorBatch` and is denied. Also classify `ak_collector_observe` with arguments `{}` and assert the legal result (`allow: true`, permitted operational `ak_collector_observe`), rather than only asserting `collectorToolArgumentsValid`.
2. `test/collector-role.test.ts`: replace/extend the sole observe+blank-rationale sibling with the two required real-Pi sibling rows: unknown leg field and `unavailable` without `unavailableScope`. Each row must assert nonzero/fatal termination and all six GitHub counters zero.

Skill authority — implement only the owner-approved distinction.
3. Add a real-Pi startup test using a normally loaded/discovered Skill (`noSkills: false` plus an actual Skill resource/command, including the command-only `disable-model-invocation` boundary if matrixed). It must prove startup configuration failure, nonzero exit, no successful `ak_collector_output` receipt, all six GitHub counters zero, `faux.state.callCount === 0`, and the response remains pending. The existing fabricated extension command and late mutation tests do not prove this loaded-Skill path. The current activation-time `pi.getCommands()` seam should already own this; if the red test exposes a gap, detect `command.source === "skill"` there. Do not add a provider proxy.
4. Keep the existing `before_agent_start` Skill-mutation row explicitly labeled as an unsupported hostile sibling-extension injection. Assert latched fatal/nonzero, no successful receipt, and all six GitHub counters zero. Do not assert provider call count or pending responses for this row: Pi 0.82.1 makes provider count non-normative at that late seam. This does not permit the Skill. The packet-4 demand for provider-zero on this late Skill row is superseded; preserve the existing non-Skill ambient rows without using them to broaden that exception.
5. `README.md` and packaged `--help`: state categorically that Collector forbids every Skill, including command-only Skills; state the supported profile (`--no-skills`, `--no-extensions` with only the explicit Collector package extension, no prompt templates/context files, one print/JSON prompt); and document the narrow Pi 0.82.1 late-hostile-extension limitation without implying a security boundary or provider-zero guarantee. Strengthen `test/collector-package-lifecycle.test.ts` to assert the actual help text, not merely the presence of Collector flags.

No Soul change, provider gateway/proxy, or upstream Pi dependency is authorized. Run the focused Collector role/receipt/package tests and `npm run typecheck` on the new head.
