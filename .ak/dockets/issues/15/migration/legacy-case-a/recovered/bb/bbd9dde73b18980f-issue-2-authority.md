# Issue #2 frozen authority

READY for construction under this frozen interpretation of issue #2.

Evidence basis: HEAD is the requested clean `c04d24e19390cab239b8c7e31c7ccb9a8975ad18`; issue #3 is CLOSED by PR #7 and its law is now in `souls/judge.md` (Authority/Plan/Apply/Review inferred from supplied material; Plan uses Behavior/Owner/Red/Green/Scope). `npm test` passed 259/259, typecheck passed, and `npm pack --dry-run` confirms the current published surface. ADR 0010 makes composition/provenance caller-owned; ADR 0011 forbids turning current Reviewer/Collector implementation facts into universal provider law.

Authority and owners:
1. `packets/judge-authority.md`, `judge-plan.md`, `judge-apply.md`, `judge-review.md`, and `fixer-repair.md` are repository-contributor templates for manually supplied artifacts. They consume #3; they do not restate or amend Judge law. The filename identifies an evidence burden, not a verdict, phase flag, transition, or required predecessor/successor. Selection and use are caller-owned. No packet may say a repair packet came from Judge, must go to Fixer, or must return to Judge.
2. `docs/development-closure.md` owns only this repository's contributor/dogfood checklist. Its canonical manual record sequence is: seal authority inputs; record any authority judgment; seal a construction plan with the five Plan facts; preserve construction receipt/commit/test evidence; record Apply judgment; preserve independent review and per-finding adjudication; issue a new forward repair artifact when needed without overwriting prior artifacts. Maintainers may omit an inapplicable beat with explicit disposition. This is descriptive host practice, not packaged workflow authority or generic role ordering.
3. `CLAUDE.md` owns probe hygiene. A probe is temporary evidence, not specifically a Judge draft. After its evidence purpose is disposed, delete it or graduate its behavior exactly once into the regression suite owned by the affected seam; delete the scratch copy. Only a bare-seam probe that ordinary tests cannot reach may remain under `test/adjudication/`. No Judge origin, Fixer/Coder destination, `/tmp` truth, or duplicate permanent shape is required.
4. #3 remains the sole owner of posture semantics. Soul files, README verdict semantics, and role contracts are not to be changed by this issue.

R# contradiction resolved: the repeated final “file-only” scope controls. Current Fixer accepts only `status`, free-form Markdown `report`, and optional `commitSha` in `src/worker-role.ts`; it has no Soul-compliance audit. Judge's Soul audit is Judge-only, Reviewer has a separate method audit, and Collector's ledger is role-specific. Therefore issue #2 grants no schema, runtime, auditor, receipt-envelope, or cross-role ledger change, and “reuse existing audit/receipt machinery” is factually inapplicable. `fixer-repair.md` may require contributor packets to enumerate unique `R1..Rn` items and request a Markdown report table `R# -> implemented(test name) | refused(reason)`; the contributor checklist may require manual exact-set reconciliation before accepting that artifact into the development trail. It must not claim mechanical enforcement or Soul-audit rejection. Mechanical Fixer receipt enforcement requires separate authority explicitly changing the public Fixer contract, with the Fixer output schema/runtime as owner.

Evidence and trust freeze:
- Every instantiated contributor packet identifies artifacts by repository-relative path plus SHA-256 of exact bytes. Code/apply facts also bind the full target commit SHA; review ranges bind full base and target commit SHAs. Digests seal identity only—they do not prove truth, acceptance, or freshness. Judge independently checks claims against the supplied current target.
- Authority packets carry clauses, decisions, counterexamples, boundaries, and unresolved owner choices; they do not require Apply proof. Plan packets carry authority identity and Behavior/Owner/Red/Green/Scope, without fixture pseudocode or blanket `file:line`. Apply packets carry sealed authority/plan identities, live code/tests, real-seam and boundary evidence, committed target/range identity, and `file:line` only where applicable. Review packets bind every finding/disposition to sealed authority, fixed reviewed range, and current facts; they define no reviewer protocol. Guardrail triad evidence appears only when a change adds or approves a guardrail.
- A successful Judge audit proves demonstrated Judge-Soul compliance, not independent fact verification or digest validation. Fixer/Coder have no Soul audit; Reviewer audit and Collector runtime retain only their current documented meanings. Templates create no new trust level.
- An accepted artifact is preserved with its exact bytes/digest and receipt. Amendment or replacement is a forward commit/new digest with explicit disposition of the prior artifact. Verdicts are recorded in receipts/trail, never inferred from names. Restart/compaction recovery is manual rereading of that trail; no package memory or resume semantics arise. Timeout guidance remains docket-dependent and creates no runtime budget.

Authorized construction surface is the five `packets/*.md` files, `docs/development-closure.md`, and one concise `CLAUDE.md` probe paragraph. Keep `packets/` and `docs/` outside `package.json#files`; do not modify README, souls, schemas, runtime, role receipts, or audits. Preserve all current tests/typecheck and verify the packlist remains unchanged except that these repository-only files stay excluded.

Non-goals remain absolute: no orchestration, routing, next-role, stage/transition machine, workflow DSL, flow ledger, package memory, posture flag, mandatory role repetition, conditional `direct-apply`/`plan-first` marker from the unratified #3 comment, or provider-specific generic Reviewer/Collector law.
