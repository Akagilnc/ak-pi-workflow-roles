Authority set: /tmp/ak-roles-authority-v2.md clauses 1–9; no ADR or ticket was supplied or found. Prior approved adjudication at c822717, repair-packet item 6, remains binding. Fresh evidence was checked on clean HEAD c206022: strict ancestry from c7eaeda, npm test (34/34), npm run typecheck, and npm pack --dry-run are green.

Finding record:
1. fix_now [P2] The required package-entrypoint/Pi-boundary test is still absent (authority clauses 1–4; prior repair item 6). test/judge-role.test.ts:18–22 defines an any-typed captured tool, :33–51 hand-builds a partial ExtensionAPI, :54–74 manually inserts the assistant message, and :109–115, :145–151, :265–272 directly invoke tool.execute. No test imports/loads extensions/role-runtime.ts or drives Pi’s real extension/tool lifecycle. That leaves the highest-risk new logic at src/role-runtime.ts:115–135 unverified at the actual schema-validation, persisted-assistant-batch, parallel-sibling, and terminate boundary. The supplied live runs do not discharge the explicitly required committed integration test, and the two fresh review axes independently confirm this gap.

Repair: add the previously ordered package-entrypoint smoke/integration test through Pi’s actual extension loader/session/tool execution path—not another hand-written ExtensionAPI cast and not direct tool.execute calls. It must load extensions/role-runtime.ts from the packaged entrypoint, demonstrate injection of the complete bundled judge Soul, exercise provider-facing schema rejection, show that the singleton check observes Pi-persisted assistant batches including a sibling/parallel call case, and verify successful audited output terminates through the real lifecycle. Keep it deterministic and offline by using Pi-supported model/auth stubs or a narrow test injection seam; do not add production workflow/report mechanisms. Retain the existing focused unit tests where useful, but they cannot substitute for this boundary test. Re-run tests, typecheck, pack dry-run, clean-tree, and strict-forward-ancestry checks.

Disposition/history: all prior P1 records and the fixer-Soul dependency are closed in implementation and current unit coverage; no runtime regression is reported. This remaining item is an original-defect test obligation, not a fix-fix or invention. Surface audit: c7eaeda..c206022 is +436/-123 and exceeds a small first-round surface, but the diff is dominated by evidenced original defects and their adversarial tests; continue only for the bounded missing integration test, adding no new mechanism beyond the minimum test seam required.
