# Issue #3 fresh-review fix packet

Two supported defects remain at HEAD 0546c8b.

1. Harden the JSONL acceptance oracle in `test/judge-posture-recordings.test.ts`. `extractAcceptedJudgeOutputs()` currently accepts an orphan `tool_execution_end`/tool-result row solely from its claimed tool name, ID, success marker, and details; `Map.set()` also lets a later conflicting row with the same ID replace the earlier payload while still yielding the required count of one. Bind an accepted terminal result to the corresponding completed assistant `ak_judge_output` tool call and `tool_execution_start` (same non-empty ID/name and matching arguments), require the accepted details to match that issued call, and require all duplicate terminal representations for one ID to agree rather than overwrite. Add red probes for standalone accepted end/result rows and conflicting same-ID terminal rows; retain both real bundles as green.

2. Complete the CLAUDE Soul audit without deleting the new professional judgment kernel. Preserve material-relative Authority/Plan/Apply/Review burdens, the five Plan facts, complete-first/late-finding/authority-freeze chronology, strict production-evidence review, and Review finding adjudication: these are generic judgment methods, not caller routing. Trim only non-Soul carriers from `souls/judge.md`: replace the flags/call-history/topology catalog with the shorter materials-only principle; compress Authority/Apply fixture, byte/time, `Pi/包`, and commit/diff examples to generic executable-proof/production-seam principles; retain the local-mechanics-vs-Plan-blocker judgment but remove the `note` field instruction; remove verdict field catalogs/combinations and non-zero Action/error-handling mechanics while preserving material-relative convergence, actionable-repair, and owner-escalation judgment. Put any needed `judgeStatus`/`note` transport semantics in the registered tool schema/README, their proper owners, with no new status or routing behavior. Update `test/judge-soul.test.ts` so it protects those irreducible principles instead of requiring flags/topology, `note`, verdict-schema tokens, or infrastructure mechanics; deduplicate the repeated README overview/contract wording. Because the recordings pin exact Soul bytes, re-record both opaque bundles after the final Soul change rather than hand-editing them.

## Judge note

Reproduction against the current extractor produced `standaloneCount: 1` for a lone forged accepted end and `overwriteCount: 1` with the later forged `converged` details replacing earlier same-ID `continue` details. The checked-in recordings themselves contain coherent assistant-call → start → end → message_end-result sequences, so this is an oracle trust defect, not evidence that those two sessions are forged. Reviewer Standards findings are otherwise narrowed as follows: the complete-first/freeze chronology and Review boundary are retained professional judgment, not process pollution; Repeated Switches and Duplicated Code identify no observable or scope defect in this test-local heterogeneous JSONL parser; and updating both generated recordings when exact Soul bytes change is required provenance, not Shotgun Surgery. Independent verification on the exact clean HEAD passed `git diff --check`, all 249 tests, and `npm run typecheck`.
