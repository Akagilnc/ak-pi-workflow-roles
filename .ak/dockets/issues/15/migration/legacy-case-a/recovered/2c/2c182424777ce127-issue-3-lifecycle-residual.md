# Issue #3 ordered lifecycle oracle residual

Smallest residual: make `extractAcceptedJudgeOutputs()` validate an ordered, unambiguous per-ID lifecycle rather than three global last-write maps. At `test/judge-posture-recordings.test.ts:264-265,312,326,366-373`, row order is discarded and duplicate issued calls/starts overwrite. Mechanical probes against the current function returned 1 accepted output for all four malformed streams: terminal→call→start, start→call→terminal, conflicting duplicate assistant calls with one ID, and conflicting duplicate starts with one ID. Require exactly one assistant issuance and one matching start in call < start < terminal order, rejecting conflicting/replayed same-ID lifecycle evidence; add these four negatives while retaining the ordered real-bundle and two-distinct-ID greens. This is oracle-only: the checked-in sessions are themselves coherent and ordered, so no Soul change or bundle re-record is needed.
