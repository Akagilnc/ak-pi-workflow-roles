# Issue #3 Fixer receipt

```json
{
  "status": "completed",
  "report": "# Fixer apply — Issue #3 residuals on 62596ba\n\n**Status:** completed  \n**Base HEAD:** `62596ba`  \n**New HEAD:** `5933d20` (strict forward descendant; clean tree; no amend)\n\n## Residuals landed\n\n### Residual 3 — Judgment-only Soul principle\n- Replaced `souls/judge.md` Plan fixture-detail catalog with a short principle: after the five facts are set, Apply-decidable local mechanics/implementation details are not plan blockers; carry via optional `note`.\n- `test/judge-soul.test.ts`: presence no longer requires `fake|夹具|helper|字节`; pollution denylist extended (EN + 中文 catalog residue).\n\n### Residual 2 — Non-forgeable acceptance parser + negative probes\n- `extractAcceptedJudgeOutputs` now requires `isError === false` (missing rejected), non-empty `toolCallId`, merges same id across stream/`message_end`, and treats distinct ids as distinct even with equal details.\n- Negative probes: missing `isError`; two distinct accepted ids; retained receipt-only forge probe; merge-same-id control.\n\n### Residual 1 — Opaque neutral recordings + JSONL-bound input trust\n- Offline validator extracts user prompt from JSONL `message_end` and materials-read path/body from successful `read` bound to that path.\n- Neutrality denylist runs on JSONL-derived surfaces; denies `/r-block/`, `/r-ready/`, `expected.json`, readiness tokens, coaching keys.\n- Static `input/prompt.md` must prefix/equal instruction body; `input/materials.md` must byte-equal materials-read content.\n- Fixture README documents detached opaque export + opaque case workspace re-record procedure.\n- Both bundles re-recorded for real packaged Judge runs (`--ak-role judge`, `--mode json`) from package export without `test/fixtures/judge-postures` and opaque temp materials paths:\n  - `r-block` → `continue` (missing Red/Green oracles)\n  - `r-ready` → `converged`\n- `meta.soulDigest` pinned to current soul (`65266ef6…`); receipts derived from sole accepted JSONL verdict.\n\n## Verification\n- `git diff --check` clean\n- `npm run typecheck` pass\n- `node --import tsx --test test/judge-soul.test.ts test/judge-posture-recordings.test.ts` — 18/18\n- `npm test` — 249/249 pass\n- Scope only: `souls/judge.md`, judge tests, posture fixtures/README. No `src/**`, `schemas/**`, `extensions/**`, package/lock, other Souls.\n\n## Non-adopted\nNone. All three residuals landed; no AC relaxation.",
  "commitSha": "5933d20385e5c408f8414d806afa4e3a37424998"
}
```
