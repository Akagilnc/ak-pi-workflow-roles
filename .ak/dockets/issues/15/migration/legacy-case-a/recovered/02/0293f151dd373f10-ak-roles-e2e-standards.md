## Standards

- **P1 — Judge target binding is absent.** `src/role-runtime.ts:11-31` omits optional `targetHead` and rejects additional properties; `src/role-runtime.ts:299-308` proceeds directly to Soul audit without checking `AK_JUDGE_TARGET_HEAD`. This violates `docs/adr/0004-targethead-binding-check.md:6` and owner authority §14. **Consequence:** callers cannot bind a verdict to the reviewed commit, allowing stale-head acceptance. **Repair:** add optional `targetHead` to the receipt and fail closed on exact environment-bound equality before auditing.

- **P1 — Judge role gating is not implemented.** Judge activation at `src/role-runtime.ts:222-240` only loads the Soul, and registration at `src/role-runtime.ts:285-320` never calls `setActiveTools`. This violates `docs/adr/0008-role-gating-judge-toolset-narrowing.md:6` and owner authority §15. **Consequence:** judge sessions retain write/edit tools despite the documented mechanical restriction. **Repair:** on judge activation, narrow active tools to the documented evidence-gathering allowlist and add a real-entrypoint integration assertion.

- **P1 — Bundled Judge Soul remains the prohibited business-specific legacy fork.** `souls/judge.md:14-22,27-38,43-58` references container-global law, issue/ADR numbers, `stationReceiptContracts`, families, blocked tickets, and ledgers. `docs/adr/0005-soul-layering-generic-law-plus-host-overlay.md:6` explicitly requires removing these into host overlays; owner authority §13 ratifies that layering. **Consequence:** the independently installable package imposes unavailable Ming-specific institutions and cannot reliably adjudicate ordinary Markdown review material. **Repair:** rewrite the bundled Soul to retain only generic adjudication law; leave business rules to Pi’s native host overlay.

- **P2 — The canonical glossary contradicts the ratified Fixer contract.** `CONTEXT.md:10` says Fixer has no submission tool and its receipt is only a commit, while `README.md:52-58` and owner authority §§7-12 establish `ak_fixer_output`. **Consequence:** maintainers and integrations receive conflicting architectural guidance. **Repair:** update the Fixer glossary entry to define the typed report receipt and distinguish advisory Git evidence.
