Repair the remaining Reviewer completion bypass. On current `d979a87`, a real-Pi probe with sibling `Agent` calls `valid-leg` and schema-invalid `invalid-leg` persisted `invalid-leg` as `isError:true`, but only `valid-leg` entered `agentAttempts`; the captured batch contained both IDs, and a passing audit still allowed `ak_reviewer_output` to accept `completed`. At the Reviewer runtime seam that owns Agent outcomes, record or otherwise mechanically reconcile every persisted batch Agent ID—including Pi validation failures—with exactly one attempt, and prohibit `completed` unless every such attempt is successful; the compliance auditor must not be able to waive this invariant. Add a real-Pi regression for the mixed valid/schema-invalid sibling case that proves no completed receipt is accepted, plus the missing duplicate/conflicting provenance boundary tests claimed by the receipt (zero child start, explicit failed diagnostic, and no acceptance). Re-run the full verification and commit a forward fix.

## Judge note
The approved repair is otherwise directly verified: `d979a87` is one clean forward commit from `98f36c0`; all 68 tests, typecheck, diff-check, and 13-file pack dry-run pass; batch serialization/grouping, independent consumer lifecycle, bounded peak concurrency, class-private provider delegation, and the seven fatal rows are present. The pre-receipt `/tmp/ak-reviewer-fresh-review.jsonl` is quarantined and is not accepted as correctly ordered fresh-review evidence. Its malformed-sibling concern was independently reproduced above. Its lazy-snapshot concern is rejected for this repair: that code predates `d979a87`, the contract requires one fixed snapshot shared by the session’s legs rather than activation-time capture, and no head/ref-drift failure was mechanically demonstrated; no new pinning mechanism is authorized. The ordering violation creates no code rollback, but it does require a wholly new independent canonical review after the corrected head’s receipt has been returned and adjudicated as otherwise sound; do not run that replacement review before the next receipt or reuse the current artifact. Other claims in the preemptive report are not imported as fresh-review evidence.
