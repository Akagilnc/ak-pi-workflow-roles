# Collector repair packet 4

Close the remaining exact-oracle defects on a new forward commit. (1) In `test/collector-receipt.test.ts`, pass observe `{}` through `classifyCollectorBatch` and assert the legal decision; do not skip any row the matrix labels malformed (currently `raw === null` is skipped for batch classification). In `test/collector-role.test.ts`, replace/extend the sole blank-rationale observe+output sibling with the two mandated real-Pi sibling rows: unknown leg field and unavailable-without-scope. Each must prove fatal/nonzero termination and all six GitHub counters remain zero. (2) Restore the F3 provider-unused oracle for every ambient row. Skills, contextFiles, and appendSystemPrompt currently assert only the seam-specific fatal and GitHub-zero result; unlike required-tool/commands, they never assert `faux.state.callCount === 0`/pending response, and the skills test explicitly relaxes the obligation because Pi swallows `before_agent_start` throws. Add the provider assertions and repair the owning Collector lifecycle/failure seam so detection prevents model invocation rather than merely latching fatal before a provider turn. If current Pi APIs cannot honestly stop that turn, return concrete evidence for an owner decision instead of claiming completion or weakening the oracle.
