# Observe visibility Fixer receipt

```json
{
  "status": "completed",
  "report": "# Fixer apply report: Collector observe model-visibility\n\n## Status\n**completed** — one forward commit from `6fc54e1`.\n\n## Commit\n- **SHA**: `9cc03df314f3014e9cbfa57f3c010a9e1658b8ac`\n- **Parent**: `6fc54e117ebc5e43414015c6e02ef332e9f7d2af` (strict forward descendant)\n- **Title**: `fix(collector): project observe modelView into provider-visible content`\n\n## What was fixed\n\n### Production (presentation seam only)\nIn `src/collector-role.ts` observe `execute` return:\n\n- **Before**: `content` was a human summary string (`Observed snapshot …`); model-useful fields lived only in host-only `details`.\n- **After**:\n  - `content: [{ type: \"text\", text: JSON.stringify(modelView) }]`\n  - `details: modelView` (same object reference; no second projection)\n\nRequest refusal, filtering, bounds, ledger/receipt/schema/Soul untouched.\n\n### Regression coverage (`test/collector-role.test.ts`)\nContent-only helpers: `toolResultContentText` / `parseObserveModelViewFromContent` — parse decision data exclusively from toolResult content text.\n\n1. **Exact-head qualifying review (content-only valid path)**  \n   Model path reads `snapshotId`, review `evidenceId`, configured author, `COMMENTED` state, exact `commitOid` from content; submits `valid` with content-derived ID; accepted; `transport.calls.create === 0`. Also asserts one-projection identity: `JSON.parse(content)` deep-equals JSON-normalized `details`, and unrelated-author review is filtered out.\n\n2. **Authenticated same-head request-marker → wait → missing**  \n   Seeds issue comment with exact `buildCollectorRequestMarker` + real manifest digest/leg/HEAD under authenticated login. Content exposes marker body/author/evidenceId + snapshotId/HEAD. Next op is `wait` (not `request`); clock past cutoff → final observe → `missing`. Asserts zero comment creation and zero request tool results.\n\n## Gates\n| Gate | Result |\n| --- | --- |\n| `npm run typecheck` | pass |\n| `HOME=$(mktemp -d) npm test` | pass (201/201) |\n| `npm pack --dry-run` | pass (26 files; collector modules present) |\n| `git diff --check` | pass |\n| Worktree | clean after commit |\n\n## Refused\nNothing refused from the authorized packet.\n\n## Out of scope (preserved)\n- Ledger/receipt/schema/Soul/tool definitions unchanged\n- Request refusal laws unchanged (visibility is the fix, not softening refusal)\n- Existing details-based host/receipt tests left as-is",
  "commitSha": "9cc03df314f3014e9cbfa57f3c010a9e1658b8ac"
}
```
