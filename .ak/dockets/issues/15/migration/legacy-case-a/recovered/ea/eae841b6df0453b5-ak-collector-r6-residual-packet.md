# Collector R6 residual packet

Close the R6 acceptance-criteria relaxation in `src/collector-github.ts:188-193`. `optionalUserLogin` currently maps not only the authorized literal `user: null`, but also an absent `user`, a non-object value, and any object whose `login` is not a string to a tombstoned author. An independent current-HEAD probe confirmed all of `{user: absent}`, `{user: {id: 7}}`, and `{user: {login: 5}}` are accepted across the three surface normalizers; `9cc03df` rejected these as `GitHub payload missing user.login`. Preserve only literal `null` as the tombstone case and retain the prior fail-closed validation for every non-null value. Add regressions for review, issue-comment, and review-comment payloads proving literal `user: null` remains preserved/nonqualifying while missing or malformed non-null users reject. Make no R10, Soul, schema, or unrelated mechanism changes.
