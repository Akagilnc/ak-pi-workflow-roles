Close Cluster G’s evidence-trust contract before construction: keep prompts/materials neutral (no expected status or blocker-class answer in model-visible input); store expected blocker metadata only outside model input; and make offline CI parse the raw packaged-run JSONL to derive the sole successful `ak_judge_output`, exact verdict details, `isError:false`, and `Judge verdict accepted`, cross-checking `receipt.json` instead of trusting a self-asserted audit marker. Retain real-model runs through the packaged loader, revised bundled Soul digest pinning, real audit, and paid-model-free CI. This explicitly supersedes the earlier attempted approval: deleting its Apply note did not resolve this behavioral-oracle trust gap.
