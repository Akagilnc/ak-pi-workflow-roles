# Collector wait cadence repair packet

Authority ruling: no authority addendum is required. v2 §6 already assigns wait capping to `ak_collector_wait` runtime, v2 §9 owns boundary cadence, and v2 §13 expressly keeps timer mechanics out of Soul; v3 does not alter that ownership. Sustain the defect: on head `a817975`, `/tmp/ak-collector-dogfood-pr4.jsonl` records a valid `900000` ms request truncated only to the remaining `762387.586541` ms, preventing any re-observation for about 12m42s, while PR5’s 60s/180s choices demonstrate model-dependent variance. Existing tests cover only deadline capping, so their green result does not dispose of this gap.

Smallest Fixer packet:
1. In `src/collector-ledger.ts`, add one private five-minute single-wait constant (`300_000`) at the owning wait seam and change only the effective-duration calculation to `Math.min(input.durationMs, remaining, 300_000)`. Preserve the public tool schema maximum (`900_000`), cutoff law, ledger fields, abort behavior, and all other mechanics.
2. In `src/collector-role.ts`, make the existing method/tool wait wording truthful about the five-minute runtime cap plus the remaining-eligibility cap. Do not change kickoff, schema, README contract, or any other method law.
3. In `test/collector-ledger.test.ts`, first add red behavior tests through the real `createCollectorLedger().wait()` seam (do not mock or duplicate the cap): (a) with 15 minutes remaining, requesting `900_000` records `requestedMs: 900_000`, returns/records exact `effectiveMs: 300_000`, leaves `600_000`, and does not mark cutoff; also prove a shorter request is unchanged; (b) reproduce the PR4 shape with about `762_388` ms remaining, request `900_000`, assert exact `300_000`, then call the real ledger `observe` with the fake GitHub transport and prove that complete observation occurs before `deadlineMono`. Keep the existing near-cutoff assertion proving the remaining deadline still wins when it is below five minutes, and retain post-cutoff rejection coverage.
4. Add no scheduler, watcher, polling loop, extra timer, Runner behavior, public schema change, or Soul/README clause. The cap narrows the existing runtime-owned seam; there is no parallel mechanism to add or root mechanism to delete.
5. Make a forward commit and run on the new head: `npm run typecheck`; `HOME=$(mktemp -d) npm test`; `npm pack --dry-run`; `git diff --check`.
