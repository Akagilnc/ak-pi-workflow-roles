# Collector Coder receipt

```json
{
  "status": "completed",
  "report": "# Collector v1 apply receipt\n\n## Summary\nImplemented Judge-approved Collector v1 on `feature/collector-role` as an additive standalone role. Plan A controlled behavior/TDD; retained B’s v3 supersession table, tagged-provenance wording, pure-helper factoring, and installed-package profile. Rejected B’s sequential mode, model-supplied reports, ambiguous HTTPS transport, and mandatory deep file topology.\n\n**Commit:** `ce5d2e48477c238b3c4fe3f7b5f11dda723384d4` (forward only; no amend/rewrite/push)\n\n## Authority binding\n- v2 digest `05bbfd7718d804c67a2bf9f379ce6027cb0e18f14e29f419f6a9156d5041961d`\n- v3 digest `04f736d8977a1bb15865e7c8d306c326f1edd0e845dbd433513d698dd186fd43`\n- Baseline head was `c5f75b63415bf24b8a2318ef8744a60d255eb135`\n\n## Knife edge / modules\n| Module | Responsibility |\n|---|---|\n| `src/collector-config.ts` | repo grammar (addendum D), PR int, UTF-8 + nested/escaped duplicate-key lexical scan, exact integer version `1`, author normalize/overlap, 60_000 UTF-8 body bound, canonical digest |\n| `schemas/collector-legs-v1.schema.json` | machine-readable manifest schema |\n| `src/collector-github.ts` | fixed `gh api --hostname github.com` argv transport; pagination; ambiguous POST loss |\n| `src/collector-evidence.ts` | immutable versions, `before\\|within\\|after\\|uncertain`, 8/32 MiB constants, qualifying review helper |\n| `src/collector-ledger.ts` | activation/cutoff, atomic observe commit, request marker/at-most-once/recovery, wait cap, batch cardinality latch |\n| `src/collector-receipt.ts` | narrow model candidate; runtime-built tagged reports; self-contained `snapshots[]`+`evidenceRecords[]` subset materialization |\n| `src/collector-role.ts` | Pi lifecycle: startup latch, fixed kickoff + image strip, later-input handled abort, `message_end` batch gate, four tools only |\n| `souls/collector.md` | six irreducible principles only |\n| Additive wiring | `src/role-runtime.ts`, `extensions/role-runtime.ts`, README/CONTEXT/package `files` (+`schemas`) |\n\nNo orchestrator files, no `souls/collect.md`, no Skill, no existing-role contract behavior change beyond additive flag/help/tarball inventory.\n\n## Synthesis corrections implemented\n1. **Batch gate** at finalized assistant `message_end` before schema/execute; ledger latches fatal; `tool_call`/execute cross-check blocks; tools remain parallel-preflight (no sequential mode).\n2. **Receipt subset** embeds refs + configured-author substantive history + request/transport/final-snapshot identity material; unrelated non-author observations not copied merely because observed.\n3. **Explicit counterexamples** covered: nested/escaped duplicate keys; 60_000/60_001 UTF-8 body; 8 MiB snapshot overflow; first-input image removal; startup no-prompt nonzero shutdown; batch permutations (multi-op, mixed op+output, dual output); irrelevant receipt omission.\n\n## Guard-audit seams\n- Startup/lifecycle: activation failures latch + `input handled` + nonzero exit; no reliance on swallowed throws alone.\n- Duplicate-key scan owns lexical identity before `JSON.parse`.\n- Detectable ambient skills/context/appendSystemPrompt/commands fail closed where Pi exposes them.\n- Request marker recovery for ambiguous POST loss; process-local once per `(repo,PR,HEAD,leg)`.\n- 8/32 MiB loud failure, no truncation.\n\n## TDD evidence (focused red→green)\n1. Config/schema tests first (`test/collector-config.test.ts`) → `collector-config.ts` green.\n2. Ledger/cutoff/request/batch (`test/collector-ledger.test.ts`) → ledger green.\n3. Receipt/state-time/provenance (`test/collector-receipt.test.ts`) → receipt green (materialization bounds fixed after red on unrelated-record omission + snapshot ref).\n4. GitHub argv/pagination (`test/collector-github.test.ts`) → transport green.\n5. Real Pi lifecycle (`test/collector-role.test.ts`): mode fail-closed, input replace/image strip/later input, all-valid print/json path, parallel sibling fatal, no-prompt shutdown, bad manifest pre-side-effect.\n6. Package lifecycle/pack inventory (`test/collector-package-lifecycle.test.ts` + updated reviewer/help inventories).\n7. Soul thinness (`test/collector-soul.test.ts`).\n\n## Same-pattern check\n- Follows existing role pattern: flag registration → session_start activate → registerTool + lifecycle hooks → setActiveTools → terminating `ak_*_output`.\n- Purpose-built ledger (not layered on Reviewer ledger/Agent/bash).\n- Tests use packaged harness (`withHermeticHome`/`withInProcessPi`/`runPiSubprocess`) + hermetic fake GitHub transport; mode extended with `\"json\"`.\n\n## Introduced-regression check\nFull suite after apply: **147/147 pass**, including all pre-existing Judge/Fixer/Coder/Reviewer tests. Help/tarball inventories updated only additively for Collector.\n\n## Behavior facts\n- Immediate all-valid path: observe → output receipt with `targetHead`, runtime non-finding report for blank COMMENTED review, embedded snapshots/evidenceRecords.\n- Parallel observe+wait batch: fatal latch, zero GitHub pulls, nonzero exit.\n- Unsupported TUI mode / bad manifest: zero provider/GitHub side effects, exitCode 1.\n- First input fully replaced with fixed kickoff; caller images stripped; second input handled without consuming provider responses.\n- Startup with no prompt: tools active, shutdown sets exitCode 1 without accepted output.\n\n## Unified gate (post-commit)\n```text\nnpm run typecheck                          # pass\nHOME=$(mktemp -d) npm test                 # 147/147 pass\nnpm pack --dry-run                         # 25 files; schemas+collector modules+soul present; no SKILL.md\ngit diff --check                           # pass\n```\n\n## Out of scope preserved\nNo orchestrator adapter/wiring, no enterprise hosts, no global request journal, no `refused` status, no unrestricted tools, no copied orchestrator collect Soul.",
  "commitSha": "ce5d2e48477c238b3c4fe3f7b5f11dda723384d4"
}
```
