Do not authorize apply yet. Correct three remaining plan defects:

1. **Complete the topology disposition.** `docs/adr/0009-minimal-engineering-face-git-distribution.md:6` contains explicit future `phase-2 编排器` integration text but is absent from the claimed repository-wide inventory. Classify it explicitly (likely KEEP as future integration/version-coupling context, not worker provenance) and include it in the residual audit.

2. **Make the Fixer Soul plan genuinely judgment-only.** The “Retain in Soul” list still preserves the exact phase/status mappings (`planned|completed|refused`), `report`/Markdown field semantics, and `commitSha` semantics, while the green shape still proposes a role-output-tool handoff line. Those are schema/runtime/tool-metadata or transport concerns and contradict both the plan’s own removal table and `CLAUDE.md`. Remove status names, field names, tool/API instructions, and caller-disposition transport from the proposed Soul. Retain only professional repair judgment: phase boundaries, investigation, refusal criteria, evidence quality, test discipline, blast-radius control, and forward-history discipline. An evidence-sufficiency principle may remain without naming receipt fields.

3. **Disposition the unanchored negative Codex observation.** Root 3 still proposes stating that no `PullRequestReview` and no Codex check run existed for the c73 run, but supplies no captured fixture or durable object anchor for that absence. Live API queries currently corroborate it, but those mutable listings are not durable evidence. Because fixture capture is out of scope, remove this negative factual claim rather than retain it as an observation; otherwise explicitly bring a captured fixture into authorized scope.
