1. Make the test suite hermetic. Remove every test-time dependency on user-global Skills, including `test/reviewer-package-lifecycle.test.ts:59-62`, `test/audit-failure-subprocess.test.ts:81-82`, and the independently found pre-existing `test/package-entrypoint.integration.test.ts:352-353`. Create minimal valid Skills under test-owned temporary homes, point native `--skill`/loader inputs there, and ensure the runtime’s canonical homedir lookup sees the same temporary home. Do not add a production fallback, bundle the canonical methods, or provision private host state in CI.
2. Preserve opaque review-task bytes. In `src/role-runtime.ts`, load the raw task, use `raw.trim()` only to reject blank input, then retain and pass the unmodified raw string to both `<review_task>` and `ReviewerAuditInput.task`. Add a regression with four-space leading indentation and a trailing newline that asserts exact system-prompt and audit-input preservation.
3. Remove the substantiated compliance-auditor clone. Move the common pass/revise decision-tool shape and audit dispatch/completion/options/parsing scaffold into the existing `src/compliance-transport.ts` seam; keep only role-specific tool names, prompts, serialized inputs, labels, and public auditor factories at the two call sites. Preserve exact authentication/provider failure behavior and decision validation; do not introduce another parallel transport.
4. Verify on the repaired HEAD with the exact CI commands and a genuinely empty home: `npm run typecheck`, `HOME=$(mktemp -d) npm test`, and `npm pack --dry-run`; all tests must pass without any preinstalled `~/.agents/skills/*`.

## Dispositions
Disposition at clean HEAD `3e789579181e07785e4eebea4eb11de689f0bcf2`:
- Standards #1 SUSTAINED. Both cited focused tests fail under an empty `HOME` on missing `code-review/SKILL.md`; `.github/workflows/ci.yml` only runs checkout, setup-node, `npm ci`, typecheck, and `npm test`. The full empty-home suite reproduced 3 failures (67/70 passed), revealing a third global dependency on `tdd/SKILL.md`; the ordinary 70/70 pass is host-contaminated because this machine has the external Skills.
- Standards #2 SUSTAINED as a judgement-call smell, not a hard violation. A direct diff of the 116-line Soul auditor and 117-line Reviewer auditor shows nearly the entire control flow is cloned, with differences largely limited to role labels, prompts, and payload construction. The existing transport extraction owns auth and parsing but not the repeated invariant scaffold, matching the canonical Duplicated Code definition.
- Spec P2 SUSTAINED. `src/role-runtime.ts:696` assigns `.trim()` output, then injects that altered value at lines 1208 and 825. A byte probe confirms four leading spaces before `#` are removed, changing an indented Markdown code block into a heading/instruction despite README’s explicit opaque-input contract.
- Independent current-head checks: ordinary `npm test` passed 70/70, typecheck passed, pack dry-run passed, and the worktree remained clean. Those checks do not cure the empty-home CI failure or the semantic mutation.
