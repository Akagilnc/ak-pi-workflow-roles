# Cancellation precedence receipt

```json
{
  "status": "completed",
  "report": "# Fixer apply: signal-before-tag cancel order\n\n## Status\n**completed** at `c73bf31a3d22815b26b9a33a5d28fd1f242f5701` (forward from `f2c0614`)\n\n## Root cause\n`createIssueComment` catch in `src/collector-github.ts` checked `ambiguousGhFailure` **before** `input.signal.aborted`. When a hung POST was aborted with a tagged reason (`Error(\"deadline exceeded\")` + `ambiguousGhFailure: true`), the catch returned `{ kind: \"ambiguous_loss\" }` instead of rethrowing the exact abort reason.\n\n## Repair (sole ordering change)\nFour separate branches, exact green-oracle order:\n\n1. `if (input.signal?.aborted) throw error;` — authoritative cancel, exact identity\n2. `if (ambiguousGhFailure === true) return ambiguous_loss;` — non-aborted transport tag only\n3. `if (name === \"AbortError\") throw error;` — non-signal belt after tag\n4. `return rejected`\n\nDid **not** merge `signal.aborted || name === \"AbortError\"` ahead of the tag (that would reclassify non-aborted AbortError+tag as cancel).\n\n## Tests\n- **A (primary):** hung POST aborted with tagged reason — `Object.is` identity, child dead, `attempts[0].status === \"started\"`, one-shot retained\n- **B (control):** non-aborted mock reject with `name: \"AbortError\"` + `ambiguousGhFailure: true` → `ambiguous_loss`\n- Helper tightened for all hung-POST cases: exact reason identity + explicit `started` status\n- Existing plain abort reasons retained (`\"request canceled\"`, `\"deadline exceeded\"`, `\"stop now\"`)\n\n## Verification\n| Gate | Result |\n| --- | --- |\n| `node --import tsx --test test/collector-github.test.ts` | 19/19 pass |\n| `npm run typecheck` | clean |\n| Working tree | only `src/collector-github.ts` + `test/collector-github.test.ts` |\n| Git | new forward commit, no amend; `FORWARD_OK` vs start HEAD |\n\n## Scope held\nIn: catch order + red/green tests only.  \nOut: ledger, runner spawn/kill, other transports, README, AC relaxation, reason-text matching, parallel cancel mechanisms.\n\n## Not refused\nNo authority conflict; residual was real at `f2c0614` (tag-before-signal).",
  "commitSha": "c73bf31a3d22815b26b9a33a5d28fd1f242f5701"
}
```
