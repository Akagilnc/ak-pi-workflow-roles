# Collector R10 Fixer receipt

```json
{
  "status": "completed",
  "report": "# R10 residual apply — observation-scoped normalized snapshot budget\n\n**status:** completed  \n**base HEAD:** `c86c2a2`  \n**commitSha:** `d38dc1dbc0599f17295bb0847ad56b0386b90531`  \n**forward of base:** yes (`c86c2a2` ⊂ HEAD)\n\n## Root cause closed\n\n`paginate()` charged **per-call raw `bodyText` `retainedBytes`**, resetting on every surface. Production-shaped multi-surface observe (≈2.2 MiB body × reviews / issue-comments / review-comments) stayed under each local raw cap, fetched all three lists + terminal PR, then failed only at ledger commit (`measureNormalizedBytes` → 13 202 455 > 8 MiB).\n\nSame failed method family as c86c2a2 R10 raw/per-surface caps — **not revived**.\n\n## Repair\n\n| Owner | Change |\n| --- | --- |\n| `src/collector-evidence.ts` | `createSnapshotByteBudget()` — incremental retain over cumulative `measureNormalizedBytes(records)` |\n| `src/collector-ledger.ts` | One budget per `fetchObserveSurfaces` attempt (resets on PR-identity retry); charge user + PR + each page’s normalized rows before further fetch |\n| `src/collector-github.ts` | Removed raw `retainedBytes` + `COLLECTOR_SNAPSHOT_MAX_BYTES` import; optional `retainPage` on list*/`paginate`, invoked **before** aggregate append / next Link page |\n| `test/helpers/fake-github-transport.ts` | Invokes `retainPage` per page so ledger path is testable without `gh` |\n\nPreserved: Link pagination; exact final `applyEvidenceVersionHistory` → `assignWindowRelations` → `measureNormalizedBytes` gate before store/commit; `COLLECTOR_SNAPSHOT_MAX_BYTES = 8 MiB`; R1–R9/R11 behavior.\n\n## Tests\n\n1. **Cross-surface real path** (`createGhCollectorGitHubTransport` + `ledger.observe`): ~2.2 MiB bodies; each surface alone under 8 MiB; cumulative over; observe rejects `/snapshot exceeded|UTF-8 bytes|8/i`; **review-comments = 0**, **terminal PR = 0** (pullCalls=1 initial only); fatal; no `latestCompleteSnapshotId`.\n2. **Intra-surface multi-page** (github retain-hook + fake ledger path): stops before the page that would cross the shared normalized budget; later pages/surfaces/terminal PR do not run.\n3. **Regression:** existing MAX / MAX+1 boundary and non-size observe paths green.\n\n## Verification\n\n- `npm run typecheck` — pass  \n- `node --import tsx --test test/collector-ledger.test.ts test/collector-github.test.ts` — 38/38 pass  \n- `npm test` — 215/215 pass  \n\n## Refused\n\nNone. Full packet adopted; no AC relaxation, Soul/schema/README churn, or history rewrite.",
  "commitSha": "d38dc1dbc0599f17295bb0847ad56b0386b90531"
}
```
