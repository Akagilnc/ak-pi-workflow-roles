Remaining blockers:

1. §2.6 does not prove the required recovery ownership case. Its latestRelevant fixture uses two `succeeded` attempts, so the older attempt has no `recoverySnapshotId`; asserting that an absent value was not linked is vacuous. Make the older same-leg attempt actually `recovered` (with comment, request snapshot, and recovery snapshot all defined), add a later eligible same-leg attempt, and assert all three older IDs are absent from both leg and terminal-report refs.

2. The timestamp-less text row is not executable as written: it leaves the surface as “issue or review comment” and uses null/missing `created_at`/`updated_at`, while the current transport normalizers require those fields and would fail before evidence history. Select one exact fixture and its owning seam—for example a stable review whose body changes while `submitted_at` is reused, so the later distinct review version is forced to `authoritativeTime = null`—and remove the `or`/null-vs-missing alternatives. Likewise choose one exact state fixture rather than “reused or cleared.”

3. §3.2 still does not specify exact collision facades. In particular duplicate snapshot uses “e.g. … or,” and duplicate evidence says only to “embed both.” State the precise overridden return values and guaranteed embed path for each case. Concrete paths are: duplicate an embedded final PR evidence row in `allEvidence()`; duplicate the final snapshot in `allSnapshots()`; and for cross-namespace collision return a forged snapshot whose ID equals an embedded PR evidence ID and make `getSnapshot(id)` resolve it. Each must reach the corresponding check inside `buildCollectorReceipt` first.

4. §3.3 retains placeholders and alternatives forbidden by the corrections: skills uses two alternative injection mechanisms, append prompt uses two alternatives, and ambient command uses `{ …minimal RegisteredCommand fields… }`. Choose one real `DefaultResourceLoader` option and exact nonempty fixture value for each surface, and spell out the complete `registerCommand` call. The direct Collector extension proposed for overflow must also explicitly register `session_start` and call `collector.activate(ctx, event)`; construction alone does not activate the runtime.

5. The exact-byte constructions do not guarantee reachable targets. In the 8 MiB fixture, `review.body` is serialized both as the normalized `body` and inside `raw`, so each ASCII pad byte changes the measurement by two; binary search cannot manufacture both adjacent sizes. In the one-leg missing/unavailable 32 MiB fixture, rationale is serialized in both `legs[].rationale` and terminal report `report`, creating the same problem. Specify an independent one-byte legal tuning field/parity fixture for 8 MiB, and use a reproducible receipt fixture with one-byte granularity (for example a one-leg `valid` output, whose rationale occurs once). Then define exact calibration/replay into the activated in-process runtime for both MAX and MAX+1; a refusal trigger is not a substitute for a construction that can reach the mandated boundaries.
