# Collector R6 residual receipt

```json
{
  "status": "completed",
  "report": "# R6 residual apply — literal-null tombstone only\n\n## Status\n`completed` on HEAD `6604a73` (strict forward descendant of start `d38dc1d`).\n\n## Defect\n`optionalUserLogin` (sole owner for `normalizeReview` / `normalizeIssueComment` / `normalizeReviewComment`) over-mapped to null after `c86c2a2`:\n\n- `raw === undefined`\n- non-record `user`\n- record without string `login`\n\nAll became `userLogin: null`. Authorized R6 behavior is tombstone **only** for literal JSON `user: null`; every other shape must fail closed with `GitHub payload missing user.login`.\n\n## Changes\n**In scope only**\n\n1. `src/collector-github.ts` — `optionalUserLogin`\n   - `if (raw === null) return null;`\n   - else require record + string `login`, else throw same message as `requireUserLogin`\n   - comment tightened to “literal JSON null only”\n2. `test/collector-github.test.ts` — sibling R6 regression\n   - keep existing green: `user: null` → `userLogin === null` + non-qualify / receipt rejection\n   - add reject matrix on all three surfaces: user absent, `{id: 7}`, `{login: 5}`, non-object → `/GitHub payload missing user\\.login/`\n\n**Out of scope (untouched):** R10 budget, Soul, schema, evidence qualification, receipt, ledger, unrelated tests.\n\n## TDD evidence\n1. **Red:** added reject regressions only → `R6 non-null user shapes fail closed...` failed with `Missing expected exception.`\n2. **Green:** tightened `optionalUserLogin` only.\n3. **Verify:**\n   - `npm run typecheck` — pass\n   - `npm test` — 216/216 pass\n\n## History / method family\nDid not re-ship `c86c2a2` map-to-null family. Restored pre-overbroad fail-closed for non-null while preserving the authorized literal-null tombstone.\n\n## Git\n- Start HEAD: `d38dc1d`\n- New commit: `6604a73` `fix(collector): tombstone userLogin only on literal user null (R6 residual)`\n- No amend; working tree clean; forward ancestor check OK.\n\n## Not refused\nAll packet items adopted; no owner decision required.",
  "commitSha": "6604a733886dfb5d074f558963e20e01e587aa6d"
}
```
