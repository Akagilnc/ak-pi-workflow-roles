Delete only the unused Fixer host-actions seam: remove `_hostActions: WorkerRoleHostActions` from `createFixerRoleRuntime` in `src/worker-role.ts`, remove the corresponding `hostActions` argument at the composition call in `src/role-runtime.ts`, and remove the now-extra host-actions fixture argument from the direct Fixer factory call in `test/judge-role.test.ts`. Retain `WorkerRoleHostActions` and all live Judge/Coder/Reviewer uses. Add no replacement abstraction, behavior test, documentation, or other cleanup. Verify the new HEAD with `npm run typecheck`, `npm test`, and `git diff --check`.
