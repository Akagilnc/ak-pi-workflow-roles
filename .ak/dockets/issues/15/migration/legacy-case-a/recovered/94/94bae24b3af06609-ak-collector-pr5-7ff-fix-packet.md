# PR5 7ff current-head repair packet

At exact HEAD 7ffbd3684fe41dc63c28cb7cb79696f2f5e0c3ed, repair only these three sustained current findings:

1. Monotonic first-sighting eligibility (`src/collector-ledger.ts` / `src/collector-evidence.ts`). The post-fetch first-sighting trust decision still compares wall `firstObservedAt` with wall `deadlineTime`, although the 15-minute cutoff is owned by `deadlineMono`. Reproduction: activation mono 0, deadline mono 900000, observe at mono 960000 after stepping wall back to 00:20 before wall deadline 00:25; the review retained submitted_at 00:05, became `before`, and a `valid` receipt was accepted. Decide whether a newly seen review version may retain submitted_at from the monotonic surface-completion/first-sighting point; keep wall time only as receipt metadata. Preserve known-version authoritative null reuse and conservative start-before/finish-after behavior. Add rollback coverage proving a first sighting after monotonic cutoff is uncertain and cannot prove valid or unavailable, plus exact monotonic-boundary coverage.

2. Child stdin failures (`src/collector-github.ts:createGhApiRunner`). `child.stdin` has no error handler. A real `gh` stub that exits before reading a 32 MiB POST body reproducibly terminates Node with an unhandled `write EPIPE` instead of settling the runner promise. Attach stdin error handling before write/end and route it through the existing single-settlement/cancellation path; preserve signal identity and POST ambiguity/rejection semantics. Add a real-child or equivalently deterministic regression that proves early exit/EPIPE rejects without an uncaught process error or double settlement.

3. Published author schema parity (`src/collector-config.ts:COLLECTOR_LEGS_SCHEMA` and `schemas/collector-legs-v1.schema.json`). Both schemas accept `expectedAuthors:["   "]` while `loadCollectorManifest` rejects it as non-blank. Add the same non-whitespace constraint to both schema owners and retain their deep equality. Add behavioral schema validation for whitespace-only rejection (not merely schema-file equality), while preserving accepted trim-normalization for nonblank authors.

These are mechanical runtime/schema fixes; do not add Collector Soul text or parallel classifiers.
