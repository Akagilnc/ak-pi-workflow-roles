# Collector R10 residual repair packet

R10 is still incomplete at `src/collector-github.ts`/`src/collector-ledger.ts`. `paginate()` resets `retainedBytes` for each of the three surfaces and charges raw response bodies, while the owning 8 MiB invariant measures the cumulative normalized snapshot. A direct production-transport/ledger probe used one 2,200,000-byte body on each reviews, issue-comments, and review-comments surface: every page stayed below the local cap, all three surfaces plus the terminal PR read were fetched, and rejection occurred only afterward at `Collector snapshot exceeded 8388608 UTF-8 bytes (13202455)`. Replace the per-surface approximation with one observation-scoped incremental budget over the same normalized/materialized record shape, enforced before appending retained entries across all paginated surfaces; retain pagination and the exact final ledger measurement. Add a real-path cross-surface test where each surface is individually below 8 MiB but cumulative normalized materialization exceeds it, asserting later surface/terminal fetches stop and no snapshot is committed.
