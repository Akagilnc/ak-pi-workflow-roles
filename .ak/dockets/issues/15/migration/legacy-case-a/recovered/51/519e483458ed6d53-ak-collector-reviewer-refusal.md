# Reviewer refusal receipt

Unable to produce a method-compliant receipt in this session. The immutable target and authority were established, and the first mandatory sibling-parallel Standards/Spec batch completed, but `ak_reviewer_output` rejected its aggregation because the Standards prompt omitted requested severity. Following that rejection, additional clarification/replacement Agent calls were made; the receipt validator now correctly observes that the session contains more than the task-authorized single parallel batch. That execution history cannot be undone or made compliant by declaring later legs superseding. A genuinely fresh Reviewer session is required to run exactly one parallel Standards/Spec batch with the full canonical smell baseline and severity requirements included up front, then aggregate only those two reports. No original-worktree mutation, repair, commit, or push was performed.
