# Collector Fixer receipt 5

```json
{
  "status": "completed",
  "report": "# Fixer apply receipt — Collector repair packet 5\n\n**Status:** `completed`  \n**Base HEAD:** `91a1a92`  \n**New HEAD:** `3bec03f` (strict forward descendant; single new commit; no amend)  \n**Working tree:** clean  \n\n## What was fixed\n\n### 1. F1 receipt exact-oracle residual (`test/collector-receipt.test.ts`)\n- Removed `if (raw === null) continue` so **null** joins every malformed output row through shared `collectorToolArgumentsValid` + `classifyCollectorBatch` and is denied.\n- Observe `{}` now asserts full legal classify: `allow: true`, permitted sole operational `ak_collector_observe`.\n- Invalid observe `undefined`/`null` deny rows retained.\n- **Production:** none (shared Check/classify already correct).\n\n### 2. F1 real-Pi observe+invalid-output siblings (`test/collector-role.test.ts`)\n- Replaced sole blank-rationale sibling with exactly two batches, each **valid observe + invalid output**:\n  - unknown leg field (`extra: true`)\n  - `unavailable` without `unavailableScope`\n- Each asserts `exitCode === 1` and all six GitHub counters zero via `assertZeroGitHub`.\n- Sole-output invalid matrix retained.\n\n### 3. Loaded-Skill startup fail-closed (item 3)\n- New real-Pi row `F3-loaded-skill-startup-fail-closed`:\n  - Writes a real `SKILL.md` with `disable-model-invocation: true`\n  - Loads via `noSkills: false` + `additionalSkillPaths` (DefaultResourceLoader normal discovery)\n  - Mechanically proves loader has the skill (`disableModelInvocation === true`) and `pi.getCommands()` exposes `{ name: \"skill:hostile-cmd-only\", source: \"skill\" }`\n  - Asserts: activation configuration failure text (`ambient instruction commands: … skill:hostile-cmd-only`), nonzero exit, no successful `ak_collector_output` receipt, all six GitHub counters zero, `faux.state.callCount === 0`, response remains pending\n- **Red against current activation seam:** already green. Name filter (`name.includes(\"skill\")`) catches `skill:hostile-cmd-only`.\n- **Production `command.source === \"skill\"`:** **not added** — gap not demonstrated; plan forbids production edit unless red demands it.\n\n### 4. Late hostile Skill-mutation sibling\n- Renamed/relabeled to `F3-ambient-skills unsupported hostile sibling-extension injection`.\n- Comments state this is unsupported hostile sibling-extension injection, not normal Skill discovery.\n- Asserts latched fatal/nonzero, no successful receipt, all six GitHub counters zero.\n- **Does not** assert provider callCount or pending responses (Pi 0.82.1 late seam non-normative).\n- Non-Skill ambient rows (contextFiles/append/commands) preserved unchanged.\n\n### 5. Categorical Skill forbid in README + packaged `--help`\n- **README Collector section:** categorical forbid of every Skill incl. command-only; supported profile (`--no-skills`, `--no-extensions` + only explicit package extension, no prompt templates/context files, one print/JSON prompt); narrow Pi 0.82.1 late-hostile-extension limitation without security-boundary or provider-zero claim.\n- **Flag descriptions** in `src/collector-role.ts` (`ak-collector-repo` / `pr` / `legs`) surface the same categorical forbid + profile + late-hostile note in `pi --help`.\n- **Lifecycle help test** matches actual help substrings (forbid Skills/command-only; profile flags/words; late-hostile; not a security boundary), not mere flag names.\n- Updated hard-coded help/description expectations in `test/judge-role.test.ts` and `test/package-entrypoint.integration.test.ts`.\n\n## Refused / not done\n- Nothing refused from the approved packet.\n- No Soul edits.\n- No provider gateway/proxy.\n- No upstream Pi dependency change.\n- No parallel Skill detector; no production `source === \"skill\"` (red did not require it).\n- No broadening of late-seam exception to non-Skill ambient rows.\n\n## Verification\n- `npm test` — **196 pass / 0 fail**\n- `npm run typecheck` — clean\n- Focused Collector role/receipt/package paths exercised inside the full suite\n\n## Commit\n`3bec03f` — `fix(collector): close F1 exact-oracle residual and categorical Skill ownership`\n\nFiles: `README.md`, `src/collector-role.ts` (help text only), `test/collector-receipt.test.ts`, `test/collector-role.test.ts`, `test/collector-package-lifecycle.test.ts`, `test/judge-role.test.ts`, `test/package-entrypoint.integration.test.ts`.  \nNo `souls/*` changes.",
  "commitSha": "3bec03f84b1bee6a5f7d7c7068028b3c2849116f"
}
```

Driver independent gate: typecheck, empty-HOME full suite 196/196, pack dry-run 26 files, diff-check, clean worktree.
