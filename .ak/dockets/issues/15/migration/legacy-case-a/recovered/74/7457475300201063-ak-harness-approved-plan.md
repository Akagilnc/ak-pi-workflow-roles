# Wave-4 consolidated implementation plan

No files or commits were changed. Current HEAD is baseline `802d28af17efa588aa12d9f8dc689e4a740ebc1d` on `feature/deepen-role-runtime`; baseline ancestry is valid and the worktree is clean.

## Duplication inventory and extraction boundary

- Extract the five repeated full in-process Pi bootstraps across `test/package-entrypoint.integration.test.ts`, `test/reviewer-role.test.ts`, and `test/reviewer-package-lifecycle.test.ts`: faux provider/model runtime/auth, settings, hermetic `DefaultResourceLoader`, reload/error access, in-memory session setup, flag binding, and disposal.
- Extract repeated temporary HOME/agent allocation, exact HOME restoration, and recursive `finally` cleanup used across full-Pi/process tests.
- Extract the three repeated raw subprocess collectors in `test/audit-failure-subprocess.test.ts`; reuse that adapter for the two package-help calls while preserving their distinct success assertions.
- Fold the existing deterministic canonical Skill writer, already consumed by three modules, into the deep harness without changing generated bytes or returned evidence.
- Share raw package-root/CLI/manifest loading and mechanical path resolution only. Keep one-off tar creation, exact tar contents, consumer Git repository, npm installation, writable clone, and installed entrypoint selection local to `test/reviewer-package-lifecycle.test.ts`.
- Leave Judge and Reviewer fast controller tests local. Their faux `ExtensionAPI` maps and direct tool execution do not justify a generic controller/mock framework.
- Leave scenario-specific provider behavior and evidence local: Judge auth/model override dispatch, Coder expansion, Fixer mixed batches, malformed Reviewer sibling behavior, and installed Reviewer parallel/audit flows.

## One deep test module

Replace `test/helpers/test-skill.ts` with `test/helpers/pi-test-harness.ts`, exposing only:

- raw package fixture metadata/loading: repository root, Pi CLI path, and raw manifest;
- mechanical entrypoint path resolution after the caller has retained its behavioral manifest assertions;
- `withHermeticHome(...)`, which creates a unique HOME/agent area, restores the previous HOME exactly, and recursively removes the temporary tree in `finally`;
- `writeTestSkill(...)`, preserving the existing canonical Skill bytes and inspectable return values;
- `withInProcessPi(options, scenario)`, owning common offline runtime/auth registration, settings, loader, session manager/session, extension flags/binding, and shutdown/disposal while exposing raw provider state, model/runtime, loader/extensions, session, and `SessionManager` to the scenario;
- `runPiSubprocess(args, options)`, which only spawns, collects streams, and returns raw `{ code, stdout, stderr }` for every exit code.

Keep genuine seams explicit: cwd/agentDir, source path or extension factory, Skill paths, provider/auth resolver, models path, mode, role flags, custom/no-tools, and Reviewer shutdown behavior. Do not add an assertion DSL, scenario language, fixture registry, generic mock framework, boolean summaries, or evidence selectors.

## Mandatory local assertion preservation

- `runPiSubprocess` must resolve for zero and nonzero exits. In each of the two help tests, retain the complete local `{ code, stdout, stderr }` result and add/retain an explicit local `assert.equal(code, 0)` before checking help output. This preserves `execFile`’s former implicit nonzero rejection and prevents valid help text followed by failure from passing. Raw stderr remains directly inspectable.
- Keep the current assertion-bearing `packageEntrypoint()` logic in `test/package-entrypoint.integration.test.ts`, beside its consumers. It must continue to assert locally that:
  - `manifest.files` contains `extensions`;
  - `manifest.files` contains `souls`;
  - `manifest.pi.extensions` is exactly `["./extensions/role-runtime.ts"]`.
- Every package-entrypoint consumer continues to pass through those visible local checks. The harness may return the raw manifest and mechanically resolve the already-validated entrypoint, but it must not absorb, summarize, or drop these behavioral assertions.
- Keep lifecycle tarball assertions local and exact, including the baseline production path list and absence of `SKILL.md`/copied canonical Skill content.

## File-by-file migration

1. `test/package-entrypoint.integration.test.ts`
   - Use raw shared package metadata and subprocess collection for both help tests.
   - Preserve local manifest assertions and explicit local `code === 0` assertions, along with raw stdout/stderr.
   - Migrate Judge/Coder/Fixer repeated Pi bootstrap and cleanup to `withInProcessPi`.
   - Keep soul/task/packet bytes, canonical TDD symlink setup, auth/model overrides, faux response queues, captured `Context`s, tool/session entry lookups, provider counts, tools, schemas, singleton termination, and receipts next to each scenario.

2. `test/reviewer-role.test.ts`
   - Migrate only the real-Pi schema-invalid Agent sibling test.
   - Leave the controller harness and all direct ledger/audit/fatal tests untouched.

3. `test/reviewer-package-lifecycle.test.ts`
   - Reuse hermetic HOME, canonical Skill writing, and in-process bootstrap.
   - Keep package/tar/Git/npm setup, exact manifest/tar assertions, parallel barrier, raw parent/child/audit contexts, session entries/usage, ledger provenance, clone-isolation snapshots, package installation, and audit correction assertions local.

4. `test/audit-failure-subprocess.test.ts`
   - Retain all three scenario builders, print/JSON arguments, parsers, stage tables, and assertions locally.
   - Route only spawn/stream mechanics through `runPiSubprocess` and cleanup through `withHermeticHome`.
   - Preserve exact exit codes, tool-specific `isError`, stage markers, provider call counts, no later turn, no accepted receipt, stdout/stderr, and fatal diagnostics.

5. `test/judge-role.test.ts`
   - No change; it remains on the fast-controller seam.

6. Remove `test/helpers/test-skill.ts` only after all existing consumers import the deep harness.

## TDD and migration order

1. Record baseline scoped test names and assertion inventory; run the five scoped files as characterization tests.
2. Add at most one harness-focused test proving HOME restoration and recursive cleanup when the callback throws, including preservation of the exact thrown sentinel.
3. Implement hermetic HOME handling and fold in the unchanged Skill writer.
4. Implement the raw subprocess adapter; migrate help and fatal-process callers without changing argument arrays or behavioral assertions. Verify explicit local help `code === 0` and local manifest checks before proceeding.
5. Implement the in-process adapter; migrate packaged Judge/Coder/Fixer, the malformed Reviewer sibling case, and installed Reviewer one at a time, running each affected module after migration.
6. Run a same-pattern check: repeated `ModelRuntime.create`, `DefaultResourceLoader`, `SettingsManager.inMemory`, `createAgentSession`, raw `spawn(piCli)`, and manual HOME restoration should be absent from migrated consumers. Legitimate fast-controller sessions and one-off package/Git/tar setup remain local.

## Preservation and architecture audit

- Retain every existing test name and behavioral assertion verbatim or mechanically relocated; do not delete, weaken, or replace assertions with harness summaries.
- Confirm all role/help, prompt-byte, tool/schema, singleton, receipt, audit pass/revise/fatal, auth/provider, Coder Skill/TDD, Reviewer parallel/clone/ledger/package/audit, Fixer phase/status, subprocess, empty-HOME, tarball, and prior counterexample coverage remains mechanically equivalent.
- Ensure raw Pi messages, provider contexts, tool calls/results, session entries, usage, receipts, process streams, exit codes, and fatal diagnostics remain directly inspectable.
- Confirm no host-state lookup or production fallback/test hook was introduced.
- Confirm the deep harness has real consumers in package-entrypoint, reviewer-package-lifecycle, reviewer-role’s real-Pi case, and audit-failure subprocess tests. Deleting it would redistribute five Pi bootstraps, three process collectors, and repeated HOME/cleanup mechanics across multiple modules.
- Confirm the production tree is byte-identical to baseline, test inventory is retained, duplicated bootstrap/install blocks are reduced, and package dry-run remains exactly baseline production contents.

## Unified verification and commit discipline

Run:

```bash
npm run typecheck
EMPTY_HOME=$(mktemp -d)
HOME="$EMPTY_HOME" npm test
npm pack --dry-run
git diff --check
```

Then confirm:

```bash
git diff 802d28af17efa588aa12d9f8dc689e4a740ebc1d...HEAD -- src extensions souls README.md CONTEXT.md package.json package-lock.json docs .github
```

is empty; strict baseline ancestry holds; exactly one forward Coder commit exists before review; and the worktree is clean. Do not amend, squash, push, or expand into production architecture.
