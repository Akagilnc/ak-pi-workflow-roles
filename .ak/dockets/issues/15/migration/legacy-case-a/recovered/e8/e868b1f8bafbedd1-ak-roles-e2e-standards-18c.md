- **P1 — Judge target binding is unimplemented.** `src/role-runtime.ts:11-31` omits the optional `targetHead` receipt field, while `src/role-runtime.ts:333-342` proceeds directly from verdict validation to Soul audit without checking `AK_JUDGE_TARGET_HEAD`. This violates `/tmp/ak-roles-e2e-authority.md:28` and `docs/adr/0004-targethead-binding-check.md:6`. **Consequence:** a bound invocation can accept a verdict for the wrong repository head. **Repair:** add optional `targetHead` to the static schema/types and, when the environment binding exists, require exact equality before invoking the auditor.

- **P1 — Judge role gating does not narrow active tools.** The judge activation path at `src/role-runtime.ts:244-355` loads its Soul and registers the submission tool but never calls `setActiveTools`; repository search finds no such call. This violates `/tmp/ak-roles-e2e-authority.md:29` and `docs/adr/0008-role-gating-judge-toolset-narrowing.md:6`. **Consequence:** write/edit tools remain available, so the judge can mechanically perform actions its documented role forbids. **Repair:** during judge activation, retain only the documented evidence tools (`read`, `grep`, `find`, `ls`, `bash`) plus the judge submission tool, removing write/edit.
