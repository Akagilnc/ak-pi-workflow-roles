# Collector 6604 current-head Fixer receipt

```json
{
  "status": "completed",
  "report": "# Apply complete: six current-head Collector seams\n\n**Base HEAD:** `6604a733886dfb5d074f558963e20e01e587aa6d`  \n**New HEAD:** `25d0da29b8288a85041e44c2a441e5e632be404e`  \n**Status:** completed — one forward commit, no amend, zero Collector Soul diff.\n\n## What was fixed\n\n### 1. Target-scope unavailability provenance (`src/collector-receipt.ts`)\n- Deleted `establishedHeadFor` entirely (no snapshot-membership / first-observation HEAD binder).\n- `qualifiesUnavailableEvidence` for `unavailableScope: \"target\"` now accepts **only** `review` / `review_comment` with non-null `commitOid === targetHead`, plus author ∈ expectedAuthors and windowRelation ∈ {before, within}.\n- `issue_comment` never proves `target`; same evidence still accepts `global` (kind-agnostic author + before/within).\n- Terminal `target` still stamps `targetSnapshotHead: targetHead` only when proof qualifies.\n- Tests: headless-target rejection rewrite of ledger3; stale review/review_comment reject; exact-head review (PENDING so R1 does not fire) and exact-head review_comment accept.\n\n### 2. Request-path AbortSignal (`src/collector-role.ts` → `src/collector-ledger.ts` → `src/collector-github.ts`)\n- Role request tool no longer ignores `signal`; passes it through `ledger.request(..., signal?)` into `createIssueComment({ signal })`.\n- Hung POST child is killed via existing runner abort listener; abort throws (`/abort|cancel/i`).\n- Cancel leaves no `rejected` / `ambiguous_loss` / success attempt status; process-local one-shot `attemptKeys` retained.\n- Abort rethrow regex broadened to `/abort|cancel/i` so reason `\"request canceled\"` is not misclassified as rejected.\n- R11 observe/GET cancellation tests remain green.\n\n### 3. After-deadline first review observation (`src/collector-evidence.ts` + ledger call site)\n- `applyEvidenceVersionHistory(pending, prior, deadlineTime)` required third arg.\n- New first review version: if `firstObservedAt` unparseable or `> deadline` → `authoritativeTime = null` (no `submittedAt` backdate); at/before deadline may retain `submittedAt`.\n- Known-version null reuse (R5) unchanged; later distinct review versions still null; comments unchanged.\n- Unit + receipt-path tests (activation 00:10, first observe 00:26, boundary 00:25).\n\n### 4. 2xx POST parse/normalization → ambiguous_loss (`src/collector-github.ts`)\n- Only the 2xx success branch wraps `parseJson` + `normalizeIssueComment`; throw → `{ kind: \"ambiguous_loss\" }`.\n- Non-2xx stays `rejected`; transport `ambiguousGhFailure` and abort paths unchanged.\n- Recovery via existing marker observe without second POST proven; three 2xx failure shapes + 422 control covered.\n\n### 5. Inline report author ownership (`src/collector-receipt.ts`)\n- `reviewInlineComments` adds `record.authorLogin === review.authorLogin`.\n- Intruder rows remain in snapshots / `allEvidence()`; not rendered or cited in configured-reviewer report.\n- Same-author inline + R7 `originalLine` stay green.\n\n### 6. Published request-body schema parity (`src/collector-config.ts` + `schemas/collector-legs-v1.schema.json`)\n- Body: `minLength: 1`, `pattern: \"\\\\S\"`, `x-maxUtf8Bytes: 60000` (TS sources constant).\n- Deep equality file ↔ `COLLECTOR_LEGS_SCHEMA`; whitespace-only runtime reject; 60_000 / 60_001 multibyte bounds retained; schema present in `npm pack`.\n\n## Refused\nNone.\n\n## Verification\n- `npm run typecheck` — pass\n- Targeted seam tests — pass\n- Full `npm test` — **223/223 pass** (R1–R11 focused tests green)\n- `npm pack --dry-run` — `schemas/collector-legs-v1.schema.json` present\n- `git diff --check` — clean\n- `git diff` on `souls/` — **empty** (zero Collector Soul diff)\n- HEAD is strict forward descendant of `6604a73`\n\n## Preservation (R1–R11)\nR1 valid precedence · R2 same-leg citation/rebinding · R3 stale-H1 rejection half (stronger carried-HEAD) · R4 full transitive embedding · R5 known-null/modelView · R6 tombstoned users · R7 originalLine · R8 raw redaction · R9 updatedAt bracket/retry · R10 pagination budget · R11 observe/GET cancel.",
  "commitSha": "25d0da29b8288a85041e44c2a441e5e632be404e"
}
```
