# Issue #3 Apply residual packet

Three Apply blockers remain on 62596ba. (1) The two recording inputs are not neutral under the frozen trust contract: the raw user prompts expose outcome labels in `/r-block/` and `/r-ready/`, although readiness-direction labels were required to remain outside model-visible input; the r-block transcript also exposes the paired `r-ready` path through its repository-wide grep. `test/judge-posture-recordings.test.ts` checks only the static `input/prompt.md` (before the invocation appends the labeled path) and does not bind the checked prompt/material bytes to the user/read bytes in JSONL. Re-record both real packaged/audited runs through opaque neutral paths/workspaces, with no expected sidecars or direction labels reachable in model-visible results, and make the offline validator derive and check the actual user prompt and material-read content from JSONL. (2) The acceptance parser is forgeable: it treats missing `isError` as success (`=== true` is the only rejection) and deduplicates by verdict details rather than tool-call identity. Independent detached-worktree probes showed the suite still passes after removing `isError:false` from accepted events and after appending a second distinct accepted tool call with identical details. Require explicit `isError === false`, correlate representations by `toolCallId`, require exactly one distinct accepted call, and add both negative probes (plus a coached/raw-input mismatch probe). (3) `souls/judge.md:44-46` embeds the exact implementation-local examples the approved plan and Soul burden law excluded—helper/function/file names, fake arrays/fixture literals, library syntax, test layout, and byte-calibration field choice—and `test/judge-soul.test.ts` positively requires those terms while its pollution regex misses the Chinese/mixed wording. Keep the irreducible rule that Apply-decidable local mechanics are not plan blockers, but remove the fixture-specific catalog and make the Soul hygiene test enforce principle presence without requiring implementation detail. Any Soul-byte change requires both recordings to be regenerated and digest-pinned again.

## Judge note

Independently verified clean one-commit forward history c721b94→62596ba, exact clean working tree, no changes under src/**, schemas/**, extensions/**, package/lock files, other role Souls, packets, seatbelt, or tracer surfaces. Existing verdict/singleton/audit/tool/fatal/caller contracts therefore remain unchanged by path and targeted contract tests. `git diff --check`, `npm run typecheck`, targeted Judge gates (57/57), and full `npm test` (243/243) pass; these greens do not cure the trust-chain and Soul-layering failures above.
