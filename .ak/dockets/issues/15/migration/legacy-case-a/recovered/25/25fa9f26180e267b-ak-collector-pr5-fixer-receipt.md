# Collector first-order Fixer receipt

```json
{
  "status": "completed",
  "report": "# Fixer apply report — Collector R1–R11\n\n**status:** completed  \n**base HEAD:** `9cc03df314f3014e9cbfa57f3c010a9e1658b8ac`  \n**new HEAD:** `c86c2a23cf87dbfb2c46b1fd9f054bfa29ea0a58`  \n**forward:** yes (`9cc03df` ⊂ `c86c2a2`)  \n**worktree:** clean after commit\n\n## Summary\n\nImplemented all eleven approved first-order roots at existing owner seams. No Soul/schema/README churn, no parallel classifiers, no second AbortController, no pagination removal, no AC relaxation. Preserved `a817975` repeated-drift fail-closed, `6fc54e1` five-minute wait cap, and `9cc03df` provider-visible `JSON.stringify(modelView)`.\n\n## Roots closed\n\n| Root | Change |\n| --- | --- |\n| **R1** | `finalHasQualifyingValidReview` rejects `missing` and `unavailable` when final snapshot has same-leg exact-HEAD qualifying review |\n| **R2** | `valid` requires every cite to be final-snapshot same-leg qualifying review; rebinds leg refs to those proofs only |\n| **R3** | Target unavailability uses `establishedHeadFor(id) === targetHead` (first chronological establishment HEAD), not final membership alone |\n| **R4** | Deleted selective `embedEvidenceIds` path; full ledger snapshots+evidence embed; transitive `snapshot.evidenceIds` resolution |\n| **R5** | `applyEvidenceVersionHistory` reuses prior `authoritativeTime` for known `versionId`; observe `modelView` built from stored snapshot records |\n| **R6** | `user: null` on review / issue comment / review comment normalizes without throw; author absent → never qualifies |\n| **R7** | `originalLine` persisted on evidence; receipt inline text and modelView line use `line ?? originalLine` |\n| **R8** | `authenticated_user.raw` retains only `{login, id?}` |\n| **R9** | `prIdentity` = `state\\|headOid\\|updatedAt`; existing one full-surface retry + fail-closed unchanged |\n| **R10** | `paginate` incremental retained body-byte budget vs `COLLECTOR_SNAPSHOT_MAX_BYTES`; final ledger measure kept |\n| **R11** | Observe tool `signal` → ledger.observe → transport GETs → `createGhApiRunner` (kill child, settle once); no snapshot on abort |\n\n## Tests\n\n- Replaced contradictory subset-embed and clear-on-H2 stale-target tests with closure / persistent-H1-on-H2 oracles\n- Added focused R1–R11 red/green coverage in receipt, ledger, and github suites\n\n## Gates\n\n- `npm test` — 213/213 pass\n- `npm run typecheck` — clean\n- `git diff --check` — clean\n- `npm pack --dry-run` — ok (26 files)\n\n## Commit\n\n```\nc86c2a23cf87dbfb2c46b1fd9f054bfa29ea0a58\nfix(collector): close R1–R11 first-order receipt/evidence/observe seams\n```\n\nBody lists R1–R11 owners and preserved wait/modelView/drift posture. No amend.\n\n## Refused\n\nNone.",
  "commitSha": "c86c2a23cf87dbfb2c46b1fd9f054bfa29ea0a58"
}
```
