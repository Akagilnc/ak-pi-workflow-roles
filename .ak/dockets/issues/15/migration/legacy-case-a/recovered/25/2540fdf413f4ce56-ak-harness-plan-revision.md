Preserve two currently implicit/local assertions in the migration plan. First, `runPiSubprocess` returns on any exit code, while the two help tests currently use promisified `execFile`, which rejects nonzero exits. Require each help caller to retain raw `{code, stdout, stderr}` and assert `code === 0` locally; otherwise help text followed by a failing exit would pass and coverage is loosened. Second, keep the current `packageEntrypoint()` manifest checks (`files` contains `extensions` and `souls`, and the exact `pi.extensions` value) visible beside the consuming tests. The harness may load the raw manifest and perform mechanical path resolution, but it must not absorb or drop those behavioral assertions. Retain the proposed single deep module, raw Pi/session/process evidence, multi-module consumers, and frozen production-surface gate.
