# Collector Fixer receipt

```json
{
  "status": "completed",
  "report": "# Collector apply receipt — six defects repaired\n\n**Status:** `completed`  \n**Start HEAD:** `ce5d2e48477c238b3c4fe3f7b5f11dda723384d4`  \n**New HEAD:** `c7ee3b56597470b273603a214abcc65ced6c1182` (strict forward descendant)  \n**Soul / orchestrator:** unchanged\n\n## What was repaired\n\n### 1. Finalized-message batch provenance\n- Replaced filter-to-Collector-names `evaluateBatch` + dead `lastAssistantBatchKey` with authoritative raw classifier (`classifyRawToolCall` / `classifyCollectorBatch`).\n- Every `type===\"toolCall\"` part is classified, including malformed id/name and schema-invalid args; unknown siblings poison the batch.\n- On allow, persists one `PermittedBatch`; `beginOperational` requires exact `callId`+`name` match; post-fatal has no permit and freezes side effects.\n- Role `message_end` supplies **raw** toolCall parts (no silent drop).\n\n### 2. Mechanical cutoff / final-snapshot law\n- Snapshots record `completedAt` + `completedMono` at successful observe finish.\n- `mutationGeneration` / `observedGeneration` dirty-clear after request/wait.\n- `assertOutputObservationLaw(candidate, clock)`:\n  - before cutoff: `missing` forbidden; valid/unavailable OK if dirty-clear\n  - at/after cutoff: every status needs complete observe with `completedMono >= deadlineMono`\n- Terminal PR reread after surfaces; drift retries full surfaces once; bind terminal HEAD/state only.\n- Request precheck uses shared `reviewQualifiesForValid`.\n- Receipt `finalObservationTime` = final snapshot `completedAt`; output path passes clock.\n\n### 3. Immutable version time provenance\n- Digests include normative fields (`htmlUrl`, `userLogin`, `position`, etc.).\n- `applyEvidenceVersionHistory`: first review version may keep `submitted_at`; later distinct review versions without GitHub edit clocks → `authoritativeTime=null` → `uncertain`.\n- Never invents `firstObservedAt` as authoritative time.\n\n### 4. Prior inline report variants\n- `collectSubstantiveReviewReports` emits variants keyed by `(reviewVersionId, coObservedInlineVersionMembershipKey)` across `allSnapshots()` — inline removal/edit/head moves retain prior membership texts.\n\n### 5. Receipt invariants / overflow\n- Unavailable: reject wrong-author decoys; bind `windowRelation` to qualifying proof (not `refs[0]`).\n- Missing: auto-link final snapshot + same-leg pending/negative/request/recovery material into leg **and** terminal report refs.\n- `recoverySnapshotId` on recovered attempts; embedded in receipt snapshots.\n- Exactly-once resolve across evidence/snapshot namespaces; id collisions fail.\n- Receipt/ledger overflow → `ledger.latchFatal` (`collectorFatal`) infrastructure path.\n\n### 6. Real operational seams\n- `createGhApiRunner()` default spawn against hermetic executable `gh` on PATH (argv, pagination, POST, rejected, ambiguous loss).\n- Ledger recovery through same PATH `gh` sets `recoverySnapshotId`.\n- Pre-registration required-tool collision fails closed before GitHub/provider.\n- Installed-tarball consumer: `npm pack` → `file:` install → load **installed** `extensions/role-runtime.ts` with default transport + PATH `gh` (print + json). No source-factory fake transport stand-in.\n\n## Red-on-`ce5d2e4` evidence (tests vs old src)\n\n| Case | Actual red outcome |\n|---|---|\n| PATH gh recovery | `recoverySnapshotId` was `undefined` |\n| Ledger suite load | missing export `applyEvidenceVersionHistory` |\n| Inline removal | `membership variants …` assertion false (single report) |\n| Missing pre-cutoff | `Missing expected exception` (accepted) |\n| Batch matrix | `pull >= 2` false (no terminal reread); old filter-only batch gate |\n\n## Verification\n\n- `npm run typecheck` — pass\n- `HOME=$(mktemp -d) npm test` — **169 pass / 0 fail**\n- `git diff --check` — clean\n- Working tree authorized repairs only; one forward commit (no amend)\n\n## Judge note\n\nObtain a **fresh independent Reviewer** on `c7ee3b5` before convergence.",
  "commitSha": "c7ee3b56597470b273603a214abcc65ced6c1182"
}
```
