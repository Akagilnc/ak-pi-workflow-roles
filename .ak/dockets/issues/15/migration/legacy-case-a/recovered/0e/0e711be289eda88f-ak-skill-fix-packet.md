Both findings are sustained and are one live wave-2 binding-seam defect class. Repair only `src/canonical-skill-binding.ts` and its existing characterization/lifecycle tests:

1. Preserve the realpath snapshot, but validate Pi’s native location/preamble against the exact pathname spelling Pi can emit for this bound source: the configured `~/.agents/skills/<name>/SKILL.md` path or its already-resolved target, with `References are relative to ...` derived from the matched pathname. Continue rejecting every unrelated/alternate path and require the same exact name, full stripped body, and request.
2. Model Pi’s no-argument serialization at the same seam: `parseSkillBlock` represents an omitted argument as `userMessage === undefined`, so compare that as the empty expected argument and return detached evidence with `userMessage: ""`. Do not add a second provenance mechanism or broaden role/runtime policy.
3. Add red/green coverage that crosses real Pi: a configured symlink whose native expansion preserves the symlink location/baseDir must authorize Coder `completed`, and bare pre-prefixed `/skill:tdd` must remain undoubled and authorize `completed` from the immediately following argument-free expansion. Keep direct binding tests for both normalizations and all alternate-path/wrong-content/wrong-request rejection cases.
4. Re-run typecheck, the initially-empty-HOME full suite, package dry-run, `git diff --check`, and clean-head/ancestry checks. Do not change Souls, public contracts, orchestration, or begin wave 3.

## Evidence
Independent real-Pi probes on clean `7bace1674eeadcfc582cea4ba28c3f6b3086791e` reproduced both failures end to end. With a symlinked configured Skill, Pi emitted the configured symlink path and directory while the binding snapshot used the target realpath; `ak_coder_output completed` was rejected. With a non-symlink Skill and input exactly `/skill:tdd`, Pi emitted a valid block with no `userMessage`; completion was likewise rejected because runtime expected `""`. Thus Standards finding 1 and Spec finding 2 are both sustained. Current typecheck and all 92 empty-HOME tests pass, confirming the existing suite misses these boundaries; ancestry is one commit from baseline, Souls are unchanged, and the worktree remains clean.
