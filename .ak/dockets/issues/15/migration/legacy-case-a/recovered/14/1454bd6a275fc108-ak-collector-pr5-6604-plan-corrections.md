Revise the plan before Apply. The six seams and existing owners are substantially correct, but the submitted plan is not contract-closed: item 1 leaves “either reject or use a head-bearing fixture,” item 6 leaves “x- (or equivalent),” the Apply section contains no fixture syntax, and preservation is not exact (R4 is absent from the explicit header list and verification says only “etc.”).

Submit one complete revised plan retaining explicit Behavior / Owner / Red / Green / Scope for each of all six items and resolving these obligations:

1. Target provenance: delete `establishedHeadFor`; `target` accepts only `review` or `review_comment` with non-null `commitOid === targetHead`; `issue_comment` never proves `target`; `global` remains author+window only. Replace the old positive headless-target case with rejection, not an alternative. Test stale and exact-head forms for both head-bearing kinds, plus headless target rejection and unchanged global acceptance.

2. Request cancellation: thread the identical signal through role execute → `ledger.request(..., signal?)` → `createIssueComment({signal})` → existing runner. Abort must throw, leave no `rejected`/`ambiguous_loss`/success result, produce no late tool result, and kill the owned POST child; retain the existing process-local one-shot behavior rather than inventing retry or cancellation state/classification.

3. Apply packet issue #3 explicitly. Make `applyEvidenceVersionHistory(pending, prior, deadlineTime)` (or name-equivalent but not behavior-optional) use strict temporal semantics: a new review version whose valid ISO `firstObservedAt` is `> deadlineTime` gets `authoritativeTime = null`; equality and earlier observation may retain `submittedAt`; invalid observation time fails closed to null. Known versions first reuse the stored value, including null (R5), and later distinct versions remain null. Add both evidence assertions and receipt-path rejection for valid and unavailable based only on this after-cutoff record.

4. 2xx POST ambiguity: only the 2xx parse/normalization block maps failures to existing `ambiguous_loss`; abort still throws and non-2xx remains `rejected`. Exercise existing ledger marker recovery and prove POST count remains one.

5. Inline ownership: filter only report attachment with `record.authorLogin === review.authorLogin`; retain the intruder record in snapshots and `allEvidence()`.

6. Schema contract: choose the exact extension key `x-maxUtf8Bytes` with value sourced from `COLLECTOR_REQUEST_BODY_MAX_BYTES` in the TS export and literal `60000` in the JSON copy; keep `minLength: 1` and add exact pattern string `"\\S"`. Deep-compare parsed `schemas/collector-legs-v1.schema.json` with `COLLECTOR_LEGS_SCHEMA`, validate whitespace rejection by the schema and runtime, retain the `"é".repeat(30_000)` / `${exact}!` 60,000/60,001-byte runtime boundary, and prove the schema file remains in the package.

Add an **Apply obligations — fixture syntax** section. Use normalized helper property names, not raw API names: `sampleReview({ commitId, submittedAt, ... })`, `sampleReviewComment({ commitId, updatedAt, pullRequestReviewId, ... })`, and `sampleIssueComment({ updatedAt, ... })` with no synthetic `commitId`. For issue 3, record activation at `00:10`, advance 16 minutes before the first observe (deadline `00:25`, first observation `00:26`), and use a pre-cutoff `submittedAt`; also cover exactly `00:25`. For cancellation, use an `AbortController`, a PATH `gh` fixture that verifies `-X POST`, records its PID and hangs, pass `controller.signal` through the registered request tool/ledger path, then assert abort rejection, dead PID, one settlement, no success details, and no rejected attempt. For 2xx ambiguity, runner fixtures must return `{status: 201, headers: {}, bodyText}` for malformed JSON, truncated JSON, and valid JSON missing required comment fields; use 422 as the non-2xx control. For inline ownership, use the same numeric `pullRequestReviewId` but `userLogin: "intruder"`. For schema, use JSON/TS regex escaping exactly as `"\\S"` and key `x-maxUtf8Bytes`.

Exact preservation is an Apply gate, not a general aspiration: keep the focused current-head behaviors/tests for R1 valid precedence, R2 same-leg valid citation/rebinding, R4 full transitive receipt embedding, R5 known-version null reuse and model-view consistency, R6 tombstoned-user handling through 6604a73, R7 `originalLine`, R8 authenticated-user raw redaction, R9 `updatedAt` bracket/retry, R10 normalized pagination budget, and R11 observe/GET abort-child cancellation. Do not delete, relax, rename away, or replace these with mocks. Preserve the correct stale-H1 rejection portion of R3 while replacing its faulty establishment proof. Require no Collector Soul diff, no new binder/classifier/recovery/cancel state, and no parallel validator or recovery path. Apply remains red-first, one forward commit, targeted tests plus typecheck/full test/pack dry-run/diff-check.
