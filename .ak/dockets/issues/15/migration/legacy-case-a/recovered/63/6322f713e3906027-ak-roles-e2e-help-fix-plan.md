At pinned HEAD a7ec90af3c65a30bca4bab652a734b45b2fdb8d9, the Fixer’s targetHead refusal is upheld and requires no repair: authority clause 14 and current `docs/adr/0004-targethead-binding-check.md` explicitly defer that gate until a real binder exists, and commit 622094b is an ancestor of HEAD. Standards is PASS. Fresh Spec P1 is sustained: real `pi --ak-role fixer --help` output derives from `src/role-runtime.ts:240-243` and currently says only that apply executes the approved plan, while authority clauses 6-7 require CLI help itself to disclose that apply also verifies and commits when repaired. Make the minimal root fix by expanding the `ak-fixer-phase` flag description to state that apply executes the approved plan, verifies, and commits when repaired. Do not add a parallel mechanism or new guard: correcting the caller-facing description at its owning registration seam is sufficient. Verify the resulting committed HEAD with `npm test`, `npm run typecheck`, and the real help command.
