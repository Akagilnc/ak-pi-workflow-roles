Authority set: `/tmp/ak-roles-authority-v2.md` clauses 1–9; no ADR or ticket authority was supplied. HEAD `c7eaedacd4b7db91fdb495cc224dac8d88601ebc` is clean. `npm test` (8/8) and `npm run typecheck` are green, but live P1/P2 defects prevent convergence.

Finding dispositions (duplicate Standards/Spec reports are merged):

1. fix_now P1 — Soul-audit parsing fails open (`src/soul-auditor.ts:49-68`). This violates clause 3: a malformed `pass`, pass-with-violations, or first-of-multiple contradictory calls can permit completion without a valid separate compliance decision. Require exactly one audit-decision tool call and validate its arguments exactly: no unknown keys; `pass` only with an empty violations array; `revise` only with a non-empty array of non-blank strings; reject malformed/mixed/multiple decisions rather than filtering them into validity.

2. fix_now P1 — Keyless providers are rejected (`src/soul-auditor.ts:84-92`). Clauses 1 and 3 require an independently usable role and an audit call, while Pi's completion option and resolved `apiKey` are optional. Preserve `auth.ok` failure handling, but allow an `ok: true` auth result without an API key and omit/pass the optional credential as appropriate.

3. fix_now P1 — An accepted judge output is not guaranteed to be the sole/final call (`src/role-runtime.ts:95-96,128-132`). The bundled Soul requires `ak_judge_output` as the final action, and clauses 2–4 require Soul-bound acceptance and the exact judge receipt. Pi's documented `terminate: true` is only a batch hint and works only when every sibling result terminates. Before audit/acceptance, deterministically reject a proposed verdict unless the current assistant tool-call batch contains exactly one call and it is `ak_judge_output`; retain singleton termination. Do not add workflow routing (clause 9).

4. fix_now P1 — Mixed verdict shapes and blank escalation text remain accepted (`src/role-runtime.ts:10-30,103-114`). Clause 4 permits exactly three shapes. Enforce both required and forbidden fields: `converged` has neither payload; `continue` has only a non-blank `fix.summary`; `escalate` has only a decision gate whose question and every option are non-blank. Keep the public tool schema provider-compatible, with runtime validation if a discriminated schema is unsuitable.

5. fix_now P2 (narrowly) — The independently packaged fixer Soul mandates an external `diagnosing-bugs` skill that is not shipped (`souls/fixer.md:13-14`, `package.json:12-14`). This conflicts with clauses 1 and 7 for installations that lack that named skill. Simplify the instruction to require diagnosis for divergent/root-unknown/repeated/flake cases without depending on an unbundled named skill; do not package workflow infrastructure.

6. fix_now P2 — Tests mock away the accepted high-risk boundaries. Add behavioral coverage for all cases above: malformed/extra/multiple/contradictory audit calls; pass-with-violations; keyless successful auth; every forbidden cross-status payload and whitespace-only gate/summary case; and sole-call/final-call enforcement under Pi's parallel-batch semantics. Exercise the real packaged extension boundary where needed rather than relying only on an `any` partial fake. Green tests must include both failure paths and valid examples for all three verdict shapes.

Refutations:

7. refute — The claim that the bundled judge Soul itself violates clause 5 is unconstitutional against clause 2. (a) Claim: references to ADRs, tickets, dispatch schemas, and blocked_by allegedly force an upstream serialized finding format. (b) Current evidence: `souls/judge.md` contains procedural adjudication rules, but `src/role-runtime.ts` accepts an ordinary transcript and only serializes the three-state verdict; it defines no upstream report/finding parser or adoption gate. (c) Authority: clause 5 forbids requiring a new serialized report/finding format, while clause 2 expressly requires the complete bundled judge Soul. (d) Counterfactual: removing those Soul rules would directly violate clause 2 without demonstrating any serialized-format dependency. (e) Disposition: refute; do not trim or rewrite the judge Soul on this finding.

8. refute in part — The claim that the package runtime must capture starting HEAD and enforce fixer commit/ancestry is contrary to clauses 7–8. (a) Claim: prompt enforcement means the package cannot ensure commit behavior. (b) Evidence: `souls/fixer.md` instructs repair/test/new non-amended commit; README assigns the strict-descendant mechanical gate to the caller; runtime only injects the role Soul. (c) Authority: clause 7 assigns work to the fixer, and clause 8 explicitly assigns mechanical Git validation to the caller. (d) Counterfactual: adding a second package-level Git gate would duplicate owner scope and intrude on routing, contrary to clauses 8–9. (e) Disposition: refute runtime Git enforcement and related Git-boundary test demands; only the unbundled named-skill dependency from that combined finding is live under item 5.

9. refute in part — The broader claim that fixer guidance (authority review, relevant history, minimal diagnosis, self-checking ancestry) is unauthorized scope creep is not established. (a) Claim: these instructions restrict otherwise free repair/testing. (b) Evidence: they are internal quality guardrails in `souls/fixer.md`; they add no serialized interface or judge state, and the fixer still consumes `fix.summary`, repairs/tests, and creates a forward commit. (c) Authority: clause 7 requires those outcomes and does not prohibit quality guidance; clause 8 says the caller owns validation, not that the fixer may not self-check. (d) Counterfactual: deleting these guardrails does not cure a demonstrated contract failure and weakens the required commit discipline. (e) Disposition: refute except for the concrete external-skill dependency already accepted in item 5.

Repair boundary: prefer strict validation and deletion of the external skill dependency over new workflow mechanisms. Do not change `souls/judge.md`, introduce report/finding serialization, implement caller-owned Git validation, or expand routing/other roles. After the forward fix commit, run `npm test` and `npm run typecheck`; fresh review on that new HEAD must return to judge.
