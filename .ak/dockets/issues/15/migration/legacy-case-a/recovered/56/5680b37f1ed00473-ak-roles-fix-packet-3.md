Authority set: `/tmp/ak-roles-authority-v2.md` clauses 1–9 (no ADR or ticket authority was supplied). P1 `fix_now` upheld against clauses 1 and 3: `src/soul-auditor.ts:111-127` resolves only API key/headers/env and then calls `provider.stream()` with the original model. Pi’s canonical `ModelRuntime.prepareRequest()` replaces that model’s `baseUrl` with `resolution.auth.baseUrl` before streaming (`node_modules/@earendil-works/pi-coding-agent/dist/core/model-runtime.js`, `prepareRequest`), while `ModelRegistry.getApiKeyAndHeaders()` omits the resolved base URL. GitHub Copilot OAuth provides a concrete credential-dependent endpoint (`node_modules/@earendil-works/pi-ai/dist/auth/oauth/github-copilot.js`, `toAuth`). Therefore the judge’s main call can succeed at an enterprise endpoint while the mandatory separate Soul audit targets the default endpoint and fails. The passing keyless faux-provider integration test does not exercise this boundary. Repair the audit dispatch so it uses Pi’s canonical request preparation, or otherwise propagates the effective auth-resolved model URL from the same request resolution; do not special-case Copilot or add a fallback that preserves the bypass. Add an integration regression whose auth-resolved `baseUrl` differs from the model default and assert the audit reaches the resolved URL with resolved authentication while retaining the existing audit/termination lifecycle checks. Then rerun the full tests and typecheck on the new clean forward HEAD. Surface audit: `original-defect`; no constitutional files were touched.
