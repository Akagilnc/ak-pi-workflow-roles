# Collector two residual packet

Two reproducible residuals remain. (1) `src/collector-github.ts:544-546` recognizes cancellation from error text instead of the supplied signal state. Aborting an in-flight POST with `new Error("deadline exceeded")` or a non-Error reason such as `"stop now"` returns `{kind:"rejected"}`; ledger request handling then records rejection/fatal state. Classify any rejection caused while `input.signal.aborted` as cancellation, preserve the original reason, and add real hung-POST coverage using reasons without `abort|cancel` text (including a non-Error reason), while retaining child kill, one-shot, and no terminal attempt/late success assertions. (2) The after-deadline review guard uses the observe-start timestamp (`src/collector-ledger.ts:700`) even when the observation crosses the cutoff. A probe starting at 00:24:59, fetching its first review at 00:25:01, and completing at 00:25:01 produced `firstObservedAt=00:24:59`, retained the 00:00 `submitted_at`, classified `before`, and accepted `valid`. Bind first-sighting trust to a conservative time/cutoff fact no earlier than actual surface observation (completion is acceptable), and add a receipt-path start-before/finish-after test with a newly appearing review; exact-deadline behavior must remain explicitly tested.
