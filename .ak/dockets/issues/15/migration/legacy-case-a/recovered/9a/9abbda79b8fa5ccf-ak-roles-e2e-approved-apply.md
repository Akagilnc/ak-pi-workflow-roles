# Approved apply packet

## Approval and authority

**Approved for construction.** Authority set: `/tmp/ak-roles-e2e-authority.md` clauses 1–17, with clauses 3, 8, 13–16 controlling these repairs; committed ADR 0003, 0004, 0005, 0007, and 0008 supplement them where consistent. No owner decision is required.

**Apply base:** live Git is clean at `5825396317fb15e23f398eb62ef8d6995356a244`. The concurrent ADR changes are now committed, not unstaged: `5d5d023` makes ADR 0004 caller-neutral, and `5825396` adds ADR 0007’s owner-ratified rule that transport/call retries reuse Pi and the package adds no retry wrapper. Preserve both commits. Do not modify or stage `docs/adr/`, `CONTEXT.md`, or either `/tmp` authority/packet file. Before editing, re-read live status and record the actual apply-opening HEAD; the repair commit must be its strict descendant.

## Construction plan

1. **Start with diagnosis and failing tests.** Use the `diagnosing-bugs` skill for the audit-failure seam. Build a minimal real Pi reproduction before locking implementation: a Judge emits a schema-valid singleton receipt, the separate audit call fails, and a queued later provider response would emit success prose. Establish how `ctx.abort()`, thrown tool errors, print/json completion, and process exit status interact. Add deterministic unit/integration/subprocess tests that fail on the present code. Do not add package-owned retry behavior; Pi-native transport retry remains authoritative under ADR 0007.

2. **Judge `targetHead` binding.** In `src/role-runtime.ts`, add optional nonblank `targetHead` to the static TypeBox schema and every `JudgeVerdict` variant. Preserve status-specific exact-key validation while allowing that one optional field. Read only `AK_JUDGE_TARGET_HEAD`; after verdict validation and before `auditSoulCompliance`, require the submitted value to be present and byte-for-byte equal whenever the environment binding is present. Standalone use remains valid with the field absent or present. Add no generic binding framework and no live-HEAD hard check. Test all three verdict shapes, standalone present/absent, bound equality, bound missing/mismatch, and zero audit calls on binding failure; include a real package-entrypoint bound path.

3. **Judge tool narrowing.** After registering `ak_judge_output` during Judge activation, call `setActiveTools` with only currently registered names from `read`, `grep`, `find`, `ls`, `bash`, and `ak_judge_output`. `write`, `edit`, and every arbitrary sibling must be inactive. Do not inspect bash command text and do not alter the Fixer branch’s tool surface. Extend the harness for tool registry APIs; in real lifecycle coverage assert the exact six-name set when all are registered, removal of write/edit/sibling, continued Judge-output availability, and an independent Fixer lifecycle unaffected by the Judge gate. Update README with the exact whitelist and state explicitly that this is role gating, not a security boundary.

4. **Generic bundled Judge Soul.** Rewrite `souls/judge.md` to retain only generic package law: current-head evidence; deletion/simplification preference and the three guard questions; direct review of behavioral, boundary, and failure-path tests; explicit disposition for every record with no silent downgrade; `converged`/`continue`/`escalate`; infrastructure failure outside verdicts; stuck-work escalation; and repair-surface auditing. Remove Ming/container institutions, all issue/ADR numbers, `family`, `stationReceiptContracts`, verify/runner topology, OPEN-ticket/`blocked_by` suppression, and ledger machinery. Add no package overlay mechanism; host law remains available only through Pi-native append-system-prompt/project skills. Expand `test/judge-soul.test.ts` with positive kernel assertions and explicit forbidden host terms/numbering while retaining byte-complete Soul injection integration coverage.

5. **Fatal Soul-audit infrastructure failures.** Preserve `revise` as an ordinary errored tool result that permits model resubmission, with no retry cap, brake, or automatic retry wrapper. For exceptions originating from the separate auditor—including auth/provider/toolchain failure and malformed auditor output—use the real Pi action seam established by step 1: abort the current agent operation, ensure noninteractive print and JSON invocations finish nonzero, then rethrow so the audit submission is an errored tool result and never an accepted receipt. Do not apply this fatal path to singleton validation, verdict validation, target binding, or `revise`. Do not claim completion unless the subprocess acceptance test proves nonzero exit, no accepted Judge receipt, and no later prose; an isolated rejected Promise or `isError` assertion is insufficient. If the minimal reproduction disproves the proposed `ctx.abort()` plus noninteractive exit-status mechanism, stop and return `refused` with evidence rather than inventing broader lifecycle machinery.

6. **Fixer singleton final call.** Generalize only the existing persisted-assistant-batch singleton helper so it accepts the expected submission tool name. Pass the real `ExtensionContext` into Fixer execution and run the singleton check before envelope/phase validation. Require `ak_fixer_output` to be the sole tool call in the persisted final assistant batch. Preserve all current phase/status/commitSha rules; add no envelope field and no live-HEAD check. Test one valid call, two Fixer calls, and Fixer+sibling in both orders, with no ambiguous receipt accepted; add real package lifecycle coverage rather than relying only on a cast context.

7. **Verification and commit discipline.** Run focused new tests, the real CLI/subprocess acceptance, `npm test`, and `npm run typecheck`. Audit the final diff for collateral changes and confirm constitutional files remain untouched by this repair. Stage only authorized runtime/auditor files if needed, `souls/judge.md`, README, tests, and a minimal test fixture. Create one forward commit without amend or history rewrite. Report the actual opening HEAD, final commit SHA, tests run, and clean/remaining worktree state in `ak_fixer_output`; any unrelated concurrent change must remain unstaged and be reported separately.

## Acceptance boundary

All five original defects remain `fix_now`: Judge target binding, Judge tool narrowing, generic Judge Soul, fatal audit-infrastructure Action failure, and Fixer singleton submission. Construction is accepted only after a new-head mechanical rerun and fresh review; Fixer testimony alone cannot establish convergence.
