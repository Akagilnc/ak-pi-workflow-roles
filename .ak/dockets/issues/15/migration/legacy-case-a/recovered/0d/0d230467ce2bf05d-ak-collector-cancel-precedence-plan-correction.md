Revise the Green oracle to match the packet’s sole ordering change. Use separate branches in this exact order: (1) `if (input.signal?.aborted) throw error` (exact caught-value identity), (2) non-aborted `ambiguousGhFailure === true` returns `ambiguous_loss`, (3) only then the non-signal `AbortError` belt rethrows, and (4) otherwise return `rejected`. The current Green snippet combines `input.signal?.aborted || name === "AbortError"` ahead of ambiguous tagging, which additionally reclassifies a non-aborted error carrying both `name: "AbortError"` and `ambiguousGhFailure: true`; that contradicts the plan’s own Behavior section, the packet’s requirement to retain ambiguous loss for non-aborted failures, and the hard sole-residual scope. Lock this order with a non-aborted ambiguous-tagged control (including the overlapping `AbortError` name), while retaining the planned real hung-POST red/green case asserting tagged abort-reason identity, child death, explicit `started`, and one-shot retention. Keep ownership and changes limited to `src/collector-github.ts` and `test/collector-github.test.ts`.
