# Final Collector Fixer receipt

```json
{
  "status": "completed",
  "report": "# Fixer apply report — repeated observe-bracket drift\n\n## Status\n`completed`\n\n## Commit\n`a817975c375255a9c1cfb7c307f32e79f23594a8`  \nBase: `3bec03f84b1bee6a5f7d7c7068028b3c2849116f` (strict forward descendant; no amend)\n\n## Root cause fixed\nAfter first-pass `prIdentity` drift (`state|headOid`), `observe` retried full surfaces once but **accepted the retry without re-comparing** retry `prInitial` vs `prTerminal`. Persistent drift still fell through to normalize / `storeEvidence` / snapshot push / `latestCompleteSnapshotId` bind.\n\n## Production change\n**`src/collector-ledger.ts`** — inside the existing single-retry `if` block only:\n\n```text\nfetch → if drift → fetch once more → if still drift → throw\n                                      else bind retry terminal\n```\n\nThrow message: `PR identity drifted across observe bracket after retry (…)`.  \nCaught by existing observe `try/catch` → `latchFatal(\"Collector observe failed: …\")` **before** pendingRecords, store, marker recovery, snapshot, or model-view return.  \nNo second retry, helpers, flags, or parallel mechanisms.\n\n## Regression\n**`test/collector-ledger.test.ts`** — `repeated PR identity drift after retry fails closed without certifying`\n\n- Sequence: `OPEN/A→OPEN/B` then `OPEN/C→OPEN/D`\n- Non-empty review + issue comment evidence (would store on wrong commit path)\n- Asserts: `calls.pull === 4`, observe rejects, `fatal === true`, reason matches observe/drift, `allEvidence().length === 0`, `allSnapshots().length === 0`, `latestCompleteSnapshotId === undefined`, `buildCollectorReceipt` throws\n\n**Control preserved:** `terminal PR reread binds terminal HEAD and retries on drift` (`A→B` then `B→B` → binds `head-b`).\n\n## Finding disposition\n| Finding | Disposition |\n| --- | --- |\n| Repeated drift after single retry accepted as complete | **Accepted and fixed** at owning observe seam |\n| Schema / Soul / README / CLI / receipt contracts | **Out of scope** — untouched |\n| Extra retry layer / soft-warn / test-only assert | **Rejected** — not used |\n\n## Verification\n1. `npm run typecheck` — pass  \n2. `HOME=$(mktemp -d) npm test` — 197/197 pass  \n3. `npm pack --dry-run` — pass  \n4. `git diff --check` — clean  \n5. Forward commit only; working tree clean after commit  \n\n## Residual risk\nNone for this packet: throw sits before commit path; control path unchanged.",
  "commitSha": "a817975c375255a9c1cfb7c307f32e79f23594a8"
}
```
