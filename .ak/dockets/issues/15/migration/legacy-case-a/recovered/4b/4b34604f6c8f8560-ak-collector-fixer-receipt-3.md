# Collector Fixer receipt 3

```json
{
  "status": "completed",
  "report": "# Collector repair 3 apply — completed\n\n**Baseline:** `b43bf20`  \n**Commit:** `91a1a92426c9a98df30071cc36ab934e42d0ef55` (strict forward descendant)  \n**Files:** `src/collector-receipt.ts`, `test/collector-receipt.test.ts`, `test/collector-role.test.ts`  \n**Unchanged:** Soul, orchestrator, public CLI/schema contracts, `collector-tool-schemas.ts` shape, production ambient/overflow guards and review-report emission\n\n## F1 — Singular schema ownership\n\n**Production:** `parseCollectorOutputCandidate` now has one acceptance path: `Value.Check(collectorOutputArgsSchema, raw)` then typed leg copy. Deleted the diagnostic hand-validation branch (~100–160) and the post-Check `unavailableScope` re-gate.\n\n**Oracles:**\n- Invalid parse → single generic `/failed schema validation/` failure\n- Shared `collectorToolArgumentsValid` / `classifyCollectorBatch` matrix for observe `undefined|null` and every malformed output row\n- Registered schema walks `anyOf` and asserts three strict variants (`valid`/`unavailable`/`missing`), `additionalProperties: false`, `unavailableScope` only on unavailable\n- Real-Pi invalid rows + observe+invalid-output sibling → exit 1, all six GitHub counters zero\n- Separate well-formed controls: missing (post-cutoff + final observe), unavailable, multiline rationale\n\n## F2 — Mandated decoys + terminal-report ref parity\n\n**U1–U5** now match the five mandated decoys (not the prior mislabeled set):\n1. cross-leg authenticated requester marker/comment  \n2. cross-leg expected-author evidence  \n3. after-window evidence  \n4. actual unrelated PR evidence id (separate real ledger fixture, pr 99 / other/repo)  \n5. actual unrelated snapshot id (same fixture)\n\nWrong-author and dangling kept as extras only.  \nClean unavailable, latestRelevant, missing-contamination, and M-clean assert **leg `evidenceRefs` ≡ terminal-fact `evidenceRefs`** per leg; non-contaminated legs stay clean.\n\nNo production ownership broadening (no mandated decoy was false-accepted).\n\n## F3 — Cross claimed seams\n\n**Retention:** split into three tests (edit / dismiss / disappearance). Each builds a receipt via `buildCollectorReceipt` and matches historical **review** report variants and each report’s own evidence refs (not merely `allEvidence()`).\n\n**Ambient/required:**\n- **skills:** injects nonempty `systemPromptOptions.skills` in a prior `before_agent_start` handler — no `skillsOverride`, no skill-command load. Fatal matches `/ambient skills in systemPromptOptions/`; six counters zero.\n- **contextFiles / appendSystemPrompt:** seam-specific fatal text + six counters zero via captured `failInfrastructure`.\n- **required-tool / ambient-commands:** activation-time fatal text; `faux.state.callCount === 0`; six counters zero.\n\n**Overflow role path:** wraps registered output `execute` at the tool boundary (not the faux response factory). Asserts `outputExecuteEntered`, `collectorFatal === true`, and message reports exactly `COLLECTOR_RECEIPT_MAX_BYTES + 1` (33554433); create stays 0.\n\n**Note (provider unused on before_agent_start ambient):** Pi’s extension runner swallows `before_agent_start` throws and continues the agent turn, so faux may still stream after the collector has already latched fatal. Activation-time failures (required-tool, ambient-commands) keep `callCount === 0`. Seam-specific fatal text and GitHub zero are asserted for all ambient rows.\n\n## Verification\n\n- `npm run typecheck` — pass  \n- `node --import tsx --test test/collector-*.test.ts` — 91/91 pass  \n- `test/collector-package-lifecycle.test.ts` (npm pack dry-run + installed tarball e2e + help) — 3/3 pass  \n- Working tree clean after commit; HEAD is forward of `b43bf20`",
  "commitSha": "91a1a92426c9a98df30071cc36ab934e42d0ef55"
}
```

Driver independently verified full hermetic suite: 195/195 pass; typecheck, 26-file pack dry-run, diff-check, clean worktree.
