Revise the plan before apply. It does not yet close the exact packet:

1. Batch provenance: `toolCallPartsFromMessage` currently drops malformed tool-call parts, and the proposed permitted shape validates only ID/name. Make one authoritative finalized-call classifier inspect every raw tool-call part and its arguments; malformed/unknown parts (including a sole schema-invalid call and a valid+schema-invalid sibling) must poison the batch before execute. Exercise every ordered matrix case in the same real-Pi session; the proposed separate valid session is not an invalid→valid test. Assert no additional transport/POST side effects after the first fatal batch.

2. Cutoff: the output predicate must receive the current clock/monotonic time; otherwise it cannot detect a provider turn crossing the deadline. Record completion wall time for every complete snapshot and internal completion monotonic time. At/after cutoff, every status requires a complete observation that finished at/after the cutoff obligation; valid/unavailable are early only before cutoff. Keep the post-request/wait dirty generation independent and require a later completed observe. Add real-Pi/fake-clock provider-crossing tests, plus observe-start-before/finish-after. The terminal PR reread must be the final read, supply the stored PR evidence/snapshot, and reject or retry if the initial and terminal HEAD/state differ.

3. Version provenance: specify the stateful owning mechanism. Normalization alone cannot know that a version is later. Before window assignment/storage, ledger history keyed by `stableGitHubId` must preserve the first submission version and force every later distinct review body/state version without a supplied version timestamp to `authoritativeTime=null`. Enumerate all normalized fields affecting identity/reporting in each digest and add a mutation test for fields currently omitted, rather than leaving completeness as an audit note.

4. Historical reports: one selected snapshot per review version cannot pass inline removal/edit when the unchanged review version appears in several snapshots. Emit/dedupe immutable report variants by `(review version, co-observed inline-version membership)` (or an equivalent single mechanism). Thus removal yields both the prior report with the old inline ref and the later report with no inline; edits yield both inline versions without cross-snapshot leakage.

5. Receipt completeness: define the concrete recovery representation, including retained ambiguous-loss diagnostics and the exact recovery snapshot ID where the authenticated marker appeared. Automatically added missing-leg pending/negative/request/recovery proof must be linked into that leg/report refs, not merely copied into `evidenceRecords`. Enforce exactly-one resolution across both ID namespaces and fail evidence/snapshot collisions. Keep the planned qualifying unavailable-ref binding and exact 8/32 MiB tests.

6. Real seams: a mocked `spawnImpl` is not the packet’s hermetic executable `gh`. Put an executable `gh` on hermetic `PATH`, invoke `createGhApiRunner`/the transport with its real spawn path for argv, pagination, and POST outcomes, then drive ambiguous-loss recovery through ledger observe using that same seam. The installed-consumer test must definitively install the tarball and load its installed `extensions/role-runtime.ts` in print and JSON modes with the PATH fake `gh`, exercising the packaged default transport; remove the source-factory/injected-transport fallback. Keep startup collision/absence/ambient-resource cases in real Pi with zero provider/GitHub calls and avoid production-only test hooks.

Add these tests before implementation and record their ce5d2e4 outcomes; do not use the checklist’s “equivalent assertions documented” escape hatch for reproducible behavioral defects.
