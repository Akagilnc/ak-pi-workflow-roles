# PR5 7ff Fixer receipt

```json
{
  "status": "completed",
  "report": "# Apply complete: PR5 three current-head repairs\n\n**status:** `completed`  \n**commitSha:** `f01c9bf8540e46820fe1bfe012af3171a19a16a2`  \n**base HEAD:** `7ffbd3684fe41dc63c28cb7cb79696f2f5e0c3ed`  \n**forward:** verified (`merge-base --is-ancestor` OK); no amend; clean worktree after commit.\n\n## 1. Monotonic first-sighting eligibility — adopted\n\n**Root cause:** cutoff ownership is `deadlineMono`, but `applyEvidenceVersionHistory` still gated on wall `Date.parse(firstObservedAt)` vs wall `deadlineTime`, so mono-past / wall-before first sightings could keep `submitted_at` and prove `valid`.\n\n**Fix:**\n- `src/collector-evidence.ts`: third arg is now `{ deadlineMono, firstObservedMono }`. Predicate: non-finite mono or `firstObservedMono > deadlineMono` → `authoritativeTime = null`; else `submittedAt ?? null`. Wall `firstObservedAt` is not a trust gate.\n- `src/collector-ledger.ts`: at the existing post-fetch surface hand-off, stamp `firstObservedAt = clock.wallNow().toISOString()` (metadata) and `firstObservedMono = monoNowOrThrow(clock)` (trust); pass cutoff object. No new evidence-record fields.\n\n**Green proof:**\n- Unit: `=== deadlineMono` keeps `submitted_at`; `>` nulls; known-version null reuse; non-finite mono nulls (wall-unparseable trust gate dropped).\n- Integration: `mono past deadline with wall before deadline nulls first-sighting trust` — mono `960000` / wall `00:20` / `submitted_at=00:05` → `authoritativeTime null`, `windowRelation=uncertain`; both `valid` and `unavailable` receipts rejected.\n- Existing start-before/finish-after mid-observe coverage remains green.\n\n## 2. Child stdin EPIPE settle — adopted\n\n**Root cause:** `createGhApiRunner` wrote stdin with no `error` handler; early child exit could raise process-level `uncaughtException write EPIPE`.\n\n**Fix (`src/collector-github.ts` only):** attach `child.stdin.on(\"error\")` **before** `write`/`end`; route through existing `settle()`:\n- aborted → reject `signal.reason`\n- else → reject with `ambiguousGhFailure: true` (same non-HTTP tag family as close path)\n\n**Green proof:**\n- Real-child stub exits immediately; 32 MiB stdin → single rejection, `uncaughtException` count `0`.\n- Same stub via `createIssueComment` → `ambiguous_loss` (not process death / flaky `rejected`).\n- Prior abort / rejected / ambiguous PATH tests stay green.\n\n## 3. Published author schema parity — adopted\n\n**Root cause:** body items had `minLength: 1` + `pattern: \"\\\\S\"`; `expectedAuthors.items` had only `minLength: 1`, so schemas accepted `\"   \"` while `loadCollectorManifest` rejected non-blank.\n\n**Fix:** both `COLLECTOR_LEGS_SCHEMA` and `schemas/collector-legs-v1.schema.json` author items now `{ type: \"string\", minLength: 1, pattern: \"\\\\S\" }`. Runtime trim/lowercase unchanged.\n\n**Green proof:**\n- file ↔ TS deep equality retained.\n- author `items.pattern === \"\\\\S\"` + minLength 1.\n- Behavioral `RegExp(pattern)` rejects `\"   \"` / `\"\"`; accepts `\"CodexBot\"` and `\" CodexBot \"`.\n- Existing blank-author load rejection + trim-normalization tests stay green.\n\n## Explicit non-goals honored\n- No Collector Soul / binder / classifier changes.\n- No mono stored on evidence records.\n- No window-relation wall math rewrite for trusted clocks.\n- No new cancel statuses / attempt states.\n- No AC weakening.\n\n## Verification\n- `npm test` → **233 pass / 0 fail**\n- `npm run typecheck` → clean\n- Touched owners only (9 files): evidence, ledger, github, config dual-schema, four test files.\n\n## Refused\nNone. All three findings adopted in one forward commit.",
  "commitSha": "f01c9bf8540e46820fe1bfe012af3171a19a16a2"
}
```
