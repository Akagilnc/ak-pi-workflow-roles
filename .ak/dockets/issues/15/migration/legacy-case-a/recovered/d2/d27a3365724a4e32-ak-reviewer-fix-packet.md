Treat this as a live Reviewer construction defect, not audit overreach or a task defect. The immutable JSONL itself proves the two `Agent` calls were siblings in one assistant message (`/tmp/ak-reviewer-self-review-2.jsonl`, event line 171) and both succeeded (lines 177/179). However, `ReviewerAgentAttempt` and `ReviewerExecutionRecord` flatten them (`src/role-runtime.ts:104-128,502-528,622-628`), while `src/reviewer-auditor.ts` receives only that structured record. The completed-receipt audit therefore correctly could not prove an explicit canonical requirement that the task and Skill both impose. The accepted refusal is not a valid completion substitute: the review ran, and only the runtime discarded its provenance.

Smallest repair:
1. At the existing Reviewer `Agent.execute` seam, derive immutable invocation evidence from `ctx.sessionManager.getLeafEntry()`: the assistant session-entry ID and the sibling `Agent` tool-call IDs containing the current call. Store one typed batch record (or equivalent shared batch ID) in `ReviewerExecutionRecord`; retain `executionMode: "parallel"`. Do not infer from prose, descriptions, timestamps, or add orchestration APIs.
2. Fail closed if an Agent call cannot be tied to its persisted assistant message, and give the Reviewer auditor the batch evidence. Add tests proving two calls from one actual Pi assistant message share one batch, calls from separate messages do not, the two child executions overlap, and the auditor can accept the resulting completed receipt. Re-run the independent review to obtain a completed receipt; do not preserve the present refusal as the outcome.
3. Repair the independently verifiable active-provider defect in `src/reviewer-agent.ts:301-330`: `Object.create`/`Object.assign` fabricates an object without class private slots. Use a narrow Reviewer child-provider adapter that delegates/binds streaming behavior to the original provider instance while overriding only resolved auth/model exposure. Add a real class-based provider regression whose stream method reads a private field.
4. Close only the mandated Reviewer test gaps: run the installed tarball from an independent temporary consumer Git repository (the current lifecycle test uses `cwd: packageRoot` and leaves `fixture` unused), and table-drive print/JSON fatal checks for the specified child preparation/provider/session/malformed-output and audit auth/provider/malformed-decision classes. Keep all work inside Reviewer; add no CMR, panel, model-selection, routing, chain, or generic subagent/clone framework.

## Judge note

Other draft findings are explicitly disposed: symbolic `refs/remotes/*` identity is not currently actionable—the snapshot preserves the fixed-point name/object resolution required by canonical diff/log commands, and no review failure was shown from making an immutable symbolic ref direct. The Repeated Switches item is a non-blocking smell suggestion; introducing a role map is unnecessary for this repair. Current head is clean; all 65 tests, typecheck, diff-check, and pack dry-run pass, but those tests do not cover the provenance or class-provider counterexamples above.
