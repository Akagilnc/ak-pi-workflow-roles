# Wait cadence Fixer receipt

```json
{
  "status": "completed",
  "report": "# Apply complete: Collector wait cadence (5-minute single-wait cap)\n\n**Status:** completed  \n**Base HEAD:** `a817975`  \n**Commit:** `6fc54e117ebc5e43414015c6e02ef332e9f7d2af`  \n**Ancestry:** strict forward descendant of `a817975` (no amend)\n\n## What was fixed\n\n### Root cause\n`createCollectorLedger().wait()` computed `effectiveMs = Math.min(requested, remaining)` only. A valid `900_000` ms request with ~762k ms remaining slept the full remainder (~12m42s), blocking re-observation until near cutoff (PR4 dogfood).\n\n### Code changes\n\n1. **`src/collector-ledger.ts`** — private single-wait cadence at the wait seam:\n   - `const COLLECTOR_SINGLE_WAIT_MAX_MS = 300_000`\n   - `effectiveMs = Math.min(input.durationMs, remaining, COLLECTOR_SINGLE_WAIT_MAX_MS)`\n   - Constant not exported; schema max, cutoff law, records, abort path untouched\n\n2. **`src/collector-role.ts`** — truthful wait wording only:\n   - method line, tool `description`, and `promptGuidelines` now state both the five-minute runtime cap and the remaining-eligibility cap\n   - no kickoff/schema/Soul/README edits\n\n3. **`test/collector-ledger.test.ts`** — real-seam red→green proofs:\n   - **Ample remaining:** activate → `wait(900_000)` → `requestedMs: 900_000`, `effectiveMs: 300_000`, `remainingMsAfter: 600_000`, `cutoffReached: false`; ledger record mirrors; control `wait(120_000)` unchanged\n   - **PR4 shape:** advance `137_612` ms (remaining `762_388`) → `wait(900_000)` → exact `effectiveMs: 300_000`; real `ledger.observe(...)` with fake transport completes with `completedMono < deadlineMono`\n   - **Near-cutoff + post-cutoff retained:** remaining `60_000` still wins over the 5m cap; post-cutoff wait still rejected\n\n## Out of scope (preserved)\n\n- Public schema `maximum` / `COLLECTOR_ELIGIBILITY_MS` (still 900_000)\n- Soul, README, ADR, Runner, scheduler/polling/watcher\n- No second timer or auto-reobserve mechanism\n\n## Verification\n\n| Gate | Result |\n| --- | --- |\n| `npm run typecheck` | pass |\n| `HOME=$(mktemp -d) npm test` | 199 pass / 0 fail |\n| `npm pack --dry-run` | pass |\n| `git diff --check` | clean |\n\n## Findings disposition\n\n| Finding | Disposition |\n| --- | --- |\n| Five-minute single-wait cap missing | **fixed** at ledger `wait()` |\n| PR4 long single-wait blocks re-observe | **fixed** + proven with exact fake-clock setup |\n| Role wait wording omitted 5m cap | **fixed** |\n| Near-cutoff remaining wins / post-cutoff reject | **retained** unchanged behavior |",
  "commitSha": "6fc54e117ebc5e43414015c6e02ef332e9f7d2af"
}
```
