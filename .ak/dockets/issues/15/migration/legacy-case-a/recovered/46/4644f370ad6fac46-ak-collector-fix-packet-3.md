# Collector repair packet 3

Repair only these remaining construction/test defects on a forward commit:

1. **Restore singular F1 schema ownership.** In `src/collector-receipt.ts:100-160,179-186`, delete the schema-rejection diagnostic validator (manual top-level/leg key lists, status/rationale/ref/scope checks) and the redundant post-check scope validation. `parseCollectorOutputCandidate` must have one acceptance mechanism: `Value.Check(collectorOutputArgsSchema, raw)`, then a typed copy/transform; invalid input gets one generic schema failure.

2. **Complete the exact F1 oracles.** Add the shared-check/batch matrix for observe `undefined|null|{}` and every malformed output row; inspect the registered union’s strict variants rather than the current vacuous structural assertion. Replace/add real-Pi sibling cases with a valid operational `observe` plus an invalid output (at least unknown leg field and missing unavailable scope), not the current output+output case. Exercise well-formed missing, unavailable, and multiline controls separately. For every invalid/sibling row assert all six GitHub counters are zero and fatal/nonzero termination.

3. **Use the mandated F2 decoys and assertions.** In `test/collector-receipt.test.ts`, replace the mislabeled U1–U5 rows with one call each for: cross-leg authenticated requester marker/comment, cross-leg expected-author evidence, after-window evidence, an actual unrelated PR evidence ID, and an actual unrelated snapshot ID. Dangling/wrong-author cases may remain as extras but are not substitutes. Assert both leg and matching terminal-report refs for both clean legs and for the non-contaminated leg in the missing/latestRelevant fixtures. Do not broaden production ownership logic unless an exact counterexample fails.

4. **Make F3 tests cross their claimed seams.** Split `F3 review edit/dismiss/disappearance retention` into three tests that build a receipt and assert the historical review report variants and their own evidence refs; its current assertions stop at `allEvidence()`. The ambient-skills test currently fails earlier at activation as `skill:ambient-collector-skill`, so construct an in-process fixture that reaches the `before_agent_start` `systemPromptOptions.skills` guard and assert its fatal reason; likewise assert the specific context/append failure seam, provider unused, and all six GitHub counters for every ambient/required-tool row. In the 32 MiB role test, replace `outputExecuteEntered` (currently set while generating the assistant call) with evidence from the execute/failure lifecycle, and assert `collectorFatal` plus the reported serialized size is exactly `COLLECTOR_RECEIPT_MAX_BYTES + 1`.
