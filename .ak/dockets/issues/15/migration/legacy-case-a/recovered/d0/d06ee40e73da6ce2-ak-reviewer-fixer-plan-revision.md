The repair surface is appropriately narrow and covers the four live finding families, but the planned tests do not yet mechanically prove all named paths. Amend the fatal table so every row asserts a unique expected stage diagnostic/marker and the expected provider-call count (in addition to exit 1, no later turn/receipt/refusal); in JSON, assert `isError:true` on `Agent` for child rows or `ak_reviewer_output` for audit rows, not merely a generic aborted stop. Otherwise a provenance/startup failure could make the entire matrix pass without reaching child preparation/provider/session/output or audit auth/provider/decision. Likewise, make malformed/missing leaf tests assert abort, a failed recorded attempt, and zero child starts. Bound the real-Pi overlap barrier with a deterministic timeout/release failure path so a sequential-execution regression fails rather than hanging the suite; assert peak concurrency is exactly two. Retain the plan’s batch provenance, delegated class-provider adapter, independent consumer-repository lifecycle, auditor serialization/acceptance gate, full verification, and fresh-HEAD completed review.
