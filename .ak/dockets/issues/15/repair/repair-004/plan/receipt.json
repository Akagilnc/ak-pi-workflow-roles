{
  "status": "planned",
  "report": "Read-only investigation completed at clean HEAD; no files or Git history changed. Authority is `.ak/dockets/issues/15/repair/repair-003/apply/judge/receipt.json`; fixed predecessor is `8bc5db9837fff3aa7b16000cffeb888dfd76233a`. History confirms `d81605c` created repair-003 and `8bc5db9` only resealed its ledger. Current dispositions contain 277 admitted items and exactly seven immutable references.\n\nR1\n- Behavior: The closed association extractor must apply `scanTextAssociations` to every JSON string regardless of whether it is the top-level parsed value, an array element, or an object value, while retaining structural-key handling and omission of `issuePrCommitAssociations`. Commit labels must reject syntactically valid but unresolved 7–39-hex tokens.\n- Deepest Owner: `walkJsonAssociations` / `extractAssociationsClosed` and their existing boundary test in `test/legacy-case-a-migration-verifier.test.ts`.\n- Red: Add same-extractor cases for a top-level JSON string and a string directly inside an array; both currently return no association. Replace `zzzzzzz` with a deterministic valid absent prefix: enumerate seven-hex candidates in fixed ascending order and select the first for which no commit in the sealed four-root universe starts with it; assert the candidate is valid, absent, and rejected. Remove the unasserted `deadbee` probe.\n- Green: Both JSON-string cases produce their expected issue/PR/commit sets through `extractAssociationsClosed`; the selected valid absent prefix produces `null`; existing structured, ambiguous-prefix, excluded-subtree, and exact-set tests remain green. Re-derive all 597 associations and change only `issuePrCommitAssociations` in `dispositions.json` and/or `recovered-index.json` if the corrected derivation differs.\n- Scope: Existing verifier/extractor only, plus association-only persisted output changes proven necessary by derivation. No generic/session payload reads, excluded-payload reads, helper verifier, or unrelated migration changes.\n\nR2\n- Behavior: Before importing `dist/recorder/scanner.js`, the repair-003 child must resolve its tracked bytes to a full repository/full-commit/path/blob-OID/SHA-256 Git tuple, ledger that tuple, compare the worktree bytes with it, and fail closed on drift. The independently checked `src/recorder/scanner.ts` tuple remains an additional input and cannot stand in for the imported executable.\n- Deepest Owner: `verifyAndLoadScanner`, scanner tuple setup, summary schema, and red gates in `.ak/dockets/issues/15/repair/repair-003/recorder-closure/exhibits/cutoff-scan-implementation`; mechanical verification remains in `test/legacy-case-a-migration-verifier.test.ts`.\n- Red: Exercise the existing child gate with a mismatching/perturbed dist digest and prove rejection occurs before dynamic import; add verifier assertions that the scanner ledger contains distinct source and dist tuples and that the summary’s executable path/hash/tuple agree. This specifically replaces the ineffective source-only `scannerHash` proof.\n- Green: On the sealed target, both source and dist worktree hashes equal independently resolved Git tuples; the dist tuple is present in `identityLedger.gitTuples` and the scanner section; only then is the tracked dist module imported and `scanBytes` accepted.\n- Scope: Modify only the existing repair-003 child and sole verifier. Keep source identity, add dist executable identity, and add no alternate scanner, helper, build substitution, or successor recorder mechanism.\n\nR3\n- Behavior: Ledger and verify every required immutable Git input, including all seven disposition reference tuples even when private-rescue or `/tmp` bytes win source selection. Establish completeness from an independently derived required-tuple set rather than from entries already present in the ledger; rerun and reseal repair-003 evidence at a new forward target.\n- Deepest Owner: Tuple collection/completeness gate and result generation in the existing repair-003 child; independent completeness oracle and artifact verification in `test/legacy-case-a-migration-verifier.test.ts`; seals in `recorder-closure/inputs/cutoff-scan-summary` and `recorder-closure/manifest.json`.\n- Red: Derive required tuple keys independently from immutable discovery spec, walk, inventory and namespace snapshot; committed dispositions; every admitted disposition’s reference, redaction report, and recovered derivative when applicable; both scanner source/dist inputs; and every other repository execution input. Delete/perturb one expected key in a test copy/set comparison and assert failure. Explicitly assert the seven reference dispositions produce seven matching reference tuples even though current source selection uses external copies; a self-enumerated ledger with any one omitted must fail.\n- Green: Resolve every expected tuple and compare exact canonical tuple-key sets (and each repository, full commit, path, blob OID, SHA-256), rejecting missing, extra-conflicting, or mismatched entries. Independently verify all 277 external seals against item key/source SHA/frozen metadata without retaining bytes. Rerun the existing child and regenerate only its summary and manifest seals after the corrected code/association state is fixed, using the minimal forward construction/sealing sequence without amend or history rewrite. Prove child exit 0, 597/0/0, exactly 277 scans, exactly two redactions, all source/derivative/hit matches, complete Git/reference/external ledger, and no raw-source duplication.\n- Scope: Preserve every artifact predating repair-003, all four immutable commits and historical nonconformance identities, repair-002 seals, and repair-003’s `d81605c`/`8bc5db9` commits. No Case B, #16/#17, generic payload inspection/retention, raw source copies, Soul/runtime/schema changes, parallel verifier/helper, amend, or rewrite. Refuse during apply if any required historical or external identity fails resolution or hash comparison.\n\nApply verification (not run in this plan phase): run the child with its existing execution seam, mechanically verify regenerated seals and identity completeness, then run `npm run typecheck` and `node --import tsx --test test/legacy-case-a-migration-verifier.test.ts`; inspect the final diff for association-only migration changes and repair-003-only evidence changes before creating forward commit(s)."
}
