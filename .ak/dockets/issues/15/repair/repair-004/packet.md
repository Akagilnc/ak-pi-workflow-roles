# Issue 15 Case-A repair-004 — Apply-proof closure

Authority: `.ak/dockets/issues/15/repair/repair-003/apply/judge/receipt.json`. Fixed predecessor construction target: `8bc5db9837fff3aa7b16000cffeb888dfd76233a`.

## Exact repair set

R1: Repair the existing closed association extractor so top-level JSON strings and strings directly nested in arrays pass through the same text grammar. Add same-extractor red/green boundaries for both. Replace the invalid `zzzzzzz` unresolvable-OID probe with a deterministically selected syntactically valid 7–39 hex prefix absent from the sealed four-root commit universe and assert rejection. Update persisted association outputs only if corrected derivation changes them.

R2: In the existing repair-003 Recorder child, before import resolve and ledger a full repository/commit/path/blob-OID/SHA-256 tuple for the actual tracked `dist/recorder/scanner.js`, compare worktree bytes to that tuple, and fail on drift. Keep the scanner source tuple as an additional input; do not substitute source identity for the executable module actually imported.

R3: Record and verify every admitted immutable reference tuple, including all seven reference dispositions, even when an external source copy is selected. Add a completeness oracle derived independently from immutable dispositions, reports, derivatives, scanner and other execution inputs; it must fail if a required tuple is absent rather than merely validating self-enumerated ledger entries. Rerun the existing repair-003 child, regenerate its result/manifest seals at one new forward target, and prove 597/0/0, exactly 277 scans, both redactions, all source/derivative/hit matches, complete Git/reference/external identity ledger, and no raw-source duplication.

## Readiness and preservation

For each key state Behavior, deepest Owner, Red, Green, and Scope. Exact reconciliation requires `{R1,R2,R3}`, each key once, each keyed disposition nonblank; headings and status wording are not conformance requirements.

Use only the existing verifier and existing repair-003 Recorder child/evidence seam. Add no parallel verifier/helper or successor mechanism. Preserve every artifact predating repair-003, the four immutable commits and historical nonconformance identities, repair-002 seals, and repair-003’s two already-recorded construction commits. No history rewrite, generic/session payload inspection or retention, excluded-payload read, raw-source duplication, Case B, #16, #17, Soul/runtime/schema change. Refuse if any required historical or external identity is now absent or changed.
