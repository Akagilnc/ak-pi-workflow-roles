{
  "status": "planned",
  "report": "| R# | Disposition |\n| --- | --- |\n| R1 | implemented(extend `test/legacy-case-a-migration-verifier.test.ts` to seal immutable-Git spec/walk/inventory and all 597 identities/predicates/post-cutoff partitions, then preserve independently executable corroboration code and result in a new Recorder successor; refuse during apply if any required source identity changed) |\n| R2 | implemented(in the same successor invocation run real `scanBytes` on all 277 original non-generic sources and compare source SHA-256, derivative SHA-256, and complete hit reports including `42a9fc…` and `af289a…`, storing only implementation/result and failing on source drift) |\n| R3 | implemented(make the existing association test derive and verify the exhaustive exact set from frozen metadata and admitted non-generic bytes, add a second-item `33454566…` red mutation, reuse one local association type, rename PR5 locals, remove the unused `gitIsAncestor` head parameter, and run focused verifier plus typecheck) |\n| R4 | implemented(add one append-only closure record sealing the Judge-specified original and `49807d4` Receipt/manifest blob and SHA-256 identities, classify the latter as nonconforming successors, bind R1-R3 evidence, verify exact-set manual reconciliation, and leave all historical paths byte-unchanged) |"
}
