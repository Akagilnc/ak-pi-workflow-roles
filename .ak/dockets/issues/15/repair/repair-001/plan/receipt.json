{
  "status": "planned",
  "report": "| R# | Disposition |\n| --- | --- |\n| R1 | implemented(plan: delete exactly the seven sustained recovered executable probe/snippet paths; add immutable `ea64733e382c6dcf14906b3b52782ec3f1c07535` commit/path/blob/SHA lifecycle coordinates in existing dispositions; map only genuinely live behavior to existing seam regressions; recompute recovered-index, redaction joins, counts, coverage matrix, and manifest; verify no archive, helper, or duplicate regression) |\n| R2 | implemented(evidence: metadata-only no-follow/hash comparison against `git show ea64733e382c6dcf14906b3b52782ec3f1c07535:.ak/dockets/issues/15/migration/legacy-case-a/construction-walk.json` found recorded=597, matched=597, missing=0, changed=0 without semantic JSONL reads; plan: replace the existing self-comparison test with fixed-target Git assertions and an independently coded sealed-predicate walk, then perform one Recorder-preserved current `/tmp` corroboration separating post-cutoff additions) |\n| R3 | implemented(plan: because all 597 identities matched, invoke the actual Recorder scanner on every admitted non-generic source, reproduce source/derivative hashes and hit reports including `42a9fc…` and `af289a…`, preserve formal invocation evidence without generic payload or duplicate bytes, and make any mismatch fail closure) |\n| R4 | implemented(plan: add structured issue/PR/commit associations to existing migration metadata wherever frozen provenance or admitted bytes establish them, including `ecec8803…` → PR 5 / `6604a733886dfb5d074f558963e20e01e587aa6d`, and extend the existing verifier rather than creating an index) |\n| R5 | implemented(plan: recover/reference `fixer-commit-msg.txt` and `issue-1-commit-msg.txt` separately by recorded identity, deduplicate and Recorder-scan them, correct classifier precedence in the existing seam, then recompute dispositions, joins, hashes, counts, and seals) |"
}
