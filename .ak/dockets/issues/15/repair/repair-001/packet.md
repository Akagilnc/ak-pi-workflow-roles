# Issue 15 Case-A migration repair packet

Immutable migration target: `ea64733e382c6dcf14906b3b52782ec3f1c07535`. Preserve it; repairs are forward-only. Governing judgment: `.ak/dockets/issues/15/review/review-004/judge/receipt.json`.

| R# | Requirement |
| --- | --- |
| R1 | Delete the seven sustained recovered executable probe/snippet files from current tree; bind their immutable target commit/path/blob/SHA identities in lifecycle dispositions; reconcile dispositions/recovered index/redaction joins/counts/matrix/manifest without archive/helper or duplicate regression. |
| R2 | Replace the producer self-comparison verifier with fixed-target Git verification and independently implemented sealed-predicate source walk. Run one metadata-safe current `/tmp` corroboration through Recorder, separating post-cutoff additions. If any of 597 historical identities is absent/changed, refuse and require owner escalation; do not substitute a cutoff. |
| R3 | Where all recorded sources match, run actual Recorder scanner over every admitted non-generic source and reproduce source/derivative hashes and hit reports, especially the two redactions. Preserve invocation evidence without generic payload or second byte copy; refuse on mismatch. |
| R4 | Add structured known issue/PR/commit associations in existing metadata wherever frozen provenance or admitted bytes make them known, including item `ecec8803…` → PR 5 / target `6604a733886dfb5d074f558963e20e01e587aa6d`; extend existing verifier, no second index. |
| R5 | Rescue recorded `fixer-commit-msg.txt` and `issue-1-commit-msg.txt`, deduplicate and scan, recover/reference separately, correct classifier precedence and all joins/seals. |

Plan/Apply response must be exactly one two-column Markdown table (`R#`, `Disposition`) with R1..R5 exactly once and no other R-like key; each disposition `implemented(<test/evidence>)` or `refused(<reason>)`. Case B, #16, #17, history rewrite, JSONL semantic reads, generic payload retention, and Reviewer changes are excluded.
