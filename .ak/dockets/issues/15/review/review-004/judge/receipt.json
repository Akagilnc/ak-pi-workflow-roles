{
  "judgeStatus": "continue",
  "fix": {
    "summary": "Adjudication against frozen Case-A authority:\n\n**Standards**\n1. `collector-adjudication-probe.ts` — **sustain**: permanent executable scratch outside `test/adjudication/`.\n2. `collector-cross-cutoff-probe.ts` — **sustain** independently on the same lifecycle fact.\n3. `collector-cancel-probe.ts` — **sustain**; ordinary Collector cancellation regression now owns the behavior.\n4. `judge-malformed-probe.ts` — **sustain**; the malformed-sibling behavior is already graduated in `test/reviewer-role.test.ts`.\n5. `issue-1-independent-probe.ts` — **sustain**; live seatbelt behavior belongs to ordinary package/runtime regressions, not recovered executable scratch.\n6. `reviewer-real-pi-snippet.ts` — **sustain**; `test/reviewer-role.test.ts` is the canonical graduated shape.\n7. `ak-collector-independent-probes.ts` — **sustain**; it is a permanent aggregate scratch probe rather than the seam regressions.\n8. Duplicated mutable-clock code — **sustain only as derivative smell**. Delete the disposed probes; do not consolidate them or add a helper.\n\n**Spec**\n1. Frozen denominator — **reject**. `construction-walk.json` is the authority-required immutable walk snapshot/evidence, the independent walk records the same 597 complete identities, and Reviewer found no concrete omitted cutoff item. Frozen authority does not require an atomic filesystem image or copying forbidden generic payloads. The eight-second lag alone is not a supported finding; independently executable closure is addressed separately by P2.\n2. Independent verifier — **sustain**. The existing test compares producer JSON sets, reads ambient files, and does not pin `ea64733e382c6dcf14906b3b52782ec3f1c07535`; a consistently omitted source item can pass.\n3. Skipped directory — **reject**. `topLevelOnly:true` plus `includeDirectories:false` excludes `collector-apply-evidence` from the candidate denominator, and both walks record the skip. Recursion would change frozen authority.\n4. Scanner evidence — **sustain**. Reports self-name `scanBytes`; the verifier checks the name/hashes/booleans, not an actual source-to-derivative Recorder-backed execution, including the two redactions.\n5. Eight-axis outcomes — **reject**. Frozen Red 9 asks for explicit disposition outcomes; `coverage-matrix.json` explicitly gives `status:\"recovered\"` for all eight axes. Requiring a separately named governing closure artifact adds authority.\n6. Associations — **sustain**. Amendment 001 and Plan Red 8 require known issue/PR/commit associations. Item `ecec8803…` lacks PR 5 and target `6604a733886dfb5d074f558963e20e01e587aa6d`, both present in its admitted bytes.\n7. Uncited probes — **sustain separately**. All seven use only generic name-class admission and lack a claim/reproduction coordinate; amendments 003/005/006 do not authorize automatic permanent probe admission.\n8. `fixer-commit-msg.txt` — **sustain**. Frozen provenance expressly includes `commit-msg`, contradicting its `ephemeral-cli-scratch` classification.\n9. `issue-1-commit-msg.txt` — **sustain independently** for its distinct omitted issue-1 reasoning.\n\n**Smallest forward-only repair packet**\n\nA. Never amend, rewrite, or replace immutable target `ea64733…`. Remove the seven named executable probe/snippet files from the current tree. Map still-live behavior to existing seam tests; graduate once only if genuinely absent, then delete scratch. Preserve value-bearing Receipts/findings and record lifecycle disposition with immutable target commit/path/blob/SHA coordinates. Update dispositions, recovered index, redaction joins, counts, matrix, and manifest consistently. Do not add a probe archive, shared clock, or parallel lifecycle guard.\n\nB. Replace—not supplement—the false self-comparison in the existing migration verifier with fixed-target verification that reads `ea64733…` via Git and independently implements the sealed predicates. A one-time current `/tmp` rewalk may only **corroborate** the committed cutoff snapshot: preserve its code/result through Recorder formal-invocation evidence, report post-cutoff additions separately, and do not claim it can exclude an entry deleted after the historical cutoff. If any of the 597 recorded identities is now absent or changed, stop this repair and return for owner escalation; do not manufacture historical proof. Only if ordinary tests cannot reach this volatile seam may the single source-walk implementation remain under `test/adjudication/`.\n\nC. In the same bounded closure operation, where recorded source identities still match, run the actual Recorder scanner over admitted non-generic sources and mechanically reproduce source hashes, derivative hashes, and hit reports—especially `42a9fc…` and `af289a…`. Preserve Recorder invocation evidence, not another byte copy or production hook. Any missing/mismatched source is the same stop-and-escalate threshold.\n\nD. Populate structured known issue/PR/commit associations wherever frozen provenance or admitted bytes make them known, including the concrete `ecec8803…` association. Repair the existing metadata verifier so it checks the already-mandated field; do not create a second association index.\n\nE. Rescue the two `commit-msg` items by their recorded identities, deduplicate, scan, and recover/reference them separately; correct their inventory/dispositions and recompute all joins/seals. Correct the existing classifier/verifier precedence rather than adding a parallel name guard.\n\nF. Preserve the repair Receipt/manifest in a forward closure-evidence commit and re-review the original target plus repair diff. No Case B, #16, or #17 implementation.\n\n**Why these checks are warranted (repair three-question audit):** (1) the reproducible failures are the seven live probe copies, the verifier’s producer-only false pass, scanner-name-only evidence, the concrete missing association, and the two contradictory `commit-msg` dispositions; (2) their owners are respectively repository probe lifecycle, the existing Case-A migration verifier/closure seam, Recorder’s scan boundary, and the existing migration metadata/classifier seam; (3) deletion is sufficient for probes and is therefore the only remedy there, but deletion cannot supply missing fixed-target/scanner evidence, mandated associations, or preserve the two omitted reasoning artifacts. Those are repaired in the existing artifacts/verifier rather than by parallel mechanisms."
  },
  "note": "Authority, Plan, Plan-judgment, Apply, and Reviewer-004 Receipt hashes match their manifests. `ea64733…` is a forward descendant of the fixed base; its migration tree/verifier remain byte-identical at current HEAD. Fresh typecheck and the current verifier pass 10/10, but the latter pass does not cure P2/P4 because it compares producer records and scanner labels. If the bounded source-identity precondition in B/C fails, the next judgment must be `escalate` with owner options to accept a qualified evidence ceiling, supply an external immutable snapshot, or declare strict historical closure unattainable; a fresh cutoff must not be silently substituted."
}
