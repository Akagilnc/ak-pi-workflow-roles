# Issue 15 Case-A final closure review after repair-004/005

Review fixed diff `8bc5db9837fff3aa7b16000cffeb888dfd76233a..c71b947dff2c8103f5a48411b4e82ef54ad9b2cf` and resulting target, while rechecking all previously sustained repair-003/004 findings against the complete Case-A migration claim chain. Later Apply Receipt archival is closure evidence only.

Standards axis: repository `CLAUDE.md`, especially Soul discipline and Probe lifecycle; distinguish durable required Recorder exhibits from disposed scratch.

Spec axis: issue #15 Case-A Authority and accepted repair judgments. Independently verify:

- exact fixed target and immutable ancestry;
- top-level/array JSON-string association extraction and syntactically valid unresolved OID red oracle through the same extractor;
- closed association source universe/grammar and exact persisted outputs without generic/excluded payload reads;
- actual imported `dist/recorder/scanner.js` full Git tuple and pre-import byte gate, distinct source tuple;
- complete independently derived Git tuple universe, all seven admitted references, 277 external seals, manifest/result/required count equality;
- 597 matched/0 missing/0 changed, cutoff-derived partition, 277 scanner reproductions, both redactions, full source/derivative/hit equality;
- historical identities, repair-002 seals, no wrongly excluded value-bearing candidate, five roles and issues #1–#3, no historical-completeness claim;
- no generic JSONL/session/tool-event payload in the migration tree;
- append-only handling of intermediate construction commits and later Receipts.

Allowed: PI_* metadata, Git/tree/stat/JSON/SHA, current `/tmp` names/lstat, admitted non-generic source bytes required for scanner/association proof, target recovered/reference non-generic bytes, `npm run typecheck`, and focused migration verifier. Do not run full `npm test`. Do not inspect excluded payloads or `.ak/work`. Case B/#16/#17 are out.

Run exactly one sibling-parallel Standards/Spec batch and preserve distinct findings. Return only through `ak_reviewer_output`.
