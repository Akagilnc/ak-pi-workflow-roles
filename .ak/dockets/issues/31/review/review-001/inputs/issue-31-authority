{"body":"## Problem Statement\n\n裁类打法(判官裁类 → fixer 灭整类 → 圈界复审验残留)已在 #23 实战验证有效,但它的全部承载方式是散文与手工:判官把类裁决写进判词散文,fixer 把搜索范围与例外写进 report 散文,caller 把圈界键逐轮手打进 opaque task。后果三条,全有当日实证:每轮手抄(司机记性,session 压缩即蒸发);对账靠肉眼(七键表差点漏落纸);机器咬不住散文(锚定宪法)。过渡方案 playbook(#25)以流程文档承载,但流程文档与真实合同并存必漂移成双真源,且编排文本寄居角色仓有违宪嫌疑(ADR 0010)。\n\n## Solution\n\n把打法折进三份角色 I/O 合同:判词带类字段、回执带对账键、评审输入带圈界参数。循环次序从此不需要任何文档规定——它是三份合同的推论:任何合格 caller 拿到「continue+类判词」自然派 fixer,拿到带键回执自然对账,复审自然圈界。playbook 与其 CLAUDE.md 指针随合同落地删除,README 承说明职。\n\n## User Stories\n\n1. 作为 caller(主刀),我要判官判词自带类字段(类名/owner/边界/处置),以便直接据此编修理包,不再从散文里誊抄归纳。\n2. 作为 caller,我要 fixer 回执带 classesRepaired 键,以便与判词类键做键集对账,漏修一眼可见。\n3. 作为 caller,我要在派审时传 scopeKeys 参数即得圈界评审,以便不再每轮手打圈界散文进 task。\n4. 作为 caller,我要「无 scopeKeys=全量审」的默认语义,以便旧调用方式零迁移成本继续可用。\n5. 作为 fixer(角色),我要回执里有 searchScope 与 exceptions 的法定字段位,以便「报搜索范围与例外」的义务有处安放而不是挤在散文里。\n6. 作为 fixer,我要判词的 boundary 字段作为裁定越界的判断依据(soul 级),以便据以走 refused 出口时有明文可引。\n7. 作为判官(角色),我要 classes[] 的法定字段位,以便裁类产出不再被压进 fix.summary 散文。\n8. 作为外审/值守,我要键集闭合可机械核对(判词键 vs 回执键),以便对账不再依赖肉眼读散文。\n9. 作为 owner,我要打法活在合同里而非任何 session 或流程文档里,以便换司机、压缩窗口、隔周再来,行为都不回退。\n10. 作为 owner,我要循环次序保持为合同推论而非规定文本,以便 caller 的拓扑自由(ADR 0010)不被任何文档蚕食。\n11. 作为将来的审计/医生,我要回执中的 searchScope 与 exceptions 是 typed 字段,以便机械验证类闭合时有键可咬。\n12. 作为包的外部消费者,我要 tarball 里没有任何编排文本,以便装到的是纯角色包。\n13. 作为读 README 的新 caller,我要一节「裁类循环如何由三份合同组成」的使用说明,以便冷启动时不靠口头传承。\n14. 作为 caller,我要 task 继续保持 opaque、capabilities 继续纯授权面,以便圈界参数的引入不搅浑既有边界(#17 之法)。\n\n## Implementation Decisions\n\n(全部为 owner 2026-07-31 grill 逐题拍定)\n\n- 三份合同同波交付:判官判词、fixer 回执、reviewer 派审输入;coder/collector 合同不动(无拉力,ADR 0001)。\n- 判官判词增 `classes[]: {name, owner, boundary, disposition}`——name=对账键;owner=持有该不变式的 seam;boundary=修理授权边界(fixer 越界即 refused 的机械判据);disposition=该类修理令(散文)。boundary 为**判官给 fixer 的判断输入**(散文,soul 级消费:fixer 据以裁越界并走 refused 出口),非机械判据——机械对账仅靠 name 键(判官设计审 D5,owner 拍)。组合律:`continue` 必带 ≥1 类;`converged`/`escalate` 禁带 classes(D1)。`fix.summary` 保留作总叙事。不带 instancesCited/severity(样本真源在评审收据,义务真源在 soul)。\n- fixer 回执增 `classesRepaired[]: {name, searchScope, exceptions[]{where, reason}}`——回执只装 diff 照不出来的东西;修复内容真源=forward commit。组合律:`completed` **且派单材料含类键**时必带 classesRepaired 且键集对齐;无类派单(直接派 fixer 不经判官,ADR 0010 合法形态)禁带,其 completed 不受类键约束;`planned`/`refused` 一律禁带——拒绝证据走 report 散文(D2,r2 复审修正)。既有字段不动。\n- reviewer 圈界入口=新增 CLI 参数 `--ak-review-scope-keys k1,k2`(不新增文件、不碰 capabilities 纯授权面、task 保持 opaque;判官设计审 D4,owner 拍)——编译期机械注入双腿 prompt(走既有确定性构造);未传该参数=全量审。\n- 空数组禁令:classes/classesRepaired 禁空数组——字段缺席=无该语义,空数组拒收(schema minItems)。scopeKeys 为 CLI 参数,契约独立:缺席=全量审;传参但空串/解析零键/含空白键=invalid argv,**启动即拒不起腿**(D3,r2 复审修正,owner 拍)。\n- playbook 与 CLAUDE.md 指针随合同落地同波删除;README 增「裁类循环」使用说明一节。\n- shape 校验由 Pi 交卷层原生强制(schema 写好即每单生效),不另建校验机制。\n- 循环次序不写任何规定性文本。\n- 结晶文档:ADR 0015(打法持久化走合同)、ADR 0016(测试跟逻辑不跟格式)、CONTEXT 词条「裁类循环」,在 design/contract-doctrine 分支待收编。\n\n## Testing Decisions\n\n- 好测试=真实缝上的外部行为;格式无物可测——shape 由 Pi 交卷层每单强制,零测试(ADR 0016)。\n- 唯一新测试:scopeKeys 编译分支的一条 tracer,搭进**既有的** prompt 确定性测试(prior art:仓内 deterministic canonical prompt construction 既有用例)。三问已过:圈界静默失效=无声漏钱且无下游兜底。\n- 键集闭合不写测试——每单实审(现由 caller 对账,将来归审计/医生,#12)。\n- 循环行为不写测试——终验=下一张新窗票零口头嘱咐的实弹观察,行为回退则本 feature 未完。\n- 空数组禁令(两回执数组)=schema minItems,Pi 交卷层白送,零测试;scopeKeys 的 argv 拒收为启动器逻辑,若落码至多一条 tracer,先过三问。\n- 三态组合律(D1/D2)为跨字段 validator 逻辑:若落码,每律至多一条真缝 tracer,先过三问(ADR 0016)。\n\n## Out of Scope\n\n- fixer 交卷审计与类闭合机械验证(owner 已裁留 #12 归医生)\n- coder/collector 的合同改动\n- 枚举器工具(正则 vs AST vs 构造点收拢——owner 已裁等真拉力再议)\n- #26 全系统违宪清查本体\n- 任何规定循环次序的文本或编排机制\n\n## Further Notes\n\n**r3(2026-07-31 晚)**:r2 复审 escalate 三点(D2 拓扑侵蚀/D3 CLI 语义/D5 story-decision 冲突)owner 拍定修正。\n\n**r2(2026-07-31 晚)**:经工厂判官设计审 escalate 五点(三态×classes/三态×classesRepaired/空数组语义/scopeKeys 真实入口/boundary 性质),owner 逐条拍定后修订。\n\n#25(playbook)为前代过渡实现,已并线,作为历史保留不改写。#30 为本 PRD 之前抢跑创建的决策票,其处置(作废/降为切片/保留)归 owner 裁定。实现顺位与切片由后续 to-tickets 步骤决定。\n\n","comments":[{"id":"IC_kwDOTklDoM8AAAABMogIjg","author":{"login":"Akagilnc"},"authorAssociation":"OWNER","body":"## Owner resolution of design-recheck gate\n\nAdopt option 1, the strict bounded shared-key grammar:\n\n- keys are nonblank and comma-free;\n- comparison is exact and case-sensitive with no normalization;\n- names/tokens are unique within each Judge `classes`, Fixer `classesRepaired`, and Reviewer `scopeKeys` collection; duplicates are rejected at the owning schema/argv seam;\n- ordering is immaterial;\n- alignment is exact set equality with no missing or extra key.\n\nThis resolves the sole escalation in `.ak/dockets/issues/31/judgment/design-recheck/receipt.json`; all other Issue #31 decisions remain unchanged.\n","createdAt":"2026-07-31T12:14:33Z","includesCreatedEdit":false,"isMinimized":false,"minimizedReason":"","reactionGroups":[],"url":"https://github.com/Akagilnc/ak-pi-workflow-roles/issues/31#issuecomment-5142743182","viewerDidAuthor":true}],"number":31,"state":"OPEN","title":"PRD: class-repair doctrine as role I/O contracts","updatedAt":"2026-07-31T12:14:34Z","url":"https://github.com/Akagilnc/ak-pi-workflow-roles/issues/31"}
