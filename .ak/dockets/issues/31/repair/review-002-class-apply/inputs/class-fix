# Issue #31 direct class repair

Sole authority: `.ak/dockets/issues/31/judgment/review-002/receipt.json`. Apply directly with no Fixer Plan.

Repair exactly the two sustained classes and the full bounded owner seams stated in the Judge receipt:

- `ReviewerScopePromptDataIntegrity`
- `ReadmeTypedContractExamples`

Treat Reviewer-cited lines as samples. For scope prompt data, census every parent/child production sink, repair one shared serialization/construction seam with reversible injective delimiter-safe exact-identity representation, and add the authorized real-parent/every-real-child table/property oracle including delimiter forging, quotes, backslashes, controls, and escape-looking collisions. Do not narrow or normalize owner-valid keys.

For README, census live canonical Judge/Fixer usage examples and synchronize legal status-dependent typed shapes without topology, mandatory Judge, routing, repetition, or package-owned reconciliation claims.

Preserve every Judge non-goal. Run focused red/green and normal gates, commit forward, and submit `ak_fixer_output` with `classesRepaired` containing exactly the two names above, each with nonblank `searchScope` and explicit `exceptions` (empty allowed). Report census, shared owner, repaired sinks, exceptions, and current-head evidence.
