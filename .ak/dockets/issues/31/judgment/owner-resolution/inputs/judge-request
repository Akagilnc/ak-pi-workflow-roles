# Issue #31 owner-resolution judgment

The prior design recheck escalated only the shared key grammar. Owner selected option 1 and persisted it on Issue #31:

- nonblank, comma-free keys;
- exact case-sensitive comparison with no normalization;
- unique keys within Judge `classes`, Fixer `classesRepaired`, and Reviewer `scopeKeys`, duplicates rejected at each owning schema/argv seam;
- order immaterial;
- exact set equality, no missing or extra key.

Confirm whether this resolves the sole gate and makes Issue #31 construction-ready without reopening O1 or any settled design. Submit only through `ak_judge_output`.
