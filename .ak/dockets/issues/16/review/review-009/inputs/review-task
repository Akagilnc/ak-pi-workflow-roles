# Issue #16 final candidate-pair review

Use exactly this range in both sibling prompts:

```text
git diff 78cd4e9807aa8a6ebf5a0d852b096b62180fb2da...1490fbe95bcadedd4f1a434213da712b06a4c8ff
```

Authority: issue #16 and prior closure under `.ak/dockets/issues/16/`.

Verify the latest PR-finding repair:

1. complete terminal content—not text projection alone—must match before collapse;
2. mismatch/candidate analysis is linear rather than quadratic and does not cross-product independent lifecycles;
3. a complete failed dual-representation call followed by a complete successful retry under a new call ID accepts only the retry;
4. true mismatched candidate IDs and a valid pair plus any extra opposite representation remain fail-closed;
5. tee-only usage copying remains non-mutating and all prior lifecycle/provenance/order/replay/conflict/split/promotion/absence obligations remain discharged.

Review Standards and Spec in exactly one sibling-parallel batch. Reviewer and both legs use Codex Sol high. Fresh invocation; no second batch.
