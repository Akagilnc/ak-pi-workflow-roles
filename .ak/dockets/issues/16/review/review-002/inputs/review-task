# Issue #16 fixed-target review — corrected invocation

Review exactly the merge-base range using this canonical command in both sibling prompts:

```text
git diff 78cd4e9807aa8a6ebf5a0d852b096b62180fb2da...49a73c6b71683da488ec593731c23b2e834b176d
```

Authority: GitHub issue #16 as preserved at `.ak/dockets/issues/16/review/review-001/inputs/issue-16`.

Required behavior:

1. A production-shaped combined `tool_execution_end` plus `message_end` `toolResult` for one accepted Judge or Reviewer call canonicalizes to one logical terminal and yields its typed Receipt/audit observation.
2. Equivalence binds call ID, package tool, accepted/error state, content, typed details, and compatible usage.
3. Same-representation replay, conflicting details, mismatched identity, replay, and out-of-order lifecycle events remain fail-closed.
4. Complete Recorder promotion proves the combined shape; lawful absent-Receipt behavior is unchanged.
5. No Soul, role contract, orchestration, migration, or generic-session-retention changes.

Review Standards and Spec in exactly one sibling-parallel batch. Treat generated `dist/recorder/extract.js` consistency with source as part of Standards. This is a new Reviewer invocation after the preserved review-001 refusal; do not dispatch more than one batch in this invocation.
