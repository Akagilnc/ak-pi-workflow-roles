# Issue #16 final closure judgment after ordering repair

Judge immutable construction target `ba5824558101d68275f19d772c9728d132cdd33d`.

Authority: `.ak/dockets/issues/16/review/review-001/inputs/issue-16`.
Prior continue judgment: `.ak/dockets/issues/16/judgment/final/receipt.json`.
Final independent review: `.ak/dockets/issues/16/review/review-007/receipt.json` (Standards 0, Spec 0).

The prior blocker required the dropped `tool_execution_end` to occur after a genuinely matching start. Final target now requires strict issuance → start matching issued tool and args → tool_execution_end → equivalent message_end/role:toolResult. It retains the early terminal for both `issued → tee → start → toolResult` and `issued(Judge) → mismatched start → tee → matching start → toolResult`, causing null Receipt/audit.

Rejudge the entire #16 obligation set: one-envelope current-Pi canonicalization, retained toolResult trust source, exact message/message_end provenance, raw mismatch validation before collapse, all conflict/replay/order/identity/split/extra-terminal negatives, typed Judge/Reviewer Receipt and audit promotion, lawful absence, source/dist consistency, and unchanged excluded surfaces.

Local final evidence: focused extraction 21/21; typecheck passed; build/source-dist clean; diff check passed. Return only through `ak_judge_output`; do not edit or commit.
