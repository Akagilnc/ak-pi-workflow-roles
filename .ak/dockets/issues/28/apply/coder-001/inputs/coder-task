# Issue #28 Coder apply — Navigator v1 and package-owned Assisted Runner

Implement the complete construction authorized by these admitted receipts, in order:

1. `.ak/dockets/issues/28/authority/final-authority-001/receipt.json`
2. `.ak/dockets/issues/28/authority/assisted-runner-final-001/receipt.json` (narrowly supersedes lifecycle/trigger ownership)

There is no Plan leg. Work test-first and commit forward on the current branch.

Deliver all authorized package slices:

- Navigator Soul, closed typed snapshot/evidence/output contracts, bounded evidence reader, same-model audit, exact tool gate, runtime registration, public exports, CLI/help/docs, and honest ordinary/insufficient/refused/escalated outcomes.
- Package-owned Assisted Runner public API and `ak-assisted-run` CLI for enter/resume/recover/end/status, with one caller-selected non-Navigator packaged role per call, automatic initial/fresh Navigator consultation, no automatic recommended-role dispatch, legal deviation, no recursion, and direct-role compatibility.
- Canonical append-only hash-chained assisted-run ledger under `.ak/work/issues/<parent>/assisted/<runId>/`, atomic/idempotent call and invocation identities, concurrency failure, crash/recovery truth, end semantics, and typed status result.
- Typed Git/GitHub acquisition for the caller-declared exact parent/child/workspace/evidence universe; no prose-derived membership/routing; credential/raw-response/session boundaries remain intact.
- Reusable Recorder one-invocation seam without changing existing `ak-docket-record` behavior; Navigator terminating-tool/audit/receipt registration and sealing.
- Authoritative schemas or exported runtime schemas as required by the final authority; package files/bin/entrypoint/packed-artifact integration.
- ADR/README/CONTEXT updates, including the narrow ADR 0010 amendment and explicit distinction among Navigator, Assisted Runner, Recorder, caller, and existing roles.
- Production-seam tests for every red/green class enumerated in the final authority, including real temporary Git repositories/process settlements where relevant. Follow ADR 0016: test distinct authored logic, not formatting matrices.

Non-negotiable boundaries:

- Navigator never invokes roles, Agent, shell, Git/GitHub, filesystem traversal, worktree operations, or mutations.
- Assisted Runner never selects a role, schedules, continues a workflow, manages worktrees/queues, or enforces ordinary advice. It wraps exactly the caller-supplied packaged role and automatically consults Navigator.
- Recorder remains a one-native-invocation sealer.
- Existing Judge/Fixer/Coder/Reviewer/Collector/Doctor and direct Recorder behavior remain compatible.
- Unknown/infrastructure failures never impersonate role outcomes. No history rewrite, no push.

Run focused tests while developing, then the unified gates:

```text
npm run typecheck
HOME=$(mktemp -d) npm test
npm pack --dry-run
git diff --check
```

Submit `completed` only after committing the implementation and preserving TDD evidence plus same-pattern, introduced-regression, and behavior-fact checks in the report. If a literal external prerequisite makes a construction slice impossible, return a precise evidence-bearing `refused`; do not silently narrow scope.