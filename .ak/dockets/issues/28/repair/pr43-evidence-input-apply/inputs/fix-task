# PR #43 Fixer apply — acquisition/evidence/input classes

Apply exactly these Judge-authorized classes from `.ak/dockets/issues/28/judgment/pr43-review/receipt.json`:

- `NavigatorDecisionInputVisibility`
- `TrackerNullBodyNormalization`
- `EvidenceAdmissionContainment`
- `EvidencePagingCoverage`
- `AssistedInputContractClosure`

Follow every owner, boundary, disposition, and production proof. Use one bounded descriptor evidence seam, immutable admitted bytes, capability/root-verified handles, UTF-8-safe pages, interval coverage, unique generated/caller IDs, typed label mappings, and closed public inputs. Do not add timeout policy, second tracker, worktree manager, or infer from prose. Work test-first, rebuild, run unified gates, commit, and report exactly these keys.