# Issue #28 Fixer apply — convergence proof completion

Close the three open classes in `.ak/dockets/issues/28/judgment/final-convergence-retry-002/receipt.json`:

- `NavigatorEvidenceReadTruthResidual`
- `LedgerLoserRereadResidual`
- `CurrentHeadExecutableGate`

Do not add mechanisms. Correct the TS2554 test-harness error. Add executable tests through `createRecorderAssistedTransportV1` for live and `readSealed` out-of-catalog partial/complete native evidence reads and all required rejection cases. Add deterministic overlapping identical-resume and concurrent-recovery barrier/count tests, including pending fail-closed, canonical result/status, one recovery fact, and incompatible/hash-corrupt failure.

Run actual `npm run typecheck`, focused tests, isolated-HOME full suite, pack dry-run, and diff check on final HEAD. Commit forward and report all three exact class keys.