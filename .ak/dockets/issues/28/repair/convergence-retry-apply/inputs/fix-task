# Issue #28 Fixer apply — convergence retry residuals

Close exactly the two still-open classes in `.ak/dockets/issues/28/judgment/final-convergence-retry/receipt.json`:

- `NavigatorEvidenceReadTruthResidual`
- `LedgerLoserRereadResidual`

Fix read-record ordering canonically at the owning seam so native read order cannot invalidate truthful catalog-bound coverage, with real Recorder live sealing and readSealed recovery tests for all specified cases. Fence/reconcile canonical same-call lifecycle state before every launch-bearing transition, not only after append conflict, and add deterministic overlapping identical enter/resume and concurrent recovery barrier tests matching the Judge's count/result oracles.

Do not add a tracker, lock ledger, wait loop, scheduler, or parallel mechanism. Work test-first, rebuild, run unified gates, commit forward, and report exactly these two keys.