# PR #43 Fixer apply — Runner/ledger lifecycle classes

Apply exactly these Judge-authorized classes from `.ak/dockets/issues/28/judgment/pr43-review/receipt.json`:

- `LifecycleAdmissionExclusion`
- `RecoveryStateContinuity`
- `LedgerTemporaryPublicationSafety`
- `TypedMachineControlFlow`

Repair each complete class at the stated owner with every production-seam oracle. Preserve append-only/no-lock design, caller role choice, Recorder boundaries, and failure honesty. Do not add scheduler, timeout, retry cap, backoff, public error taxonomy solely for tests, or broad refactor. Work test-first, rebuild, run unified gates, commit forward, and report exactly these class keys.