# Issue #28 Fixer apply — final code classes

The owner-direct Navigator Soul repair has been committed separately as `0ec52558ef30901124bbb7723b9ecd3e3e1d0a7c`. Apply exactly the three code classes authorized by `.ak/dockets/issues/28/judgment/review-005/receipt.json`:

1. `Assisted run locator UUIDv7 predicate`
2. `Dense AssistedCall validator names`
3. `Tracker observation freshness digest`

Follow each Judge boundary/disposition and verification burden. Use the shared UUID predicate, perform only bounded local validator renames/readable reflow without contract redesign, and distinguish capture time from tracker observation time in canonical freshness. Add the production lifecycle regression proving observation-time-only tracker drift reconsults before role launch.

Do not touch Soul prose, add mechanisms, change role topology, or broaden naming cleanup. Work test-first, run unified gates, rebuild distributions, commit forward, and submit exactly these three `classesRepaired` keys with search scopes/exceptions.