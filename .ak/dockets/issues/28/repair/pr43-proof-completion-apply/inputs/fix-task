# PR #43 Fixer apply — complete authorized production proof matrix

Close every OPEN class in `.ak/dockets/issues/28/judgment/pr43-final/receipt.json`:

- `LifecycleAdmissionExclusion`
- `NavigatorDecisionInputVisibility`
- `TrackerNullBodyNormalization`
- `EvidenceAdmissionContainment`
- `SelectedSettlementEvidenceTruth`
- `RecorderArtifactAuthenticity`
- `RecoveryStateContinuity`
- `LedgerTemporaryPublicationSafety`
- `EvidencePagingCoverage`
- `NavigatorReceiptContract`
- `AssistedInputContractClosure`
- `SchemaContractParity`
- `NavigatorActivationAtomicity`

Implement the two reproduced defects (nonblocking regular-file admission before FIFO open; optional callId shared locator validation) at existing seams. Add every exact production-seam test/oracle required by each Judge disposition—do not substitute mocks, direct helper-only assertions, title claims, post-extraction mutation, or package-presence inspection where a real adapter/runtime/Recorder/schema/CLI/cold-package seam is required.

Preserve all rejected policy/style dispositions: no timeout, retry cap/backoff, lock, scheduler, second tracker, raw environment promotion, orchestration dependency, or style-only extraction. Work test-first, rebuild, run real typecheck/isolated full suite/pack/diff gates, commit forward, and report exactly the 13 keys with honest search scopes and exceptions.