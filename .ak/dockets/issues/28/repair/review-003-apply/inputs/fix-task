# Issue #28 Fixer apply — Review-003 authorized classes

Apply exactly the five repair classes authorized by `.ak/dockets/issues/28/judgment/review-003/receipt.json` at current HEAD. There is no Fixer Plan leg.

Required class keys:

1. `NavigatorEvidenceReadTruth`
2. `RecorderFailureTruth`
3. `LedgerLoserReread`
4. `IdentifierAndSnapshotClosure`
5. `PromotedArgvCredentialBoundary`

For each class, follow the Judge's owner, boundary, disposition, and verification burden. Repair the full class at the owning seam, not just cited examples. Preserve all Issue #28 final authority boundaries and existing role/Recorder compatibility.

Specifically do not:

- create a second evidence tracker;
- manufacture hashes/references from arbitrary exceptions;
- weaken atomic no-replace ledger publication or add a parallel lock ledger;
- create another UUID validator family;
- replace the credential boundary with an unauthorized argv allowlist;
- broaden into naming churn, lifecycle-owner decomposition, orchestration, worktree management, or role scheduling.

Work test-first for each distinct authored logic class. Run focused production-seam tests and then:

```text
npm run typecheck
HOME=$(mktemp -d) npm test
npm pack --dry-run
git diff --check
```

Commit forward. Submit `completed` with `classesRepaired` containing exactly the five keys above, each with honest search scope and exceptions. If the authorized boundary is impossible, return evidence-bearing `refused`; do not silently narrow or relabel.