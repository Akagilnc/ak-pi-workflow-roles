# Issue #12 PR #39 CodeRabbit adjudication

Adjudicate all inline comments in `.ak/work/issues/12/pr39-comments.json` against current HEAD and authority. Do not edit code. Grouped questions:

## Immutable and documentation

1. Two comments request rewriting archived final-authority input exhibits (`owner-authority-03/06`). These are immutable copied historical evidence; determine dismissal versus a forward current-authority clarification. Never rewrite docket history.
2. CONTEXT StatsLine glossary, ADR 0012 R1 encoding language, and ADR 0013 target enum/key language may duplicate runtime/schema mechanics. Authority explicitly required forward ADR/CONTEXT updates but also says glossary is not a second contract. Decide exact thinning, if any, without losing durable policy.

## Production contracts

3. `canonicalJson` silently aliases/drops unsupported values such as `undefined`; decide closed JSON-domain validation and typed failure.
4. `isStatsLine` catches every validator exception; decide expected typed validation failure versus unexpected-cause propagation.
5. Direct exported `validateDoctorEvidenceIndex` permits committed entries without resolver identities; decide whether all committed kinds must require resolver-derived identity.
6. `src/recorder/manifest.ts` allegedly validates only shallow nested shape before casting RecorderManifestV2, allowing malformed nested literals into StatsLine. Verify current shipped schema/parser ownership and define exact repair without a parallel schema core.
7. `createGitCommittedSnapshot` accepts unvalidated `targetCommit`, potentially option-like values. The repo supports SHA-1/SHA-256 elsewhere; define full commit/object-format law rather than blindly hardcoding 40 if inappropriate.
8. StatsLine validator allegedly omits cross-metric laws `source.manifests.length === recordedInvocations.total` and `judgeContinueCount <= byRole.judge`.

## Tests/package seams

9. Package registration test checks checkout paths rather than packed artifact inclusion.
10. StatsLine validation failures lack typed code/metric identity and tests assert only generic Error; determine anchoring/failure-honesty repair.
11. Evidence-resolution test duplicates canonical serialization helper; canonical-json tests lack unsupported-value regression.

For each comment, sustain/dismiss with current counterexample and exact owner/boundary. Consolidate shared roots. Historical docket input rewrites are prohibited. No Plan unless a genuine major unresolved interface choice exists; otherwise direct Fixer classes. Submit via `ak_judge_output`.
