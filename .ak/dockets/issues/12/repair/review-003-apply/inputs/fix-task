# Issue #12 Fixer Apply — canonical JSON owner and receipt-byte classification

Sole authority: `.ak/dockets/issues/12/judgment/review-003/receipt.json`.

Repair both exact classes:

- `canonical-json-owner`: one internal canonical JSON serializer shared by Doctor contract/evidence consumers; preserve exact raw committed sealing and normalized canonical identities, no second serializer.
- `stats-line-receipt-byte-classification`: implement exact complement law. Recognized non-workers always paper; recognized Coder/Fixer exact single corresponding `apply` is apply, every other phase state paper; ambiguous/missing/unknown role is unclassified and ratio unavailable. Every classified verified receipt enters exactly one bucket.

Add logic-focused canonical semantics and table-driven production classification/byte-conservation coverage specified by Judge. Preserve closed metrics, determinism, F1-F5 and package behavior. Regenerate artifacts; run focused, typecheck, isolated-HOME full, package/cold-install, pack dry-run, diff check; commit forward and submit exactly both keys.
