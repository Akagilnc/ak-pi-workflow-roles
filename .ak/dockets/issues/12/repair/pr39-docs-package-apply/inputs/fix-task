# Issue #12 Fixer Apply — forward docs and packed-artifact regressions

Sole authority: `.ak/dockets/issues/12/judgment/pr39-review/receipt.json`.

Repair exactly:

- `forward-documentation-thinning`: thin current CONTEXT StatsLine glossary to role meaning; thin ADR 0012 to durable R1 policy without field/discriminant/presentation encoding. Do not edit archived docket inputs or ADR 0013.
- `packed-artifact-and-seam-regressions`: replace checkout-path package assertions with the existing real isolated pack/tarball file list and assert Doctor Soul/contracts/StatsLine/canonical module are shipped; retain role/flag assertions. Ensure owning suites contain the authorized typed/canonical counterexamples once, with no duplicate test serializer.

Preserve all production fixes and no doc-as-contract duplication. Do not repair other classes. Run focused, typecheck, isolated-HOME full, package/cold-install, pack dry-run, diff check; commit forward and submit exactly both keys.
