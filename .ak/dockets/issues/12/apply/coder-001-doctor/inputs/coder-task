# Issue #12 direct construction — Doctor v1 and StatsLine v1

Implement the complete direct-construction authority in `.ak/dockets/issues/12/judgment/final-construction-authority/receipt.json` on the current post-#33 native-session Recorder base. No Coder Plan is authorized or required.

Deliver all seven bounded classes as one coherent package delivery:

- `authority-contracts`
- `stats-line-v1`
- `doctor-evidence-intake`
- `doctor-output-contract`
- `doctor-role-runtime`
- `doctor-soul-audit`
- `package-and-regression-surface`

Follow each owner/boundary/disposition exactly. Incorporate owner R1 + L1: `lastRealBite` is the required actual-citation/noRealBite union; `keep` requires actual citation; noRealBite permits only thin/delete; the host four-reason law must not enter Doctor Soul/output/runtime/tool descriptions.

Build production contracts and runtime seams first, then focused tests that follow distinct owned logic. Preserve caller-owned composition/manual summoning, the evidence-only factory patient, one existing docket ledger, exact five target kinds, frozen digest-bound intake, constrained evidence tool, sole final Doctor output, same-model compliance audit, typed unavailable statistics, deterministic committed-snapshot enumeration, exact role/phase classification, and package/cold-install surfaces. Do not repair or route #13/#17/#22/#26 and do not add scheduling, cadence, memory, tracker querying, role topology, or README law.

The merged Recorder is v2 native-session authority; adapt StatsLine to its current shipped manifest/receipt contracts rather than reviving v1 transport assumptions. Authority statements about current observability remain controlling unless #33 now supplies typed proof; never infer unavailable metrics from prose, paths, Git cadence, or raw sessions.

Use TDD at authored seams. Update ADR 0012-0014, CONTEXT, and README only within their authorized ownership. Export and package every public module/Soul needed by a cold consumer. Run focused tests, typecheck, isolated-HOME full tests, package/cold-install checks, `npm pack --dry-run`, and `git diff --check`; commit forward. Submit through `ak_coder_output` with truthful status and evidence.
