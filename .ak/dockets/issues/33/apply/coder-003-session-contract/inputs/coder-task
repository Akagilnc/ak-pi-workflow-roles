# Issue #33 direct Coder Apply slice A — config and session integrity

No Plan. Continue from current HEAD under the converged Issue #33 authority and Coder-002 receipt. Complete this bounded construction slice and commit forward.

Own the full production-seam matrix for:

1. config v2 exact-object/state rules, v1 rejection, canonical UUIDv7/session path, forbidden Pi flags, direct executable/print-mode protocol, injected args/env, and every rejection before the counter child spawns;
2. exclusive 0700 session-leaf creation, ancestor containment/symlink law, `(dev,ino)` binding, zero/multiple/extra top-level entries, optional bounded `reviewer-legs/`, nested/symlink/irregular leg rejection, and raw-session non-promotion;
3. main-session no-follow open and fd/path/inventory mutation checks, including deterministic replacement/growth/shrink/metadata/inventory races without adding a production test hook;
4. Pi v3 streamed JSONL structural contract: closed header, canonical ISO/cwd/id/no-parent, final newline, line/entry bounds, blank/non-object/malformed/duplicate ids, exact linear parent chain, truncation and second header.

Restore/adapt useful historical real-seam tests rather than replacing the above with a handful of helper examples. Repair implementation owners exposed by the matrix. Do not touch unrelated role/output/transaction slices unless required by a shared invariant. Run focused tests plus typecheck/build and enough full-suite evidence to detect regressions. Submit `completed` for this slice if and only if its whole matrix is proven; do not claim whole Issue #33 closure. Otherwise refuse precisely. Sol low, direct Apply, no Plan.
