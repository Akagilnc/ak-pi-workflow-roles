# Issue #33 single direct Coder Apply — finish the whole ticket

Continue from current HEAD and complete the entire converged Issue #33 construction authority. No Plan. This is one whole-ticket Coder invocation, not a slice.

The prior Coder-001 through Coder-004 `refused` receipts were induced by caller prompts that incorrectly treated ordinary remaining work as refusal. They are WIP evidence only and do not narrow authority or define new slices. Incomplete implementation, failing tests, a large remaining matrix, or the need for more TDD cycles are **not lawful refusal reasons**: keep working within this invocation, use compaction if needed, repair production seams, and do not submit a partial result.

`refused` is reserved for a concrete authority contradiction, unavailable required capability/dependency, or infrastructure condition that makes continued lawful construction impossible. If work merely remains, continue it. Submit `completed` only after the whole Issue #33 contract and complete C8 matrix are delivered at current HEAD.

## Whole-ticket completion burden

Implement and prove every class in `.ak/dockets/issues/33/judgment/design-authority/receipt.json` and its adopted candidate, including the Judge correction for complete rejected attempts before one final acceptance. Preserve the existing forward implementation where correct; deepen or replace it where incomplete.

Close all remaining production-seam behavior, including:

- exact config-v2/Pi-only pre-spawn law and injected argv/environment with real counter-child proof;
- exclusive session identity, containment, inventory, fd/path races and all exact stream boundaries;
- full Pi-v3 corruption and lifecycle matrix;
- valid Coder/Fixer/Judge/Reviewer/Collector receipt extraction, Collector projection, audit usage and rejected-attempt laws;
- promoted-derivative credential scanning and validation;
- child exit/signal truth versus sealer failure precedence;
- declaration admission before spawn, atomic/no-replace publication races, cleanup and raw-session exclusion;
- bounded/backpressured pass-through with >2 GiB logical proof and modest spawned integration;
- closed v2 config/manifest/failure schemas, generated dist, README usage, package inventory and cold installed-bin behavior;
- deletion of every old v1/machine-envelope/dual-representation compatibility branch while retaining valuable invariant tests in v2 form.

Do not waive or silently skip required coverage, mutate frozen migration baselines to hide drift, or count prior-head gates as current evidence. Run focused TDD, typecheck, build, the complete isolated-HOME suite, package/cold-install lifecycle, `npm pack --dry-run`, and `git diff --check`. Commit forward and submit one whole-ticket `completed` receipt with exact current-head evidence.

Do not implement #12/#13/#22/#26, modify Souls, add workflow topology, or reintroduce Plan.
