# Issue #33 direct Fixer Apply — Review-011 closure classes

Sole authority: `.ak/dockets/issues/33/judgment/review-011/receipt.json`. Apply directly; no Plan.

Repair exactly these complete bounded classes:

- `ISS33-CLEANUP-FAILURE-HONESTY`
- `ISS33-ONE-CORE-ARRAY-ADAPTER`
- `ISS33-SECURITY-SOURCE-READABILITY`

For cleanup failure honesty, preserve the primary Recorder/child truth, exit 125, no publication, and cleanup attempt; add only the smallest typed scanner-safe secondary cleanup evidence in the existing failure owner/schema/runtime. The focused oracle must induce a real filesystem cleanup failure without a test-only production hook. Never expose raw cause text/path/credentials.

Delete the test-only exported array wrapper and make focused tests exercise `AcceptanceCollector` directly. Rename only Judge-sustained trust-path bindings in `run.ts`; leave `dest` and `err` alone and add no behavior for readability.

Preserve the zero-finding Spec closure, amended C8, owner option 4, rejected fixture duplication, and all other closed classes. Rebuild generated artifacts. Run focused red/green tests, typecheck, isolated-HOME full tests, package/cold-install checks, `npm pack --dry-run`, and `git diff --check`. Commit forward and report typed `classesRepaired` for exactly these three keys if completed.
