# CONTEXT — @akagilnc/pi-workflow-roles 词表

> 只放术语,零实现细节。决策的为什么在 `docs/adr/`。

- **角色(Role)**:有明确职掌、受门禁约束、以交卷物为法定出口的**车间内**治理单元（默认 typed；游奕使按陛下原话以散文为出口）。角色只管一次调用的内政。按派单权分**寺监级**与**省部级**两品级（法律分类，非 runtime 品级机制）。实现不限于 LLM：确定性机制亦可为角色。见 ADR 0010、0047、0051。
- **Soul**:LLM 角色的身份与不可约判断原则,经系统提示注入。分**通用层**（本包内,零业务词）与**业务 overlay**（宿主项目附加）。审刑院与门下省另有共享执法准绳；确定性角色无 soul。见 ADR 0005。
- **角色方法 Skill(Role method Skill)**:供一个角色执行具体任务方法的包版本化材料；Soul 持判断原则，Skill 持可替换方法步骤。强制 Skill 是包的运行依赖。见 ADR 0032、0082。
- **角色门禁(Role gating)**:车间内的机械限制——对 LLM 角色是工具集收窄与调用拦截,对确定性角色是其能力边界。区别于 soul 的文本约束。见 ADR 0008。
- **交卷工具(Submission tool)**:角色具名的 terminating 工具(`ak_<role>_output`)。**回执(Receipt)** = 其产物,是角色劳动成果的法定出口（默认 typed；游奕使例外为散文）。工具＝回执通道：handler 只记录,不校验形状、不判内容、不丢字段。无回执终局是生命周期对「未获接受回执」的 typed 事实,不是回执。并非每个角色都有交卷工具。见 ADR 0003、0041。
- **审核席(Review seat)**:父席交卷后由代码按受审物传召的审核角色（将作监/修内司→台院、判官/给事中→符宝郎、判官→审刑院等）。代码只读结论字段排队；对方原文递回；读不出三态则 resume 说话者本人。见 ADR 0055、0079、0080。
- **格式契约(Format contract)**:在一个具名输入、输出或持久化边界上,由真实生产路径执行、会改变接受或拒绝结果,并且有明确 owner 与 consumer 的格式不变式。同一契约的多种表达不是多个契约。见 ADR 0036。
- **最小必需验证(Minimum-required validation)**:输入输出只验证必须有的;除此之外一概不管。见 ADR 0025。
- **形状校验(Shape validation)**:拒收理由**只涉数据的排布**（在场/缺席、键拼写、基数、类型、跨字段组合）。一旦拒收理由需要引用外部可观察事实或世界规则,即**非**形状校验。代码对角色输出的形状拒收权与中止权归零（CLAUDE.md 第 0 条）；该禁令只约束代码,审刑院等 LLM 角色打回不在禁止之列。见 ADR 0055。
- **记账位(Ledger slot)**:每份角色输出唯一一个精确 key 及取值域(如 `judgeStatus`),供落账、呈现与渲染分支。不是编排控制流；组合与顺序归调用者。见 ADR 0010、0040、0057；CLAUDE.md。
- **承接者判据(Successor test)**:**阻塞项（现行规则唯一出处）**——删与留之争的可核验判据：**删掉之后,那一类失败还有没有人接?有人接,删就是正事;没人接,保留就是容错。**「容错」本身不可核验,承接者可以:去代码里看该失败类别的处理路径是否与既有机制相交。本判据不倒置 ADR 0036 的举证责任——举证仍在保留方,只是把「特别理由」钉成「指出无替代承接者」。**无承接者是保留的必要条件,不是充分条件**:仍须过护栏三问,且不得落入 ADR 0036 的失格类别。关联：ADR 0036；`souls/quality-law.md` 三问。
- **同类扫描(Class-wide scan)**:以会拒绝输入输出的同类行为为范围扫描全仓;清单 ID 与已知文件只是不完全实例索引,不是施工白名单。见 ADR 0045。
- **语义 JSON 校验(Semantic JSON validation)**:对 JSON 值的生产语义进行校验。见 ADR 0021。
- **发布 Schema(Published schema)**:供包外机器消费者使用的机器可读契约投影。见 ADR 0022。
- **边界 Schema 真源(Boundary schema owner)**:定义单个工具输入或输出形状的唯一 Schema。见 ADR 0023。
- **模型自报(Model self-report)**:角色在回执中声明、但未由拥有该事实的生产接缝现场观察的值。自报可留叙事供人参考,不得伪装成机械事实或进入统计真源。见 ADR 0024、0042。
- **Judge(大理寺)**:只判卷、不改码、不 commit 的裁决角色。canonical 名;`verify` 为历史别名。票庭审读归给事中；本席专事后判卷。见 ADR 0074。
- **Fixer(修内司)**:以 `plan`/`apply` 阶段处理调用方修理包；apply 按 finding 结算为完成或合法拒绝，混合称 `partially_completed`；另有 `unfinished` 交棒。见 README Fixer；ADR 0015、0034、0050。
- **Coder(将作监)**:以 `plan`/`apply` 完成首次实现或据理拒绝派单的角色。apply 绑定包内 canonical TDD 方法 Skill；可用 `unfinished` 交出未结清范围。回执不以新 commit 为无条件前提。见 ADR 0024、0032、0034、0050、0082。
- **未完终态(Unfinished)**:worker 角色 apply 阶段的合法交卷状态，语义为**受阻求援**——仅当前置缺失或违宪导致无法完成时可用；是可续交棒,不是失败,也不豁免验收。见 ADR 0050。
- **Reviewer(御史台)**:围绕固定目标形成独立、可追溯代码评审的寺监级角色;不派 worker、不修复、不发布、不作最终裁决。绑定 `ak-cross-m-review`；单次调用一个冻结 lens。见 ADR 0010、0031、0032、0082。
- **门下省(Gate province)**:审署诏敕与质量保证的省部级席位。调用者可经公开入口单独传召；交卷闸不再主动传召本省。省不是外层编排器；各官仍是独立角色。见 ADR 0067、0074、0079。
_Avoid_:把「门下省」当作通进司的公开角色名。
- **中书省(Secretariat)**:改票的出令省；按《票面法》把草稿修成可送庭文书，并在一次调用内驱动给事中复审至署或上呈。暂不细分席位。
- **给事中(Countersign)**:门下省下的**票庭审读官**。开工前的派单、方案、处置案先过本席；以起居录核授权。非闸派——由调用者开工前传召。见 ADR 0074、0075。
- **左拾遗(Gleaner-left)**:门下省下合并前无锚定风闻官；只上弹章、不封驳不裁决。由调用者合并前传召。见 ADR 0067。
- **台院(Inspector)**:纠举推鞫官；审**复杂度**与**测试质量**。受审物是将作监/修内司交卷；交卷闸按受审物直接传召。机器键 `inspector`。见 ADR 0074。
- **符宝郎(Document-fidelity auditor)**:门下省下独立文书核验角色（寺监级）。首责：**核实实际授权出处**。受审物是大理寺拟判与给事中署章；交卷闸直接传召。见 ADR 0067、0074、0075、0079。
- **起居录(ticket-provenance)**:每票一份、每票一个文件的共同案卷，内容**仅限陛下与 runner 的对话**；排除按来源不按正文。不同于一次运行的卷宗。见 ADR 0075、0081。
- **起居郎(diarist)**:判断本庭对象是哪张票、本票对话起止于何处的记录者；正文与指针由机械从会话卷原样搬运。不是设计批准者或施工指挥者。见 ADR 0075、0081。
- **通进司(Collector)**:门下省下的收证衙门。认票、收证、按机器身份分组提交自包含回执；不评审、不裁决、不修复、不路由。canonical 键 `collector`。见 ADR 0067。
_Avoid_:门下省（那是省名）。
- **评审腿(Review leg)**:公开 Reviewer 命令省略 `--lens` 时，共享执行接缝并行发起的 completeness／correctness 普通单轴 Reviewer run；命令本身不建立父 run。见 ADR 0010。
- **Soul 审刑院(Soul-compliance audit)**:独立的实质审计角色,自行取证并判断「该有的有没有」与「有的对不对」。见 ADR 0055、0062。
- **卷宗(Dossier)**:一次 run 在候簿记录之家里的全部既落账材料；卷宗即真源,无投影副本。定位靠机器注入的 typed 指针,禁 latest-run/mtime/全局扫描猜测。
- **先立卷后审卷**:**阻塞项（现行规则唯一出处）**——被审对象必先落账,审计只从账上读；手递手传料非法。缺卷或缺被审对象走既有非零故障通道（`missing-dossier` / `missing-subject`）,public CLI 无合法 Receipt。实现事实见 `src/dossier-resolution.ts`。
- **绑定(Binding)**:targetHead 一类对象同一性机械校验能力；现行为 deferred，无通用绑定闸。见 ADR 0004、0027、0037。
- **Navigator(游奕使)**:由共享角色生命周期自动旁听包角色结算的独立领航席；建议最低成本且安全的下一包角色/phase。不裁决、不授权、不执行；普通建议允许偏离。见 ADR 0061。
- **路书(Route playbook)**:游奕使用于专业判断的一组非约束参考路线。_Avoid_:默认工作流、路由表、自动编排规则。见 ADR 0061。
- **角色调用(Role invocation)**:一个角色从输入到回执的单次独立劳动。角色只拥有该次调用的内政;调用者拥有组合、顺序、重复、预算和停止条件。见 ADR 0010。
- **公开角色 CLI(Public role CLI)**:包外调用者使用角色包的唯一受支持产品入口；接收调用请求并交付终局结果，不替调用者选择、组合或续跑角色。见 ADR 0052、0082。
_Avoid_:把裸 Pi 角色入口、session 文件或事件流称为公开 CLI。
- **内部角色入口(Internal role entrypoint)**:获授权的包开发 session 用来直接激活和诊断角色的仓内接缝，不是外部产品面。_Avoid_:公开入口、备用 CLI。见 ADR 0082。
- **调用请求(Invocation request)**:一次角色调用的输入，由可选 opaque instruction、零到多个 attachments 与少量角色专属参数组成；内容是否充分由角色判断。见 ADR 0052。
- **附件(Attachment)**:调用者明确附给一次角色调用、并在受理时冻结内容的材料；resume 继续消费同一份材料而非路径上的后来版本。
- **终局结果(Terminal result)**:公开角色 CLI 对一次已受理调用交付的完整结果，汇合角色结算、Navigator 出席事实与声明的 artifacts；无已接受回执时可汇合 typed 无回执生命周期记录（该记录不是回执）。见 ADR 0041、0080。
- **角色运行(Role run)**:一次已受理角色调用的持久执行身份，连接其调用请求、session 与终局结果，并可在改选模型后继续同一现场。
- **候簿(Ledger book)**:包所有的机器级记录之家,按主仓分簿(键=git common dir 宿主目录的 basename)。记录落点的唯一真源;消费者仓零侵入。见 ADR 0048、0049。
_Avoid_:家册、账本目录、工作区记录。
- **司天台(Archivist)**:记录的所有者。职掌：**如实记录**与**生成高阶数据**（分析席由太史承担）。确定性机制,非 LLM 角色。见 ADR 0047、0065、0077。
_Avoid_:Recorder、Docket、遥测。
- **太史(Analyst)**:司天台的分析席。只读司天台记录、生成高阶数据;确定性机制,非 LLM 角色,可单独调用。见 ADR 0068。
_Avoid_:遥测、metrics-service、Telemetry。
- **Artifact reference**:终局结果中声明的本地材料引用，用于打开完整报告、证据或错误详情；补充内联核心结论，不替代结论。
- **引擎（Engine）**:角色劳动的执行后端。默认=角色 session 在 pi 内自跑；可经**外包**把推理段交给本地 CLI，内容交回同一 session——治理面永远在 pi。见 ADR 0069、0071。
- **编排器(Orchestrator)**:包外的交通系统——起各角色进程、递材料、按三态判词走边。它只读回执,不定义交卷形状。本包不含通用编排器。见 ADR 0010。
- **三态判词**:`converged` / `continue` / `escalate`。给事中票庭语义映射:署／封驳／上呈。环境/工具链故障不是判词,走非零退出故障通道。
- **裁类循环（Class-repair loop）**：由判词类字段、回执对账键、圈界参数三份合同自然组成的修理循环；次序是合同的推论，非规定流程。见 ADR 0015。
- **Merger（校书郎）**：保全双方已授权意图并完成一次普通双亲 merge commit；无进行中合并、无活可干、或需新意图/权力决定时升级；不发起、不发布、不编排 merge。见 ADR 0027。
- **尚书省（Marshal）**：审→判→修 质量收敛环的省部级驱动角色；不弹、不判、不修，只让链条转到收敛。canonical 键 `marshal`；席位待落地，仍为现行产品词汇。见 ADR 0051、0053；README。
- **Doctor(太医署)**:从一个保留的 Pi 原生 session-dir 案例读取工厂症状，产出单案过程成本诊断并开方的举证角色;不施工、不裁决、不改法。见 ADR 0012、0013、0017。
- **工厂(Factory)**:车间整体——角色、闸、法、包模板、流程站点。太医署的唯一病人;案子只是症状载体。见 ADR 0013。
- **大扫除(Factory cleanup)**:按最小完整责任边界删除无收益机制及其专属格式、适配、测试和文档的治理动作；允许扩大接受范围或删除整个机制,不是行为等价重构。见 ADR 0036、0045。
- **落地周期(Issue-to-merge lead time)**:从第一个获授权处理 issue 的角色调用起,到 merge commit 进入目标分支为止的端到端时长。工厂进度板呈现用词。见 ADR 0053；`src/factory-board.ts`。
- **方子(Prescription)**:太医署的 finding 加处置建议;治系统不治症状,同任何提案一样走法链。见 ADR 0012、0013。
- **真咬人(Real bite)**:某道闸最近真拦下东西的证据——保留 session 中实际被接受的 typed terminating tool result;回声与散文不构成证据。`noRealBite` 是无咬人证明。见 ADR 0012；doctor 输出合同。
- **过程成本报告(Process-cost report)**:由保留 runs 路径中的 Pi session 字节可重算的单案过程成本诊断；趋势是读取多案后的独立输出类型。见 ADR 0017。
